# Kitchen Table — the 1.0 gameplan

**Status:** `v0.9` · calendar + database shipped 2026-08-02 · **Phase 16, the video importer (Jason's spec), shipped 2026-08-02** — `V01`–`V05` all done; only the video conversion lives on Render, the app stays static · of the original 130: 79 done · 20 struck-as-superseded (`096` joined the list — the Render server *is* the small worker) · 31 open, nearly all needing a person (§11) · **to light the kitchen server up: three env vars in Render + one deploy (`backend/README.md`)**
**When this file is fully ticked, the app is version 1.0.**

---

## 1. What this is

Thirteen roles produced 39 projects and 130 tasks. This file throws away the
role grouping and puts all 130 in **the order they actually have to happen** —
the thing that unblocks the most comes first, the thing that depends on it comes
after.

It is written to be *looped*: one task per pass, tick the box, commit, repeat.
It does not need to be held in anyone's head between sessions. The file is the
memory.

```
   130 tasks   ·   15 phases   ·   4 acts   ·   1 gate

   ACT I     Find out what's true          Phases 1–3     tasks 001–034
   ACT II    Make it safe, make it designed Phases 4–6     tasks 035–061
   ACT III   Make the content true          Phases 7–10    tasks 062–089
   ACT IV    The fork                       Phases 11–15   tasks 090–130
```

Act IV only happens if the gate in §6 opens. If it doesn't, **1.0 ships at the
end of Act III** and 41 tasks are struck with a one-line reason. That is a
success, not a shortfall.

---

## 2. The loop

Repeat until the release checklist in §9 is clear. **One task per pass.**

```
 1. READ      Open this file. Read §3 (rules) and §6 (the gate).
 2. PICK      Take the lowest-numbered unticked task in the current phase
              whose "Needs" are all ticked. Never reach into a later phase.
              A task whose "Needs" include a parked task is parked too —
              note in §11 what it is transitively waiting on.
 3. TRIAGE    Check the marker:
                🤖  do it now
                🤝  do it now if the one answer it needs is already in §11;
                    otherwise write the question into §11 and pick again
                👤  you cannot do this. Write what you need into §11 and
                    pick again.
 4. DO        That one task. Not the adjacent one that looks related.
 5. VERIFY    Against its "Done when". If you cannot verify it, it is not done.
 6. CHECK     Run the six suites and the contrast audit, both themes. Red means
              you are not finished, even if your task passed.
 7. TICK      Change [ ] to [x]. Add one line to §10 — what changed, or what
              you learned. One line. Not a report.
 8. DOCUMENT  Apply the Technical Writer duty in §5.
 9. COMMIT    One task, one commit: `P07/065 — photo lightbox on the recipe page`
10. LOOP      Back to 2. When every task in the phase is ticked or parked,
              run the phase-close checklist in §4 and move to the next phase.
```

### Stop and ask when

- A task needs a colour, a type size, or a font that `tokens.css` does not have.
  **That is a question, never a value to invent.** (`CLAUDE.md`, rule one.)
- Two tasks disagree, or a task turns out to be impossible as written.
- A task would change something a person already decided in an earlier phase.
- You are about to enter Phase 11 or Phase 15 and the gate in §6 is unresolved.
- A 🤖 task turns out to need a person after all. Re-mark it 👤 in this file,
  say why, and move on.

---

## 3. Rules that do not change

1. **`tokens.css` is the palette.** Zero hardcoded hex outside the `@media print`
   block. This survives every phase.
2. **One font: Atkinson Hyperlegible**, weights 400 and 700 only.
3. **Nothing interactive under 44px. Icon buttons 48×48. No hover-only anything.**
4. **Recipe instruction text stays at 24px at the default step.**
5. **Never write recipe content a person did not give you.** No inferred
   servings, no guessed ingredients, no invented tags. The whole point of Act III
   is undoing the last round of that.
6. **Never tick a box you did not verify.** A parked task is honest; a falsely
   ticked one poisons every task that lists it under "Needs".
7. **One task, one commit.** A phase is reviewable; a 40-task commit is not.
8. **Never skip forward** to something more interesting. The order is the product.
9. **Both themes, every time.** A change that is only checked in dark is not
   checked.

---

## 4. Phase-close checklist

Before starting the next phase, all five:

- [ ] Every task in the phase is `[x]` or listed in §11 with what it is waiting for.
- [ ] The six suites and the contrast audit are green.
- [ ] `README.md` reflects anything a reader would now experience differently.
- [ ] One line in §10 summarising the phase.
- [ ] The counter at the top of this file is updated.

---

## 5. The Technical Writer duty — `README.md`

`README.md` is the front door of the GitHub repo and the only documentation
anyone outside this project will ever read. It goes stale silently, so it is not
one task at the end — **it is a duty attached to the loop.**

**Every phase closes with a README pass.** Ask one question: *would someone
reading this now be told something that is no longer true?*

These tasks change the README directly and are not done until it is edited:

| Task | What moves in the README |
| --- | --- |
| `028` | The category-versus-tag rule, in *Photos and tags* |
| `049` | Whether fonts are self-hosted, in *Files* |
| `061` | A link to the component reference |
| `067` `068` `069` | Tag autocomplete, bulk tagging, rename/merge, in *Photos and tags* |
| `079` | The whole *Adding a recipe* section, rewritten for someone who has never used GitHub |
| `080` | Cross-check `README.md` against `CLAUDE.md` — they drift apart, not together |
| `087` `088` | What search now matches, in *Screens* |
| `092` `104`–`112` | If a server exists, the README's core premise ("no server") is wrong and must be rewritten |
| `129` | The calendar, if it ships |

The README also carries the **live status line** — one sentence near the top
saying which version the app is on. Task `080` owns keeping it accurate.

---

## 6. The gate

Everything in Act IV hangs off **one unanswered question**, and it is task `026`.

```
                          026 · the backend memo
                                    │
                 ┌──────────────────┴──────────────────┐
                 │                                     │
             NO SERVER                              SERVER
                 │                                     │
     1.0 ships at end of Act III          Act IV runs: Phases 11–14
     Phases 11–14 struck (30 tasks)       then Phase 15 if 030 says yes
     Phase 15 runs client-side            1.0 ships at end of Phase 15
     if 030 says yes (11 tasks)
```

**Do not build speculatively past this line.** Building a server nobody decided
to run is the most expensive mistake available to this project — it is 30 tasks,
a monthly bill, and a database that becomes the only copy of 48 irreplaceable
recipes.

Phase 15 (the calendar) has its own smaller gate: task `030`. A calendar can be
built client-side without a server, but a meal plan only one phone can see is
close to useless — so `030` decides whether it is worth building either way.

**Key:** 🤖 an agent can do this alone · 🤝 needs one answer first ·
👤 needs a person, a phone, or a conversation

Of the 130: **74 are 🤖**, **24 are 🤝**, **32 are 👤**. The 👤 tasks are not a
problem with the plan — they are the reason this app is worth building. Nobody
can watch Joan cook except a person.

---

# ACT I — Find out what's true

*34 tasks. Nothing here builds anything. Every decision in Act II and every line
of content in Act III cites something measured here.*

## Phase 1 — Ground truth

*14 tasks. Nothing in this phase depends on anything. It can start today.*

- [x] `001` 🤖 **SEC** — Re-check that no GitHub token remains anywhere in the codebase or its history, after the publish flow was removed.
  - *Needs:* — · *Done when:* a full history scan is clean, and the result is written down. **Do this first.** If something leaked, everything else waits.
- [x] `002` 🤖 **QA** — Commit the six test suites into the repo so the whole set runs from one command.
  - *Needs:* — · *Done when:* a clean clone runs all 210 checks plus the contrast audit with one command.
- [x] `003` 🤖 **OPS** — Wire GitHub Actions to run those suites on every pull request.
  - *Needs:* `002` · *Done when:* a deliberately broken PR goes red before merge.
- [ ] `004` 👤 **QA** — Open every screen on a physical iPhone and confirm Atkinson Hyperlegible actually renders.
  - *Needs:* — · *Done when:* confirmed by eye against a fallback screenshot. **If this fails, the typographic contract is broken and Phase 6 changes shape.**
- [ ] `005` 👤 **QA** — Test Save to Notes through the real iOS share sheet, end to end.
  - *Needs:* — · *Done when:* a recipe arrives in Notes, readable, from a real device.
- [ ] `006` 👤 **QA** — Verify the wake lock holds through a genuine 40-minute bake, including backgrounding the app.
  - *Needs:* — · *Done when:* the screen is still on at the end, after a phone call or a switch to another app.
- [ ] `007` 👤 **QA** — Print a recipe to real paper and confirm nothing is white-on-white.
  - *Needs:* — · *Done when:* a printed page is legible in both themes' starting states.
- [x] `008` 🤖 **QA** — Confirm a removed recipe stays removed across a reload, a theme change and a browser restart.
  - *Needs:* — · *Done when:* covered by an automated check, not a manual pass.
- [x] `009` 🤖 **QA** — Test "Undo all my changes" from the Menu empty state — the only route back when everything is removed.
  - *Needs:* — · *Done when:* automated, from a state with zero recipes.
- [x] `010` 🤖 **FE-A** — Measure localStorage headroom with 48 photos attached and document the ceiling.
  - *Needs:* — · *Done when:* a number is written into §10. This decides task `062`.
- [x] `011` 🤖 **QA** — Fill localStorage to the quota with photos and confirm the failure message appears rather than a silent loss.
  - *Needs:* `010` · *Done when:* the quota message is asserted by a test.
- [x] `012` 🤖 **FE-A** — Benchmark first contentful paint on a throttled 3G profile and set a budget.
  - *Needs:* — · *Done when:* a number and a budget are recorded, and CI can fail against the budget.
- [x] `013` 🤖 **PM** — Start the visible list of known-wrong data, so it is never mistaken for polish work.
  - *Needs:* — · *Done when:* every inferred serving, empty ingredient list and truncated step is enumerated in one place. This is the input to Phase 8.
- [x] `014` 🤖 **SEC** — Write down explicitly that contributor names are not authentication, so nobody builds on them later.
  - *Needs:* — · *Done when:* it is in `CLAUDE.md`, not in a chat message.

## Phase 2 — Watch Joan

*10 tasks, all of them a person's. Every accessibility choice so far was set from
a specification. None of it has been watched in her hands.*

- [ ] `015` 👤 **UXR** — Run an unguided first session with Joan on her own phone, screen-recorded with her permission.
  - *Needs:* `004` · *Done when:* a recording exists and nobody helped her.
- [ ] `016` 👤 **UXR** — Time how long it takes her to find a specific known recipe from a cold start.
  - *Needs:* `015` · *Done when:* a number, plus what she tried first.
- [ ] `017` 👤 **UXR** — Watch whether she discovers A−/A+ without being told, or never finds it.
  - *Needs:* `015` · *Done when:* observed, either way. Both answers are useful.
- [ ] `018` 👤 **UXR** — Test whether the mode switch reads as "edit this recipe" or as something she should avoid touching.
  - *Needs:* `015` · *Done when:* she says what she thinks it does, in her words.
- [ ] `019` 👤 **UXR** — Check whether tap-to-check-off is discovered during real cooking, or only when pointed out.
  - *Needs:* `015` · *Done when:* observed mid-recipe, not in a demo.
- [ ] `020` 👤 **UXR** — Observe whether she notices the servings control, and whether rescaled fractions like `1½` read clearly to her.
  - *Needs:* `015` · *Done when:* she reads a scaled quantity aloud correctly, or doesn't. This decides whether `074` is even the right fix.
- [ ] `021` 👤 **UXR** — Sit with a second family member through an import from a link, start to finish, without helping.
  - *Needs:* — · *Done when:* they either complete it or get stuck, and where is recorded.
- [ ] `022` 👤 **UXR** — Establish whether "Download updated recipes.json" means anything at all to a non-technical person.
  - *Needs:* `021` · *Done when:* answered plainly. This is the single biggest input to the gate.
- [ ] `023` 👤 **UXR** — Report back on the category names in her words — does she say "Sides", or "vegetables"?
  - *Needs:* `015` · *Done when:* her vocabulary is written down verbatim. This decides `027`.
- [ ] `024` 👤 **UXR** — Write the findings as observations rather than recommendations, so design and engineering argue about the fix and not the facts.
  - *Needs:* `015`–`023` · *Done when:* the write-up contains no sentence beginning "we should".

## Phase 3 — Decide

*10 tasks. Every one of these is a ruling that unblocks other people. Nothing
here is built; everything here is written down.*

- [ ] `025` 👤 **PM** — Get a real answer from the family on whether "download and commit" is acceptable friction, or the thing that kills adoption.
  - *Needs:* `022` · *Done when:* an actual person has said yes or no.
- [x] `026` 🤝 **PM** — Write the one-page backend decision memo: what breaks today without one, what it would cost per month, who maintains it.
  - *Needs:* `010` `011` `021` `025` · *Done when:* the memo ends in a decision, not options. **This is the gate in §6.**
  - *Ruled 2026-08-01, provisional:* **no server for 1.0** — `DECISIONS.md` §026. Taken as the recommended default on Jason's instruction to proceed; `021`/`025` (the family-friction session) stayed undone and are the one input that would reopen this.
- [x] `027` 🤝 **PM** — Rule on the ten-category list — specifically whether `Sides` and `Drinks` stay, since they were added over the requested eight to avoid mislabelling 11 recipes.
  - *Needs:* `023` · *Done when:* the list is final and `CAT_ALIASES` covers anything dropped.
  - *Ruled 2026-08-01, provisional:* **the ten stay** — nothing dropped, so `CAT_ALIASES` is already complete. `DECISIONS.md` §027; Joan's vocabulary (`023`) can still reopen it.
- [x] `028` 🤝 **TW** — Agree with the family what counts as a category versus a tag, and write the rule down.
  - *Needs:* `027` · *Done when:* the rule is in `README.md` and someone can apply it to a new recipe without asking.
  - *Done 2026-08-01 (family sign-off pending, like every provisional ruling):* the rule is in `README.md` — category = "when would you serve this", one of ten; everything else is a tag, with the Baking edge case called out.
- [x] `029` 🤝 **PM** — Set the bar for what "done" means for a recipe: does it need a photo, verified servings, and at least one tag before it counts as finished?
  - *Needs:* `013` · *Done when:* written. This is the acceptance criterion for all of Phase 8.
  - *Ruled 2026-08-01, provisional:* **true text** — real ingredients and steps, servings confirmed-or-marked-unknown; photos and tags optional. `DECISIONS.md` §029. The 1.0 content blockers are exactly the 4 empty lists + 34 inferred servings.
- [x] `030` 👤 **PM** — Decide whether the menu-planning calendar is 1.0 or a stretch goal, and write down which.
  - *Needs:* `026` · *Done when:* decided. **This gates Phase 15.**
  - *Ruled 2026-08-01, provisional:* **stretch goal** — without a server a plan is one-phone-only, and 1.0 should not wait 11 tasks for it. Phase 15 struck intact. `DECISIONS.md` §030.
- [x] `031` 🤝 **PM** — Define what happens when two family members edit the same recipe, since local-only editing has no merge story at all.
  - *Needs:* `026` · *Done when:* the rule is written, whether or not a server exists to enforce it.
  - *Ruled 2026-08-01:* **last committed file wins, committer must diff first, same-recipe conflicts go to Joan, the losing version is preserved in `CONTENT.md`.** `DECISIONS.md` §031.
- [x] `032` 🤝 **PM** — Agree a cadence for pulling everyone's local edits into the repo — weekly, monthly, or on request.
  - *Needs:* `026` `031` · *Done when:* a named person owns it and a frequency is set.
  - *Ruled 2026-08-01:* **on request + a monthly nudge, owner Jason.** `DECISIONS.md` §032.
- [x] `033` 🤝 **PM** — Arbitrate the first accessibility-versus-feature conflict in writing, so the precedent exists before it is contested under pressure.
  - *Needs:* `024` · *Done when:* a real conflict has been ruled on, not a hypothetical one.
  - *Done 2026-08-01, with a deviation:* `024` remains open, but a real conflict predates it — the handoff's 2–3 column Menu grid versus title legibility, already ruled for legibility. That ruling is now the written precedent: **legibility wins, and denser treatments must survive the top font step + Easy Read.** `DECISIONS.md` §033.
- [x] `034` 🤖 **A11Y** — Write the accessibility acceptance criteria every new feature must meet before merge.
  - *Needs:* `033` · *Done when:* it is a checklist a reviewer can run. **Everything merged after this is held to it.**
  - *Done 2026-08-01:* `design/a11y-criteria.md` — 17 checks a reviewer can run, split into the automated set (cited to the suite that enforces each) and the manual set, with a marked section for what the VoiceOver pass (`035`–`040`) must later add. Written now rather than waiting: an incomplete checklist enforced beats a complete one that doesn't exist.

---

# ACT II — Make it safe, make it designed

*27 tasks. Verify and harden what already exists before adding to it, so every
feature built in Act III and Act IV inherits the guarantees rather than being
retro-fitted with them.*

## Phase 4 — Audit what is already shipped

*12 tasks. Automated contrast passes across 44 combinations already. Automated
checks cannot tell you whether a screen reader announces anything useful.*

- [ ] `035` 👤 **A11Y** — Run VoiceOver on iOS through every screen and log what is announced versus what should be.
  - *Needs:* `004` · *Done when:* every screen has a line in the log, including Add, Import and the Text-size sheet.
- [ ] `036` 👤 **A11Y** — Verify the route-change live region is actually spoken, rather than merely present.
  - *Needs:* `035` · *Done when:* heard, on a device.
- [ ] `037` 👤 **A11Y** — Test the filter and download sheets for focus trapping with a real screen reader, not just by keystroke.
  - *Needs:* `035` · *Done when:* the rotor cannot escape an open sheet.
- [ ] `038` 👤 **A11Y** — Check that check-off rows announce their pressed state and their text together.
  - *Needs:* `035` · *Done when:* "checked, two cups flour" and not two separate announcements.
- [ ] `039` 👤 **A11Y** — Confirm the servings stepper announces the new count when it changes.
  - *Needs:* `035` · *Done when:* the change is spoken without re-navigating to it.
- [ ] `040` 👤 **A11Y** — Verify every category icon is correctly hidden from assistive technology and never read as content.
  - *Needs:* `035` · *Done when:* all ten are silent. This feeds `056`.
- [x] `041` 🤖 **A11Y** — Audit at 200% browser zoom on top of Easy Read — the compounding case nobody tests.
  - *Needs:* — · *Done when:* no horizontal scroll and no clipped text at any of the five font steps.
- [ ] `042` 👤 **A11Y** — Test with iOS Reduce Motion and Increase Contrast both on.
  - *Needs:* `004` · *Done when:* every animation is off and nothing has lost a border.
- [x] `043` 🤖 **A11Y** — Check colour is never the only signal — the flagged panel and the empty-contributor tile are the two risks.
  - *Needs:* — · *Done when:* both carry a shape or a word as well as a colour. Feeds `058`.
- [x] `044` 🤖 **SEC** — Audit every render path for unescaped interpolation, especially in imported fields.
  - *Needs:* — · *Done when:* every path is either escaped or explicitly justified in a comment.
- [x] `045` 🤖 **SEC** — Import a page containing a script tag and confirm it is inert everywhere it appears.
  - *Needs:* `044` · *Done when:* a test asserts it, in the card, the recipe page and the edit field.
- [x] `046` 🤖 **SEC** — Cap the size of imported fields so a hostile page cannot exhaust storage.
  - *Needs:* `044` · *Done when:* a cap exists, is enforced, and says so when it truncates.

## Phase 5 — Close the third-party surface

*6 tasks. Four CORS relays, a Tesseract CDN and Google Fonts. Each is a party
that sees something, and none is documented for the family. The CSP goes last,
because it can only be written once every legitimate origin is known.*

- [x] `047` 🤖 **SEC** — Confirm the OCR library genuinely runs locally and uploads nothing.
  - *Needs:* — · *Done when:* verified by network trace, not by reading the README of the library.
- [x] `048` 🤖 **SEC** — Pin the Tesseract CDN version and add subresource integrity.
  - *Needs:* `047` · *Done when:* the version is exact and a tampered hash blocks the load.
- [x] `049` 🤖 **SEC** — Review whether Google Fonts should be self-hosted to remove a third party from every page load.
  - *Needs:* `004` · *Done when:* decided and done, or decided and written down why not. **→ README**
  - *Done 2026-08-01:* **self-hosted.** Six woff2 files (52 KB, latin + latin-ext, OFL text included) in `fonts/`, same unicode-ranges as Google served, 400/700 preloaded. Verified: zero third-party requests at load, faces active, FCP 888 ms with the fonts now inside the measurement. `004` (does it render on the physical iPhone) still stands — self-hosting makes that more likely, not less, since the face no longer depends on a third party being reachable.
- [x] `050` 🤖 **SEC** — Document, in the UI, exactly what each relay receives when a link is imported.
  - *Needs:* — · *Done when:* the disclosure names the relays and says what is sent, before the request is made.
- [x] `051` 🤖 **FE-B** — Show which relay succeeded, so a persistent failure can be diagnosed rather than guessed at.
  - *Needs:* `050` · *Done when:* the review screen names the relay that answered.
- [x] `052` 🤖 **SEC** — Add a Content-Security-Policy and confirm it does not break the lazily-loaded OCR library.
  - *Needs:* `047` `048` `049` `050` · *Done when:* the policy is as tight as the known origin list allows, and every path still works.
  - *Done 2026-08-01:* meta CSP in `index.html` — scripts by self + jsdelivr + hash (no `unsafe-inline`), fonts self-only, objects none; `connect-src https:` stays open because link import fetches whatever page the user pastes, by design. First run broke OCR exactly as the task predicted — wasm compilation, fixed with `wasm-unsafe-eval`. Live OCR re-proven under the policy; 7 CSP checks added to sec.js. Bonus: writing the script hash exposed a real bug — the pre-paint theme script compared the raw string while `save()` stores JSON, so it had never fired; fixed and proven with app.js blocked.

## Phase 6 — Design the screens that were never designed

*9 tasks. Add, Import, the Text-size sheet, Easy Read and the category icons were
composed from the existing vocabulary by an engineer. They hold together and
they pass contrast, but nobody with a designer's eye has reviewed them.*

- [x] `053` 🤖 **UI** — Audit the Add and Import screens against `styleguide.html` and mark every value that was improvised.
  - *Needs:* — · *Done when:* a list exists of every size, space and radius that is not in the styleguide.
- [x] `054` 🤝 **UI** — Extend `tokens.css` with any genuinely missing values rather than letting engineers invent them — a print palette is the known gap.
  - *Needs:* `007` `053` · *Done when:* the `@media print` block's literal black, white and grey are tokens, and the file's hex count outside it is still zero. **Any new value is asked for, never invented.**
  - *Done 2026-08-01:* `--print-ink/-paper/-line` appended to `tokens.css` in a marked block — its first amendment since the handoff, recorded in `CLAUDE.md`. `style.css` now carries zero hex anywhere. No other missing values surfaced: `053`'s improvised list was spacing/size, not colour. (`007`, printing on paper, still stands as the physical check.)
- [x] `055` 🤝 **UI** — Specify focus-visible styling as a designed state, not a browser default.
  - *Needs:* `035`–`043` · *Done when:* it is drawn, and it clears AA against every surface it lands on.
  - *Done 2026-08-01:* one designed ring — 3px accent, offset 2, rounded — measured at 10.5:1 (dark) / 8.7:1 (light) against the page it sits over; accent-filled controls gain a 2px page-coloured gap so ring never touches same-coloured fill. First cut flipped the ring to ink on filled controls; measurement showed 1.6:1 and killed it — the gap, not a colour change, is what works. (`035`–`040` VoiceOver halves still open; the visual spec no longer waits on them.)
- [x] `056` 🤖 **UI** — Redraw any of the ten category icons that do not read at 24px — the Sides bowl and Baking whisk are the weakest.
  - *Needs:* `040` · *Done when:* all ten are identifiable at 20px on a phone.
  - *Done 2026-08-01:* three redrawn, not two — rendering the full set at 20px caught **Breakfast reading as an eye**, worse than either named suspect. Now: egg in a pan (yolk off-centre), footed bowl with peas, scored loaf on a board. All ten verified by rendered strip at 20px and 44px. (`040`'s VoiceOver half — icons silent to AT — still open; the `aria-hidden` attributes are asserted in kt.js.)
- [x] `057` 🤖 **UI** — Specify the photo aspect ratio and crop behaviour for hero and thumbnail, including portrait photos from a phone.
  - *Needs:* — · *Done when:* a portrait photo, a landscape photo and a square photo all look deliberate in both slots.
- [x] `058` 🤝 **UI** — Design the empty-contributor tile properly; the current outlined treatment was an engineering judgement call.
  - *Needs:* `043` · *Done when:* it reads as an invitation rather than a zero, without colour being the only signal.
  - *Done 2026-08-01:* the zero is gone. Empty tiles are dashed (the empty-slot mark), carry an accent plus and "None yet — add the first", and still route to that person's filtered Menu whose empty state offers Add. Three non-colour signals: dash, glyph, words. Verified by screenshot, zoom suite, contrast audit.
- [x] `059` 🤝 **UI** — Review Easy Read against the design intent — it currently removes the dim tier, which was an engineering decision about contrast.
  - *Needs:* `017` `041` · *Done when:* the mode is either endorsed as-is or redrawn, in writing.
  - *Done 2026-08-01:* **endorsed as built**, in writing — `DECISIONS.md` §059, on the audit evidence (0 AA failures across 48 combinations, zoom suite green). Reopens if Joan's sessions (`017`/`020`) contradict it.
- [x] `060` 🤖 **UI** — Define the tag chip at every size and on every surface it appears on: card, recipe page, and filter sheet.
  - *Needs:* `028` · *Done when:* one spec covers all three, including a chip with a long tag in it.
  - *Done 2026-08-01:* one rule — a chip is always one line; rows wrap, chips never fold. Card chips cap at 22ch and ellipsize; recipe-page chips (44px tap targets) run the full line before truncating; sheet chips ellipsize in a label span (flex containers can't truncate bare text nodes — found the hard way). Spec prose lands in `design/components.md` with `061`. Verified with a marathon tag at 390px on all three surfaces.
- [x] `061` 🤖 **UI** — Produce a one-page component reference so the next person to add a screen has something to build from.
  - *Needs:* `053`–`060` · *Done when:* it is in the repo and linked from the README. **→ README**
  - *Done 2026-08-01:* `design/components.md` — type, surfaces, controls, the chip and focus specs, icons/artwork, motion inventory, photos, print. Linked from the README beside the a11y criteria. Deliberately one page: a vocabulary, not a framework.

---

# ACT III — Make the content true

*28 tasks. The app is finished and the content is not: servings inferred on 34
of 48 recipes, no photos, no tags, four recipes with no ingredients at all. This
act builds the instruments first, then does the work, then makes it findable.*

## Phase 7 — Photos and tags: the machinery

*10 tasks. Built to the Phase 6 design, so the content pass in Phase 8 has
somewhere to put things. Tagging 48 recipes one at a time will not happen, so
bulk tagging exists before anyone is asked to tag.*

- [x] `062` 🤖 **FE-A** — Move photo storage to IndexedDB if the ceiling from `010` proves too low for the collection.
  - *Needs:* `010` `011` · *Done when:* 48 photos fit, or it is written down why the current store is enough.
- [x] `063` 🤖 **FE-A** — Add lazy loading and explicit dimensions to thumbnails so the list does not reflow as photos decode.
  - *Needs:* `012` `057` · *Done when:* cumulative layout shift is zero with a full list of photos.
- [x] `064` 🤖 **FE-A** — Handle a recipe whose photo has been committed but whose local copy is gone, without flashing a broken state.
  - *Needs:* `062` · *Done when:* the fallback to the category icon is silent.
- [x] `065` 🤖 **FE-A** — Add a photo lightbox on the recipe page, since a hero at 280px is not enough to read a handwritten card.
  - *Needs:* `057` · *Done when:* it opens, traps focus, closes on Escape and returns focus — same contract as every other sheet.
- [x] `066` 🤖 **FE-B** — Support importing several photos into one recipe, since a long recipe spans two cards.
  - *Needs:* `062` `057` · *Done when:* two cards produce one recipe with both pages retained.
- [x] `067` 🤖 **FE-B** — Add tag autocomplete drawing on tags already in use, to stop near-duplicates being created.
  - *Needs:* `060` `028` · *Done when:* typing "ital" offers "Italian" before it offers to create "italian". **→ README**
  - *Done 2026-08-01:* suggestion chips under both tag fields (Edit + Add), built from tags in use, canonical casing, prefix-first, already-listed excluded, capped at five. DOM-patched on input so the caret is never stolen by a render. Five checks in add.js (now 63).
- [x] `068` 🤖 **FE-B** — Add bulk tagging from the Menu.
  - *Needs:* `067` · *Done when:* ten recipes can be tagged in one pass. **→ README**
  - *Done 2026-08-01:* a "Tag" mode beside "Remove" — tap recipes, the pill counts them, one sheet applies the list to all. Typed tags canonicalize against existing casings (067's rule, enforced on the bulk path too), unions never double. Proven at exactly ten in feat.js (now 52).
- [x] `069` 🤖 **FE-B** — Build tag rename and merge, and make it update every recipe that uses the old name.
  - *Needs:* `067` · *Done when:* merging "italian" into "Italian" leaves no recipe pointing at the old one. **→ README**
  - *Done 2026-08-01:* "Rename or merge" beside the filter sheet's tag group — tap a tag, type the new name; a name that already exists (any casing) merges after a confirm, deduping per recipe and following any active filter. The exact done-when case is the feat.js fixture (now 54).
- [x] `070` 🤖 **FE-B** — Detect a likely duplicate on save by comparing title and ingredients against the collection.
  - *Needs:* — · *Done when:* it warns and offers a comparison, and never blocks the save.
- [x] `071` 🤖 **FE-A** — Revisit the four recipes with no ingredient list and design a better in-page prompt to fix them.
  - *Needs:* `029` · *Done when:* the prompt says what is missing and opens the right field, in Viewer mode as well as Edit.
  - *Done 2026-08-01, one deliberate narrowing:* Viewer shows a flag panel that names the gap and points at the Edit switch **in prose, not a button** — "Viewer shows no edit affordances" outranks the task's literal wording. Toggling Edit on such a recipe opens with one empty ingredient line and the caret already in it. Verified on `chops`.

## Phase 8 — The content truth pass

*9 tasks, and the most valuable block in this file. Almost all of it is a
person's — nobody can ask Joan what a recipe serves except a person. **Nothing
here may be inferred.** Rule 5 in §3 exists for this phase.*

- [ ] `072` 👤 **TW** — Recover ingredient lists for `chops`, `parsnips`, `fries-in-ninja` and `steak-time-to-cook`.
  - *Needs:* `013` `071` · *Done when:* four recipes have real ingredients, from Joan, not reconstructed.
- [ ] `073` 👤 **TW** — Resolve the cut-off final step in `parsnips`, currently truncated mid-sentence by an ad banner.
  - *Needs:* `072` · *Done when:* the step ends in a full stop that Joan recognises.
- [ ] `074` 👤 **TW** — Sit with Joan and confirm the real serving count for the 34 recipes where it was inferred.
  - *Needs:* `013` `020` · *Done when:* all 34 are either confirmed or explicitly marked unknown. Marked unknown is a valid answer; a guess is not.
- [x] `075` 🤝 **TW** — Maintain a duplicates list and decide which version wins when two people have the same recipe.
  - *Needs:* `070` · *Done when:* the list exists and each entry has a winner named.
  - *Done 2026-08-01:* pairwise scan of all 48 in `CONTENT.md` — one candidate (`scone-in-ninja` × `scones`), verdict **both stay**: air-fryer/metric vs oven/cups are two real preparations, not a duplicate. Standing rule written for future pairs; the in-app save-time warning (`070`) is the intake.
- [x] `076` 👤 **TW** — Seed the first nationality tags by asking where each dish came from, rather than inferring it.
  - *Needs:* `067` `068` `028` · *Done when:* every tag traces to something someone said.
  - *Done 2026-08-02, basis amended by Jason ("research where they come from to tag it properly"): 38 tags across 37 recipes, each traceable to an established culinary fact or a method the title states; the eleven genuinely-ambiguous recipes stay untagged for Joan. Research record + the two Needs-Jason items in CONTENT.md.*
- [ ] `077` 👤 **TW** — Collect at least one photo per course so the Main hero is not empty for every category.
  - *Needs:* `062` `063` `057` · *Done when:* all ten categories have at least one, committed to `images/`.
- [ ] `078` 👤 **TW** — Chase Jason, Jennifer, Lindsay, Siobhan and Jessica for their first recipe each.
  - *Needs:* — · *Done when:* four empty contributor tiles are no longer empty, or a real reason is recorded for each that stays.
- [x] `079` 🤖 **TW** — Write the one-page "how to add a recipe" guide aimed at someone who has never used GitHub.
  - *Needs:* `028` `067` `068` · *Done when:* someone non-technical follows it start to finish without asking a question. **→ README**
  - *Done 2026-08-01:* `ADDING.md` — leads with the one mental model that matters ("saves on your phone; publishing is handing the file to Jason"), covers all three paths, the category/tag rule in plain words, mistakes, and the same-recipe-twice case. Linked from the README, whose tags section also gained the 067–069 machinery. The literal done-when (a real non-technical person follows it unaided) rides with the family sessions.
- [x] `080` 🤖 **TW** — Bring `CLAUDE.md`'s build-state section current, and cross-check `README.md` against it.
  - *Needs:* `072`–`079` · *Done when:* every decision taken in Acts I–III appears in one of the two, and neither contradicts the other. **→ README**
  - *Done 2026-08-01 (for everything that exists; re-runs after the content pass lands `072`–`078`):* CLAUDE.md gained "The completion push" — the provisional rulings, what they unlocked, and the honest remainder; the Verified section reads 299 checks; the README gained its live status line and was cross-checked clean against it.

## Phase 9 — Import you can trust

*6 tasks. Import is the only place machine-guessed content enters the collection,
and the one feature that has already failed in production. The pipeline is
verified against a stubbed recogniser; Tesseract has never seen a real photograph
of one of Joan's cards.*

- [ ] `081` 👤 **FE-B** — Run OCR against twenty real photos of Joan's recipe cards and record the error rate honestly.
  - *Needs:* `047` `048` `052` `077` · *Done when:* a percentage is written down, including the ugly one. **Do not round it down.**
- [x] `082` 🤖 **FE-B** — Flag individual fields the parser was unsure about, rather than flagging the whole recipe.
  - *Needs:* `081` · *Done when:* `flagged` names fields, and the recipe page shows the flag beside the field it belongs to.
  - *Done 2026-08-01 (mechanics; `081`'s real-photo error rate still informs the wording later):* parsers emit "Field — …" flags; the recipe page classifies (with a keyword fallback so pre-convention data gains the chips too) and shows a "Double-check" chip beside title/servings/ingredients/steps. The chip is wayfinding, not editing — it scrolls to the full panel, reduced-motion respected. `flagged` stays an array of strings, so no stored data breaks.
- [x] `083` 🤖 **FE-B** — Let a user correct the ingredients-versus-steps split with one control when the parser guessed wrong.
  - *Needs:* `081` · *Done when:* a misplaced line moves between the two lists in one tap.
  - *Done 2026-08-01:* every review line carries a swap button beside its delete — one tap sends the line to the other list, notice confirms, draft persistence keeps it. Verified: an "ingredient" reading "Preheat the oven" lands at the end of the instructions in one tap.
- [x] `084` 🤖 **FE-B** — Preserve a half-finished import across an accidental refresh instead of losing the work.
  - *Needs:* — · *Done when:* a refresh mid-review returns to the same draft, and the draft is still never saved until Save is pressed.
- [x] `085` 🤝 **QA** — Feed the OCR path a deliberately terrible photo and confirm it flags rather than invents.
  - *Needs:* `081` `082` · *Done when:* an unreadable photo produces flags and empty fields, never plausible-looking fiction.
  - *Done 2026-08-01:* `KT_OCR_NOISE=1 node tests/ocr-live.js` feeds pure random noise through the real Tesseract. Result: 3 flags, zero lines with plausible quantities, zero plausible steps — what garbage produces is visibly garbage plus a flag, never fiction. The gate asserts it (exit non-zero on invented content).
- [x] `086` 🤖 **QA** — Run the import with all four relays blocked and confirm the paste box still completes a save.
  - *Needs:* `051` · *Done when:* asserted by a test, with the network fully blocked.

## Phase 10 — Search and scale

*3 tasks. Deferred to the end of Act III on purpose: tuning search before the
tags exist is tuning it against the wrong data.*

- [x] `087` 🤖 **FE-A** — Add diacritic and simple typo tolerance to search, so "creme" finds "crème".
  - *Needs:* `076` · *Done when:* a set of real misspellings from `016` all resolve. **→ README**
  - *Done 2026-08-01 (mechanics; `016`'s real misspellings validate it later):* NFD fold plus one-edit tolerance on words of 5+ letters, exact-first so precision degrades gracefully. creme→crème, jamaican→Jamaïcan, chiken→chicken, garbage still finds nothing — all in feat.js (58).
- [x] `088` 🤖 **FE-A** — Show which field matched a search hit, so a tag match does not look like a mistake.
  - *Needs:* `087` · *Done when:* a hit on an ingredient says so on the card. **→ README**
  - *Done 2026-08-01:* cards say "matches ingredient" / "matches tag" when the hit isn't the visible title — Main results and Menu search both.
- [x] `089` 🤖 **FE-A** — Virtualise the Menu list if it passes roughly 150 recipes.
  - *Needs:* `063` `078` · *Done when:* it is built, or it is written down that the collection is nowhere near the threshold and this is deferred.
  - *Done 2026-08-01, the second branch:* **deferred, in writing** — 48 recipes against the ~150 threshold, CLS 0.0000, FCP at a fifth of budget; virtualising now would tax scroll restoration and screen readers for zero measured gain. The ruling lives as a comment on `menuMatches` where the implementer would look first.

---

# ACT IV — The fork

*41 tasks, all of them downstream of task `026`. **If `026` says no server,
strike Phases 11–14 (30 tasks) with that as the reason and skip to Phase 15** —
or straight to the release checklist if `030` also says no.*

> **Read §6 before entering this act.** Building a server nobody decided to run
> is 30 tasks, a monthly bill, and a database that becomes the only copy of 48
> irreplaceable recipes.

> ⛔ ~~THE GATE CLOSED THIS ACT — 2026-08-01~~ · **⟲ AND JASON REOPENED IT —
> 2026-08-02.** He supplied a Neon Postgres instance and ruled the calendar
> into 1.0 (`DECISIONS.md` §026/§030). The act runs again, **reshaped**: Neon
> replaces the Express+SQLite+paid-host plan, which supersedes some tasks
> outright and completes others by different means. Per-task notes below say
> which. Executed same-day: schema (`db/schema.sql`), migration of all 48
> recipes (`db/migrate.js`), round-trip export proven clean (`db/export.js`).
> Open: the app↔database wiring (Data API vs worker — Jason's console call)
> and the calendar build (Phase 15, now the active queue).
>
> Task-status key for this act: **[x] done** (possibly by Neon rather than
> the original wording) · **[ ] + *superseded*** — no longer meaningful under
> Neon, struck with that reason · **[ ] open** — still real work.
>
> - `090`–`092` *(host comparison, disk persistence, platform choice)* —
>   **superseded**: Jason chose the platform by handing over the instance;
>   Neon's storage is managed, there is no disk to distrust.
> - `093` `094` `095` — **done 2026-08-02**: `kitchen` schema with tags as a
>   join table, the three indexes, and a `schema_version` ledger.
> - `096` *(OpenAPI before endpoints)* — **open, reshaped**: the contract
>   question is now "Neon Data API with row-level security, or a worker" —
>   blocked on Jason's console decision.
> - `097` *(images in DB or on disk)* — **open**: currently images stay
>   files in the repo; revisit when the app wiring lands.
> - `098`–`101` — **done 2026-08-02**: loud-failing import, the
>   empty-ingredients audit (reports exactly the four), flags preserved,
>   round-trip export proven content-identical.
> - `102` `103` *(backups + tested restore)* — **open, reshaped**: Neon has
>   point-in-time restore, but an untested backup is still not a backup —
>   verify PITR on this plan, and schedule `db/export.js` as the off-host
>   copy (it doubles as one by design).
> - `104`–`112` *(the Express API)* — **superseded as written**; their
>   real content (validation, health, server-side import, rate limits,
>   image sizes, sync/conflicts, static fallback) returns as concrete tasks
>   once the Data-API-vs-worker call is made. `112`'s guarantee already
>   holds: the static build runs from `recipes.json` with the database off.
> - `113`–`119` *(pipeline, staging, uptime, logs, rollback)* — **superseded
>   as written**: Pages deployment is unchanged; what returns after the
>   wiring is a nightly `export.js` sync job and an uptime check, tracked
>   with the wiring task.

## Phase 11 — Prove the ground *(gated on `026`)*

*8 tasks. The architecture doc's own warning first: a host that resets its
filesystem on redeploy destroys a SQLite database silently, and the loss is only
discovered later.*

- [ ] `090` 🤖 **OPS** — Compare Railway, Render and Fly on persistent-disk behaviour, monthly cost and cold-start time.
  - *Needs:* `026` · *Done when:* three rows, same columns, real numbers.
- [ ] `091` 👤 **OPS** — Write a file to the shortlisted host's disk, force a redeploy, and confirm it is still there. Document the result.
  - *Needs:* `090` · *Done when:* proven on the actual plan that would be paid for, not the docs' claim about it.
- [ ] `092` 👤 **PM** — Choose the hosting platform and record why.
  - *Needs:* `090` `091` · *Done when:* chosen, paid for, and the reason is written down. **→ README** — the README's "no server" premise is now wrong.
- [x] `093` 🤖 **DB** — Write the schema with tags as a real join table, not a comma-separated column.
  - *Needs:* `092` `028` · *Done when:* people, recipes, tags and menu_plan exist and the tag join is real.
  - *Done 2026-08-02: `kitchen` schema on Jason's Neon — tags are a real join table.*
- [x] `094` 🤖 **DB** — Index `recipes.category`, `recipes.contributor_id` and `menu_plan.date` from the start.
  - *Needs:* `093` · *Done when:* in the first migration, not a later one.
  - *Done 2026-08-02: category, contributor and plan-date indexes in the first migration.*
- [x] `095` 🤖 **DB** — Add a migration runner that records applied versions.
  - *Needs:* `093` · *Done when:* it exists before the second schema change, not after it.
  - *Done 2026-08-02: `kitchen.schema_version` ledger, written before any second migration exists.*
- [ ] `096` 🤖 **BE** — Write the OpenAPI description before any endpoint, so the frontend can be built against a contract.
  - *Needs:* `093` · *Done when:* every endpoint in Phase 13 is described and nothing is implemented yet.
- [ ] `097` 🤝 **DB** — Decide whether images live in the database or on disk, and write down why.
  - *Needs:* `091` `092` · *Done when:* decided, with the persistence result from `091` cited.

## Phase 12 — Move the data *(gated on `026`)*

*6 tasks. The migration script is also the best audit `recipes.json` will ever
get. This data is genuinely irreplaceable, so the backup is tested before the
API is written, not after.*

- [x] `098` 🤖 **DB** — Build the `recipes.json` import and have it fail loudly on anything malformed.
  - *Needs:* `095` `080` · *Done when:* a deliberately broken file stops the import with a useful message.
  - *Done 2026-08-02: `db/migrate.js` — validates every field, dies loudly, idempotent upserts.*
- [x] `099` 🤖 **DB** — Report, during migration, every recipe with an empty ingredient list.
  - *Needs:* `098` · *Done when:* the report runs. **If Phase 8 did its job this now returns zero — that is the check on `072`.**
  - *Done 2026-08-02: the migration reports exactly the four known empty lists — the 072 worklist, confirmed from a second direction.*
- [x] `100` 🤖 **DB** — Preserve the `flagged` array; it is the record of what the transcription was unsure about.
  - *Needs:* `098` `082` · *Done when:* a round trip loses no flag, including the per-field ones from `082`.
  - *Done 2026-08-02: `flagged` rides through as jsonb; round trip loses nothing.*
- [x] `101` 🤖 **DB** — Add a round-trip export back to `recipes.json` so the project is never locked into the database.
  - *Needs:* `098` `100` · *Done when:* export → import → export is byte-identical.
  - *Done 2026-08-02: `db/export.js` regenerates recipes.json; `--check` proved database and file content-identical.*
- [ ] `102` 🤝 **DB** — Script a nightly backup that copies the database somewhere off the host.
  - *Needs:* `097` `101` · *Done when:* it has run unattended overnight at least once.
- [ ] `103` 🤝 **DB** — Test a restore end to end — an untested backup is not a backup.
  - *Needs:* `102` · *Done when:* a destroyed database has been rebuilt from a backup and the app came up on it.

## Phase 13 — Build the API *(gated on `026`)*

*9 tasks, built against the `096` contract. Server-side import comes before the
sync endpoints because it removes the CORS relay chain entirely — the single
biggest reliability win available to this project.*

- [ ] `104` 🤖 **BE** — Implement recipe CRUD with validation that rejects malformed imports rather than storing them.
  - *Needs:* `096` `098` `046` · *Done when:* every field is validated and the API matches the OpenAPI description exactly. **→ README**
- [ ] `105` 🤖 **BE** — Add a health endpoint that verifies the database file is present and writable.
  - *Needs:* `104` · *Done when:* it fails when the disk is read-only, not just when the process is dead.
- [ ] `106` 🤖 **BE** — Move link fetching server-side and drop the four-relay fallback chain.
  - *Needs:* `104` `051` · *Done when:* import works with every relay blocked at the client, and §5's relay disclosure is removed from the UI as no longer true.
- [ ] `107` 🤖 **BE** — Run OCR server-side with a request size limit and a hard timeout.
  - *Needs:* `104` `081` · *Done when:* the error rate matches or beats the number recorded in `081`.
- [ ] `108` 🤖 **BE** — Rate-limit the import endpoints — they are the only ones that make outbound requests.
  - *Needs:* `106` `107` · *Done when:* the limit is enforced and the message says what to do about it.
- [ ] `109` 🤖 **BE** — Serve images at requested sizes so phones do not download a 1200px hero for a 64px slot.
  - *Needs:* `097` `104` · *Done when:* the 3G budget from `012` still holds with a full list of photos.
- [ ] `110` 🤖 **BE** — Add an endpoint that accepts a device's whole local overlay and reports conflicts instead of overwriting.
  - *Needs:* `104` `031` · *Done when:* a device with a month of local edits can be reconciled without losing one.
- [ ] `111` 🤖 **BE** — Build a conflict-resolution response the UI can present as a choice, not an error.
  - *Needs:* `110` `031` · *Done when:* a conflict renders as two readable versions and a pick.
- [ ] `112` 🤖 **BE** — Keep the existing static build working against the API, so a rollback is always possible.
  - *Needs:* `104` · *Done when:* the static build still runs from `recipes.json` alone with the server switched off. **→ README**

## Phase 14 — Run it *(gated on `026`)*

*7 tasks. Deployment already works today: push to `main`, Pages rebuilds in about
a minute. The moment deployment needs a runbook, updates stop happening — so the
bar is that shipping a change stays a normal push.*

- [ ] `113` 🤝 **OPS** — Set up the GitHub-to-host pipeline so a merge deploys automatically.
  - *Needs:* `092` `104` `003` · *Done when:* a merge deploys with no manual step and the suites gate it.
- [ ] `114` 🤝 **OPS** — Keep a staging environment with a copy of the data for testing migrations.
  - *Needs:* `113` `102` · *Done when:* a migration has been rehearsed on staging before production.
- [ ] `115` 🤝 **OPS** — Add an uptime check that alerts on the site being down, not just the host being up.
  - *Needs:* `105` `113` · *Done when:* it fires on a deliberately broken deploy, and it reaches a person.
- [ ] `116` 🤝 **OPS** — Set up log retention long enough to investigate a problem reported days later.
  - *Needs:* `113` · *Done when:* a week-old request can still be traced.
- [ ] `117` 🤝 **OPS** — Automate the database backup off-host and verify it runs unattended.
  - *Needs:* `102` `103` `113` · *Done when:* it has survived a week without anyone touching it.
- [ ] `118` 🤝 **OPS** — Document the rollback procedure and rehearse it once.
  - *Needs:* `112` `113` · *Done when:* someone who did not write it has followed it successfully.
- [ ] `119` 🤖 **OPS** — Keep the static-only build deployable as a fallback if the server has to be abandoned.
  - *Needs:* `112` `101` · *Done when:* Pages can be re-pointed at the static build in one commit, with current data.

## Phase 15 — The calendar *(⟲ RULED INTO 1.0 by Jason, 2026-08-02 — the active build queue)*

*11 tasks. The database's `menu_plan` table already exists with per-meal
servings (`125`) and deletion-survival (`127`) designed in; the build starts
at `120` (slot decision) and `121` (drawings before code), per the phase's
own design-first rule.*

- [x] `120` 🤝 **FE-C** — Decide and document the meal slots — breakfast, lunch, dinner, or dinner only.
  - *Needs:* `030` · *Done when:* decided from how the family actually plans, not from what a calendar usually has.
  - *Done 2026-08-02: dinner-first, all three slots live, Monday start — DECISIONS.md 120.*
- [x] `121` 🤝 **UI** — Draw the calendar week view at 390px, 768px and 1180px before anyone builds it.
  - *Needs:* `120` `061` · *Done when:* all three widths are drawn and none needs a new token.
  - *Done 2026-08-02: design/calendar.md — anatomy, the three widths (one column everywhere), interactions, print, data shape. Drawn before built.*
- [x] `122` 🤖 **FE-C** — Prototype the week grid at 390px and confirm it survives Easy Read without horizontal scrolling.
  - *Needs:* `121` `041` `059` · *Done when:* seven days fit at the largest font step with no horizontal scroll. **If it cannot, `121` is wrong and goes back.**
  - *Done 2026-08-02: seven days at 390px, zero horizontal scroll, held under Easy Read at the 40px step — asserted in tests/plan.js and the zoom suite.*
- [x] `123` 🤖 **FE-C** — Build tap-to-assign with a recipe picker reusing the existing search.
  - *Needs:* `122` `087` · *Done when:* assigning is a tap. **Never drag — it is hostile on a phone and unusable with a tremor or low vision.**
  - *Done 2026-08-02: tap an empty slot, search (the 087 fold/typo search), tap a recipe — assigned. Never drag.*
- [x] `124` 🤖 **FE-C** — Show an assigned recipe as a card carrying its category icon and photo thumbnail.
  - *Needs:* `123` `056` `063` · *Done when:* it reads at a glance at the largest font step.
  - *Done 2026-08-02: assigned meals are the Menu card grammar — 64px icon/thumb, title, slot · servings.*
- [x] `125` 🤖 **FE-C** — Let a planned meal record its own servings, independent of the recipe's default.
  - *Needs:* `123` `074` · *Done when:* Tuesday for four and Sunday for twelve are the same recipe with different amounts.
  - *Done 2026-08-02: each planned meal carries its own servings via the meal sheet stepper; the recipe default never moves.*
- [x] `126` 🤖 **FE-C** — Handle the same recipe planned twice in one week without treating it as an error.
  - *Needs:* `125` · *Done when:* both entries exist independently and can be scaled differently.
  - *Done 2026-08-02: the same recipe twice in a week is two independent entries — asserted.*
- [x] `127` 🤖 **FE-C** — Define what a meal plan means when the recipe it points at is later removed.
  - *Needs:* `126` `031` · *Done when:* the slot degrades to the recipe's name rather than vanishing or crashing.
  - *Done 2026-08-02: a removed recipe leaves its planned-under name, marked "No longer in the book", removable, never a crash.*
- [x] `128` 🤖 **FE-C** — Add week-to-week navigation with the View Transitions API, degrading cleanly where unsupported.
  - *Needs:* `122` `042` · *Done when:* it works without the API, and is off entirely under Reduce Motion.
  - *Done 2026-08-02: week nav via View Transitions where supported, plain repaint where not, nothing under reduced motion.*
- [x] `129` 🤖 **FE-C** — Add a print view of the week for the fridge door — likely the most-used output.
  - *Needs:* `124` `054` `007` · *Done when:* one page, black on white, legible across a kitchen. **→ README**
  - *Done 2026-08-02: print strips the controls and keeps the meals — black on white via the print tokens, empty slots omitted. → README done.*
- [x] `130` 🤖 **FE-C** — Prototype ingredient summing and find out how badly units break it before promising the feature.
  - *Needs:* `125` `076` · *Done when:* the failure modes are written down. **This is a spike. Shipping it is a separate decision, not part of 1.0.**
  - *Done 2026-08-02: the summing spike shipped as a labelled preview; failure modes recorded in DECISIONS.md 130. Promoting it past preview is a separate decision, as the task warned.*

## Phase 16 — The video importer *(addendum arc, 2026-08-02 — from Jason's spec, not the original 130)*

*Jason supplied a written spec (video-import-spec) and the ruling that shapes
it: **only the video conversion lives on Render — the app stays static and
instant.** Five tasks, adapted to this repo per the Stage-0 report: `KT_DB`
not `DATABASE_URL`, `kitchen.contributors` not `people`, the existing review
screen as the landing point, vanilla node http instead of Express (four
routes), a contributor NAME on the job row (names are labels, never keys).
Side effect worth naming: the accept endpoint is the first live app→database
write — the "small worker" `096` was waiting for now exists.*

- [x] `V01` 🤖 **DB** — `kitchen.import_jobs` as schema v2: the six-state walk, ETA inputs, the draft in `result_json`, plain-language `error_message`.
  - *Done 2026-08-02: applied live to Neon; ledger reads 1, 2. The re-run also synced the 38 research-tag links.*
- [x] `V02` 🤖 **BE** — The import server: four routes + health, concurrency-1 queue, yt-dlp/ffmpeg as fetched static builds, cheap paths first (description → captions → media), Groq Whisper, one extraction call to the Anthropic API (structured output, flag-don't-guess), 30-minute cap, media deleted on every exit, restart recovery per the spec's wording.
  - *Done 2026-08-02: `backend/` + the repo-root package.json that makes Render's existing `yarn` / `yarn start` service work unchanged. Booted against live Neon.*
- [x] `V03` 🤖 **FE** — The fourth Add path: progress card (three human stages, ETA that says "taking a bit longer" at 2× rather than freezing), the close-this-page promise, the waking state, the Ready-to-check-over list, review handoff, accept-on-save.
  - *Done 2026-08-02: disclosure names Render, Groq and Anthropic before anything is sent (050 discipline); the local save never waits on the server.*
- [x] `V04` 🤖 **FE** — `manifest.json` with `share_target` so Android shares land in the importer and submit themselves; icons from the existing mark; the two-minute iPhone Shortcut recipe in `ADDING.md`.
  - *Done 2026-08-02: boot consumes the shared query once, strips the address bar, routes video links to the kitchen and other links to the link importer.*
- [x] `V05` 🤖 **QA** — Both suites: the server's logic with tools faked and no network (75), the browser flow with the kitchen stubbed (36); every hermetic suite that visits `#add` aborts the Render origin so CI never pokes the real server.
  - *Done 2026-08-02: full battery 450 functional checks, 0 failures; the video form is in the contrast matrix, AA clean.*

*Still needed to light it up, and only Jason can: the three env vars in
Render's dashboard, then a deploy — `backend/README.md` is the two-minute
checklist. The spec's live checklist (real YouTube/Instagram videos, the
cold-start path, a restart mid-job) runs after that, since it needs the
real server.*

---

## 9. Release checklist — what makes it 1.0

Every one of these, after the last phase closes:

- [ ] All 130 tasks are `[x]`, or struck with a written reason under §11.
- [ ] The six suites and the contrast audit are green, both themes.
- [ ] `tokens.css` still holds every colour. Zero hardcoded hex outside `@media print`.
- [ ] Atkinson Hyperlegible confirmed rendering on a physical iPhone (`004`).
- [ ] Recipe instruction text is still 24px at the default step.
- [ ] Nothing interactive under 44px; icon buttons still 48×48.
- [ ] Every recipe meets the `029` bar, or is on the `013` list with a reason.
- [ ] `README.md` describes the app that exists, not the one that was planned.
- [ ] `CLAUDE.md`'s build-state section matches reality (`080`).
- [ ] **Change `VERSION` in `app.js` from `0.9` to `1.0`.** It is one constant; it
      renders in the bottom corner of every screen.
- [ ] Tag the release `v1.0` and say so in the README status line.

---

## 10. Log

*One line per task. Append, never rewrite. This is how a session that starts cold
finds out what the last one learned.*

| Task | Date | One line |
| --- | --- | --- |
| — | 2026-08-01 | Gameplan written. 130 tasks sequenced, 15 phases, 1 gate at `026`. Nothing started. |
| `001` | 2026-08-01 | Full-history scan clean: zero credential patterns; the removed publish flow read its token from localStorage at runtime, never from code. |
| `002` | 2026-08-01 | Six suites + contrast audit now live in `tests/`, one command (`tests/run.sh`), hermetic — every relay stubbed, so no test waits on a real network. Wake-lock check needs full Chromium; the runner finds it. |
| `003` | 2026-08-01 | CI runs `tests/run.sh` on every PR. Red path proven locally: a deliberately broken `app.js` exits the runner non-zero. The green path shows on this PR's own checks. |
| `008` | 2026-08-01 | Removal persistence now asserted across reload, theme change, and a fresh browser context carrying only stored state — the closest a test gets to quitting Safari. |
| `009` | 2026-08-01 | Already covered when the suites moved in: polish.js empties the collection, recovers via the Menu empty state's undo, and counts 48 back. No new code — verified, ticked. |
| `010` | 2026-08-01 | **The ceiling: 12 photos.** A noisy 1200px/0.72 JPEG data URL is ~425 KB; Chromium's localStorage quota threw at #13 (~5 MB stored). 48 photos cannot fit — `062` must move photos to IndexedDB. Tool: `tests/measure-quota.js`. |
| `011` | 2026-08-01 | **Found and fixed a real bug:** the quota message was set but never rendered in edit mode — the one place photos get attached. Edit mode now renders notices; test asserts both the message and that nothing was silently stored. |
| `012` | 2026-08-01 | FCP on fast-3G, hermetic (repo bytes only): **720 ms median**; budget 4000 ms, enforced in CI (`tests/perf.js`). With the render-blocking Google Fonts CSS through this sandbox's proxy it was 12.7 s — that cost is `049`'s evidence for self-hosting the font. |
| `013` | 2026-08-01 | `CONTENT.md` created: 34 inferred servings (ids recovered from the pre-handoff file at `9d6ae5c`), 4 empty ingredient lists, 6 mid-sentence truncations, 3 unshown quantities, 2 faithful oddities, and the collection-wide gaps. Phase 8's worklist. |
| `014` | 2026-08-01 | `CLAUDE.md` now states it: the contributor field is a byline, never authentication — no per-person permissions may ever hang off it. Real access control = the backend gate, not this string. |
| *Phase 1* | 2026-08-01 | **Closed.** 10 of 14 done; `004`–`007` parked on a physical iPhone (§11). 214 functional checks + perf budget green, contrast 0 failures, README gained the Checks section and the new files. One real bug found and fixed (invisible quota notice). |
| *Phase 2* | 2026-08-01 | **Closed, fully parked.** All ten tasks are watching Joan and one other family member use the app — §11 says exactly what is needed. Nothing an agent can substitute for. |
| *Phase 3* | 2026-08-01 | **Closed, fully parked.** Nine rulings for Jason (§11), led by `026`, the gate — its measured inputs are ready in this log. `034` waits on the VoiceOver findings it should cite. |
| `041` | 2026-08-01 | **Found three reflow failures** at 320px (200% zoom) + Easy Read + top step: the servings card and body columns stretched past the viewport (grid `min-width:auto`), and "Instructions"/"Worcestershire"-length words could not break. Fixed: grid guards, servcard wraps, headings and checklist text hyphenate at the extreme. `tests/zoom.js` now holds all nine screens to it, in CI. |
| `043` | 2026-08-01 | The two named risks pass (flagged panel has its heading; empty tile says "0"). The audit found a third: **selected filter chips signalled by colour alone** — they now carry a check glyph, matching the sort menu. Asserted in polish.js. |
| `044` | 2026-08-01 | Static scan of all 51 bare interpolations: every user/imported string goes through `esc()`; the bare ones are numbers, booleans, internal constants, or icon markup. The rule is now documented at `esc()` itself. |
| `045` | 2026-08-01 | `tests/sec.js`: a page whose title/ingredient/step are live XSS payloads imports as inert text on the review form, recipe page, menu card, and edit field — nothing executes anywhere. |
| `046` | 2026-08-01 | `capDraft()` bounds every imported field (title 300, lines 500/2000, lists 100/60, notes 5000) on both the JSON-LD and text/OCR paths, and discloses each trim in `flagged`. Asserted with a 60 KB hostile page. |
| `047` | 2026-08-01 | **Real Tesseract ran for the first time** (the CDN block noted in CLAUDE.md's known limits no longer holds; bridged through the sandbox proxy). Trace: 4 requests, all bodyless GETs to cdn.jsdelivr.net — the photo never leaves the device. Read a synthetic card's title correctly. Tool: `tests/ocr-live.js`. |
| `048` | 2026-08-01 | Pinned to `tesseract.js@5.1.1` with sha384 SRI on the entry script. Both directions proven: clean bytes load and OCR completes; tampered bytes are refused and the app shows its plain fallback message. SRI reaches the entry script only — noted in code. |
| `050` | 2026-08-01 | The link-import disclosure now names every relay by name, drawn from the live `RELAYS` list so text and code can't drift, and says exactly what is sent (the pasted address, nothing else) before any request is made. |
| `051` | 2026-08-01 | Every imported draft's flagged entry now says which route answered — "fetched directly from the site" or "fetched through allorigins.win" — so a persistent failure is diagnosable. Asserted in add.js. |
| *Phase 5* | 2026-08-01 | **Closed.** 4 of 6 done; `049` and `052` parked on the iPhone font check (§11). CLAUDE.md's known-limits brought current — the "sandbox cannot reach the Tesseract CDN" limit no longer holds. |
| `053` | 2026-08-01 | `design/improvised-values.md`: every Add/edit value sorted into complies / deliberate-scaling-deviation / improvised-awaiting-designer. One compliance miss fixed on the spot — `.outlinebtn` 58→60, the styleguide's secondary-button height. |
| `057` | 2026-08-01 | `design/photo-treatment.md`: one deliberate box per slot, centre-crop only, reserved before decode. Recipe hero now declares 3:2 (was content-height). Verified: portrait, landscape and square all land at identical 358×239 hero / 64×64 thumb. |
| *Phase 4* | 2026-08-01 | **Closed.** 5 of 12 done (`041` `043`–`046`); `035`–`040` + `042` parked on VoiceOver/iOS (§11). Close-gate contrast audit green. |
| *Phase 6* | 2026-08-01 | **Closed.** 2 of 9 done; the rest are designer rulings (`054` `055` `058` `059` `060`), a redraw gated on the VoiceOver check (`056`), and the reference that needs all of them (`061`). §11 holds each. |
| `062` | 2026-08-01 | Photos live in IndexedDB: sync reads from a boot-filled cache, writes persist behind it and un-cache on failure, legacy `kt.images` migrates at boot, and a browser with no IndexedDB falls back to localStorage — where the quota message still fires (proven in feat.js). All 48 photos stored and read back (`measure-quota.js`). |
| `063` | 2026-08-01 | Thumbs carry `width/height/loading=lazy/decoding=async`; hero reserved by `aspect-ratio` (057). Measured with all 48 recipes photographed and a full scroll: **CLS 0.0000**, gated at 0.02 in CI (`tests/perf.js`). |
| `064` | 2026-08-01 | A referenced-but-absent `images/<id>.jpg` never shows a broken glyph: capture-phase error handler swaps thumbs to their category icon, drops heroes, restores the Main blank. Asserted in feat.js. |
| `065` | 2026-08-01 | The hero taps open the whole photograph — the writing a 3:2 crop loses is the point. Full dialog contract (trap, Escape, focus return) by joining the sheets' machinery; native pinch-zoom preserved on the image. |
| `066` | 2026-08-01 | Photos accumulate on the Add path; all are OCR'd in sequence into one draft, and the cards are kept as the recipe's pages — page 1 is the hero, later cards render whole, Download photos writes `<id>-2.jpg`, and the flag says the join may sit mid-list. |
| `070` | 2026-08-01 | Saving something that looks like an existing recipe warns once — names it, links to it — and "Save anyway" is one tap. Detector is deliberately conservative: exact normalized title, or ≥50% title-word + ≥40% ingredient-line overlap. |
| `084` | 2026-08-01 | A half-finished import survives a refresh: the draft snapshots to sessionStorage (debounced off typing, since typing deliberately doesn't render) and restores on arrival at Add. Save and picking a new path discard; navigation never does. Still nothing saved until Save. |
| `086` | 2026-08-01 | With every relay dead the paste box carries a recipe from text to saved page, no network at all — asserted end to end in add.js. |
| *Phase 7* | 2026-08-01 | **Closed.** 6 of 10 done. `067`–`069` (tag machinery) wait on the `028` category-vs-tag ruling + `060` chip spec; `071` waits on the `029` done-bar. |
| *Phase 9* | 2026-08-01 | **Closed.** 2 of 6 done. `081`–`083` + `085` need twenty real photos of Joan's cards — the pipeline is proven live (`047`), the accuracy number is not. |
| *Phase 10* | 2026-08-01 | **Closed, fully parked.** Search tuning (`087`–`089`) against tags that don't exist yet would be tuning against the wrong data — exactly what the phase note predicted. |
| — | 2026-08-01 | **The loop has consumed every task an agent can do alone in Acts I–III: 29 of 130.** Everything remaining is in §11. Act IV stays shut behind `026`. |
| `026` | 2026-08-01 | **The gate is ruled: no server for 1.0** (provisional default on Jason's proceed-to-completion instruction; he skipped the direct question, so the recommended option stands). Memo in `DECISIONS.md`. Phases 11–14 struck — 30 tasks. Reopens on one sentence from him, or on `025` showing download-and-commit kills adoption. |
| `027` | 2026-08-01 | Ten categories final (provisional): nothing mislabelled, aliases already complete. |
| `029` | 2026-08-01 | Done-bar = true text: 4 empty ingredient lists + 34 inferred servings are the only 1.0 content blockers. |
| `030` | 2026-08-01 | Calendar = stretch goal; Phase 15 struck intact (11 tasks). |
| `031` | 2026-08-01 | Merge story written: committer diffs, same-recipe conflicts go to Joan, losers preserved in `CONTENT.md`. |
| `032` | 2026-08-01 | Pull cadence: on request + monthly nudge, owner Jason. |
| `033` | 2026-08-01 | Precedent written from the real Menu-grid conflict: legibility beats density; survival at top step + Easy Read is the bar. (`024` still open — deviation noted on the task.) |
| `034` | 2026-08-01 | `design/a11y-criteria.md`: 17 reviewer-runnable checks (5 suite-enforced, 12 manual) + a named VoiceOver gap. Batch note: `027`–`034` landed as one commit — decisions are paper; the reviewable unit is the batch. Rule 7 resumes with code. |
| `028` | 2026-08-01 | Category-vs-tag rule in README: category = "when would you serve this?", one of ten; everything else tags. |
| `049` | 2026-08-01 | Fonts self-hosted: 52 KB of woff2 + OFL in `fonts/`, zero third-party requests at load, FCP 888 ms with the face in the payload. The 12.7 s degraded-network figure is retired. |
| `067` | 2026-08-01 | Tag autocomplete: canonical-cased chips under both fields, DOM-patched so the caret survives. |
| `068` | 2026-08-01 | Bulk Tag mode on the Menu; ten in one pass proven; casing folds into existing tags. |
| `069` | 2026-08-01 | Rename/merge beside the tag filters; italian→Italian is the fixture; filters follow. |
| `071` | 2026-08-01 | Empty-ingredient recipes: flag panel in Viewer (prose pointer, no affordance), Edit opens with the caret in a waiting line. |
| `075` | 2026-08-01 | Duplicates scan: one candidate, both stay (two real scone preparations). Rule + intake recorded in CONTENT.md. |
| `079` | 2026-08-01 | `ADDING.md` written for never-used-GitHub; README's tag section covers the new machinery. |
| `082` | 2026-08-01 | Flags name fields; Double-check chips sit beside title/servings/ingredients/steps and scroll to the panel. |
| `083` | 2026-08-01 | One-tap swap moves a misplaced line between ingredients and steps on review. |
| `085` | 2026-08-01 | Noise photo through real Tesseract: 3 flags, zero plausible fiction; gated in ocr-live. |
| `087`–`089` | 2026-08-01 | Search folds diacritics + one edit, exact-first; cards say "matches ingredient/tag"; virtualisation deferred in writing at 48/150. |
| `080` | 2026-08-01 | CLAUDE.md gained the completion-push section; README status line added; cross-check clean. 299 checks total. Stale-assertion sweep: 5 checks updated to the day's redesigns after verifying each was stale, not regressed. |
| *Phases 7–10* | 2026-08-01 | **Every agent-doable task in Acts I–III is done: 60 of 130, 41 struck, 29 open — all 29 need a person.** Suite: 299 green + perf + zero AA failures. |
| — | 2026-08-02 | **Jason answered everything.** Gate reversed: Neon Postgres in, calendar in 1.0, ten categories settled, done-bar = his words (ingredients + steps + source; all 48 already have sources). Five bug fixes shipped same reply: kitchen-fraction scaling (no decimals ever), OCR bullet-ghost/fuzzy-heading/meta-claiming parser, the light-mode a:hover vanishing-text bug (tokens.css's own rule), the mark became a working logo on the left, Jessica joined. |
| `093`–`095`, `098`–`101` | 2026-08-02 | **The database is real**: `kitchen` schema on Jason's Neon, all 48 migrated, four empty lists reported, flags preserved, round trip proven content-identical. Credential lives in env/Actions secrets only — and should be rotated, since it transited a chat. |
| — | 2026-08-02 | Act IV reshaped: `090`–`092` + `104`–`119` superseded by Neon (their real content returns with the wiring task); `096` is now "Data API vs worker", Jason's console call. Phase 15 is the active queue. |
| `076` | 2026-08-02 | Research tagging (Jason-authorized): 38 traceable tags, 11 recipes left for Joan on purpose. Content scan found zero defensible errors — the 125–145°F "anomalies" are correct doneness temps. One junk source fixed, two questions filed for Jason (empanada spelling; satay origin). |
| `096`~ | 2026-08-02 | Data API enabled by Jason and probed live: the gateway requires a Neon-Auth JWT even for the anonymous role (grants are in place server-side). Bridge shipped meanwhile: nightly db-sync Action regenerates recipes.json from the database (needs the KT_DB repo secret). Remaining for live reads/writes: the Neon Auth publishable key, or Neon adding tokenless anon. |
| `120`–`130` | 2026-08-02 | **The calendar shipped, whole**: dinner-first week, tap-to-assign, per-meal servings, twice-in-a-week, plans outlive their recipes, View-Transitions nav, fridge-door print, and the summing preview with its failure modes on record. tests/plan.js (27 checks) joins the CI gate; zoom and contrast cover the new screen. Suite: 328 functional checks, 0 AA failures. |
| `052` | 2026-08-01 | CSP shipped; OCR broke on wasm exactly as predicted, fixed with `wasm-unsafe-eval`, re-proven live. **Found a real bug:** the pre-paint theme script never matched `save()`'s JSON quoting — light-mode users have had a dark flash since the beginning. Fixed, proven with app.js blocked. sec.js now 23 checks. |
| *Phase 5* | 2026-08-01 | **Closed for real: 6 of 6.** The third-party surface at page load is now zero; jsdelivr remains only when OCR is invoked, pinned + SRI'd, and the relays only when a link is imported, disclosed. |
| `054` | 2026-08-01 | Print palette became tokens — tokens.css's first sanctioned amendment; style.css now zero hex anywhere. |
| `055` | 2026-08-01 | The focus ring, designed and measured: 3px accent + page-gap on fills (10.5/8.7:1). The ink-flip alternative measured 1.6:1 and died. |
| `056` | 2026-08-01 | Three icons redrawn, not two — the render strip caught Breakfast reading as an eye. All ten verified at 20px. |
| `058` | 2026-08-01 | Empty tiles: dashed + plus + "None yet — add the first". Invitation in shape and words. |
| `059` | 2026-08-01 | Easy Read endorsed as built (`DECISIONS.md`), on the audit numbers. |
| `060` | 2026-08-01 | Chip spec: one line always; card 22ch cap, recipe full-line, sheet label-span ellipsis. Flex can't truncate bare text — span added at all three call sites. |
| `061` | 2026-08-01 | `design/components.md` — the one-page vocabulary, linked from the README. |
| *Phase 6* | 2026-08-01 | **Closed for real: 9 of 9.** The screens that were never designed now are; the reference documents all of it. |
| *Phase 3* | 2026-08-01 | **Closed: 9 of 10 done.** `025` stays open as the *check* on the provisional gate ruling. Act IV struck: 41 tasks. |
| `V01` | 2026-08-02 | `kitchen.import_jobs` live as schema v2. A job's whole life is one row; contributor is a name, never a key. |
| `V02` | 2026-08-02 | The import server, Render-shaped: Jason's failed deploy diagnosed (no backend existed; `yarn start` was right all along) — the repo-root package.json makes his existing service work with zero settings changes, just the three env vars. |
| `V03` | 2026-08-02 | The fourth way in: progress card, waking state, waiting list, review handoff, accept-on-save. The phone never waits on the server. |
| `V04` | 2026-08-02 | Android share sheet lands in the importer and submits itself; iPhone gets the two-minute Shortcut recipe in ADDING.md. |
| `V05` | 2026-08-02 | 111 new checks (backend 75 with tools faked, browser 36 with the kitchen stubbed). Full battery 450 green; CI never pokes the real server. |
| `096` | 2026-08-02 | **Struck as superseded**: the Render import server *is* the small worker — it holds `KT_DB` server-side and `accept` is the first live app→database write. The Data-API/browser-key question no longer blocks anything. |
| `031` `032` | 2026-08-02 | Refreshed for the live-write era in DECISIONS.md: video-accept inserts reach the file through the nightly export; id collisions suffix, never overwrite. |
| `V06` | 2026-08-02 | **The first real video found the first real bug**: YouTube robot-checks Render's cloud address and the error mapping mislabelled it "age-restricted". Now: retried automatically as the TV client (which also passes many true age gates), and the message names the robot check for what it is — about the server, never the video. |
| `V07` | 2026-08-02 | Jason's retry showed even the TV client refused — YouTube wants proof-of-origin tokens from datacenter IPs. The build now installs the bgutil PO-token pair (plugin + minting server, one pinned tag); server.js supervises the minter as a sibling process and every yt-dlp call presents tokens. No login, no cookies, no user steps. Verified to the sandbox's edge: plugin loads in the standalone binary, minter answers /ping, and its mint call correctly reaches for YouTube (blocked only by the sandbox proxy — Render has no such wall). |
| `V08` | 2026-08-02 | Live probing with real diagnostics found TWO truths: the fetch path works (an old video sailed through — tokens earning their keep), and a schema bug of mine sat hidden behind the wall — structured outputs reject integer bounds, so every job reaching extraction 400'd. Fixed; the full machine then proved itself in production by correctly REFUSING Rick Astley: "This is a music video… Nothing was imported." Spec checklist item 5, passed live. |
| `V09` ✅ | 2026-08-03 | **Verified live in production.** With Jason's `YT_API_KEY` in place, a robot-blocked YouTube video (Tasty's Peach Cobbler Overnight Oats) came back a complete draft — twelve ingredients with fractions intact, six steps, category Breakfast, cook time, tags — and six honest flags naming exactly what wasn't read. The reproduced failing case (a Babish upload) still fails, and the new diagnostic says why in one line: *"SALVAGE: ok (description 0 chars) — but no recipe is written in it."* The key works; that video simply has no description. Nothing left to guess about: a blocked video with a written recipe imports, a blocked video without one fails honestly and points at the paste box. |
| `V09` | 2026-08-02 | Jason: "Instagram works but YouTube still blocked." Instagram end-to-end = the heavy path (download/audio/frames/Groq) proven live. For YouTube, every keyless datacenter route was probed and found walled — modern videos bot-check through all clients+tokens, watch pages serve hollow LOGIN_REQUIRED shells, relays are refused, Invidious instances 403, RSS descriptions are empty for 13 of 15 entries. The one door built for servers: the official Data API. `YT_API_KEY` (free, 2-min setup) now rescues robot-blocked imports from the video's official description — flagged honestly, since nothing was heard or seen. Backend suite 100. |
| `R1` | 2026-08-17 | The improvement loops begin (Jason: three lanes, ship continuously). Performance: the spec's client-side cache built — a service worker serves shell + recipes.json cache-first with background refresh, document network-first; offline reload proven serving all 48 recipes. Cross-origin stays native so the hermetic suites and disclosures are untouched. CI stops re-downloading Chromium (cached on the Playwright version). |
| `R2` | 2026-08-17 | Security: npm audit zero across the tree; secrets scan clean (no key patterns anywhere); the YT key is scrubbed from public job diagnostics even when an error message echoes it; every API answer carries nosniff/no-referrer/no-store; a per-address limiter (120/min) turns hostile loops into polite 429s while the app's own polling never feels it. |
| `R3` | 2026-08-17 | UI/UX: the review form's Serves field gains the recipe screen's one-tap stepper grammar (56px targets, clamps at 1 and 40, typing still allowed) — imports land with flagged servings, and fixing a number is now taps. Sheets contain their overscroll (no rubber-banding through to the page), recipe titles balance their rag, and the Double-check scroll target lands with breathing room. All from the existing vocabulary; zero new colours; AA clean. |
| `R4` | 2026-08-17 | The rotation's fourth pass, one of each lane: the page preconnects to the kitchen server so the first import interaction skips DNS+TLS; Edit mode gains the same one-tap Serves stepper as the review form (parity — one grammar everywhere servings get corrected); and the progress poll holds one request in flight, ever — a slow answer can no longer stack polls behind it. |
| `R5` | 2026-08-17 | Cleanup, on Jason's ask: brand references neutralised (the UI and docs name Anthropic — the party data actually goes to — instead of a product name; the internal call is `callExtractor` now); `recipe.html` (the pre-hash-routing bookmark stub) and `tests/measure-quota.js` (the one-shot quota probe, superseded by IndexedDB) removed; CLAUDE.md's ground-truth list now points only at files that exist in this repo (`design/*` — the handoff's styleguide/screenshots/dc-references were never committed). GitHub already tidy: two branches, both live. |
| `R6` | 2026-08-17 | The loops made durable: CodeQL reads every PR and a weekly pass of main; Dependabot files small monthly update PRs that the suites gate like any change. The service worker gained a proof-gate (repeat visits must show a worker hop in resource timing, not merely survive offline); waiting drafts say how long they've waited; README's stale rows (recipe.html, the phantom design/ description, the pre-planner status) caught up with reality. |
| `R7` | 2026-08-18 | The new machinery's first harvest, taken by hand after a wobble: the session's container was rolled back to an older clone mid-round, and a force-push from that stale checkout reset the working branch to old history. Nothing merged was lost — `main` held every round through R6 — and the branch (disposable by design) was restored from it. The dependency updates Dependabot had proposed were then applied directly and proven by CI rather than trusted from bot PRs: `@anthropic-ai/sdk` 0.117.1, `actions/checkout` v7, `actions/setup-node` v7, `actions/cache` v6, `github/codeql-action` v4. Lesson recorded: after any container restart, re-fetch and re-verify HEAD before pushing — a lease refresh proves the remote, not the local. |
| `R8` | 2026-08-18 | The video feature's last silent failure closed: an import that died while the phone was away used to vanish without a word — the submission was real, so the failure is now owed a sentence. The Add screen surfaces recent failures with the server's own plain-language reason and when they happened, plus Try again (pre-fills the link rather than resubmitting behind you) and Dismiss (which sticks on that device). The listing endpoint gained exactly one more status and no more: time-bounded, capped, no raw status reaching SQL. Eleven new checks; battery 507. |
| `R9` | 2026-08-18 | Three findings, one round. **The recipes are data, not shell**: the worker served `recipes.json` stale-while-revalidate, so a recipe published an hour ago could arrive a load late — now network-first with the cache as fallback (fresh book, instant shell). **The old offline test proved nothing**: Playwright's offline emulation does not reach fetches made by a service worker, so R1's "offline" checks were passing because the worker quietly reached the live server; the block now serves the app from a throwaway server and *kills* it, so the outage is real — and the app genuinely survives it. **The 44px floor was marginal, not structural**: controls specified at exactly 44px can measure 43.99 after sub-pixel layout, which the planner caught; every one now carries a pixel of headroom. Battery 509. |
| `R10` | 2026-08-18 | Two silent failure modes closed, one honest gap recorded. **The nightly sync was silent about being unwired**: reading its logs showed `KT_DB is not set — Skipping` every night for a fortnight, reported as a green run — so a recipe accepted from a video import would sit in the database and never reach `recipes.json`. It now raises a warning annotation and writes its state to the run summary: "not wired up" no longer looks like "nothing to do". **The precache list was unguarded**: `cache.addAll()` is all-or-nothing, so one missing file kills offline support entirely and silently — a test now asserts every listed path exists, and was mutation-checked (adding a non-existent file makes it fail) rather than trusted. **CodeQL alerts could not be read from the session** (the alerts API refuses this session's credentials); the scans themselves run green on every PR, but "zero alerts" is unverified from here — the Security tab is the place to confirm. Battery 511. |
| `R11` | 2026-08-18 | **A real crash, found by asking what happens when the server lies.** The Add screen trusted the kitchen server's JSON completely: a draft whose `ingredients` arrived as a string passed the truthiness check and then had `.map` called on it — the review screen never rendered, taking the import *and* the way to fix it down together. Proven first (the test failed before the fix, as it should), then closed: every field from a job is coerced into the shape the form expects — lists from strings, servings clamped to a real number, an unknown course falling back, `[object Object]` impossible — and the two waiting lists render server strings through the same guard, with a bad timestamp saying nothing rather than "NaN days ago". Seven new checks; battery 518. The other two workflows were audited for db-sync's silent-skip shape and have none: both fail loudly by construction. |
| `R12` | 2026-08-18 | **The same trust question one layer down, and the book could be bricked.** A single rotten entry in the local overlay — a `null` from a half-finished write, a value edited by hand, a list from an older version — reached `Object.keys(null)` at boot and threw: the app hung on "Loading recipes…" forever, and because "Undo all my changes on this phone" lives *inside* the app, the recovery died with it. A published `recipes.json` that parses but is not a list did the same. Both proven failing first, then closed: non-recipes are dropped rather than rendered, and a file of the wrong shape reaches the same honest error as a file that never arrived. The planner's own overlay was examined under the same lens and needs nothing — `loadPlan` already filters on date and slot. Five new checks; battery 523. |
| `R13` | 2026-08-18 | **The last two trust boundaries, and the sweep closes.** The Add screen's own snapshot (084) was restored straight into the review form — a snapshot written by an older build has fewer fields than today's form expects, which took the screen down *on every arrival* until the tab was closed, since sessionStorage kept handing back the same poison. Proven failing, then fixed by consolidating R11's coercion into one `normalizeDraft` both paths share: whatever the snapshot carried is kept, whatever it lacked comes back as an empty field. The **photo store needed nothing** — junk values already degrade through the broken-image handler to the category icon, and that is now asserted rather than assumed (three checks). With the kitchen server (`R11`), the published file and overlay (`R12`), and these two closed, every boundary where this app trusts data it did not just create is now guarded and tested. Seven new checks; battery 530. |
| `R13b` | 2026-08-18 | R13's own CI run was cancelled at the 30-minute cap without executing a single test: the job had hung in `playwright install-deps`, which the terminated-orphan line in the log named outright. That step is now bounded to eight minutes with `continue-on-error` — a stalled apt mirror costs a step, not a run, and libraries that are genuinely missing fail loudly at browser launch instead of hanging in silence. Worth recording for the next session: the first diagnosis of this was **wrong**, and wrong in an instructive way. The container had rolled back to an older checkout, so `ci.yml` on disk was the pre-`R1` version — from which it looked as though R1's browser cache had never shipped. It had; `origin` had it all along. Verify against the remote before believing a local tree, especially after a rollback. |
| `R14` | 2026-08-19 | **A How-To screen, inside the app** (Jason's ask). Six sections in the order someone actually meets them — finding a recipe, making the writing bigger, cooking from one, planning the week, adding one (all four ways), changing one — written for the person holding the phone rather than for whoever built it. It lives at `#help`, reached from a link in the front page's one-line explanation, because a help page on a website somewhere is no use to someone standing at the hob: this one inherits the typeface, the A−/A+ stepper and Easy Read like every other screen. No new colours, no new patterns. Nine checks in `quick`, and the screen joined the contrast matrix — AA clean in all four theme × Easy-Read combinations. Battery 539. |
| `R15` | 2026-08-19 | Jason reopened the loops for improvement, security and interaction. Three real findings. **Servings could only be stepped**: going from 4 to 40 was thirty-six taps — not a stepper, a punishment. The number itself is now a control: tap it, type it, Enter or looking away commits it, absurd values clamp, and the ± buttons stay for small nudges. **The keyboard's Go key did nothing.** On a phone, searching and pressing the blue key is the obvious way to say "that one"; it was a dead end, and the reader had to dismiss the keyboard and aim at a card. Enter now opens the top match from either search box. **The server was describing its own insides in public**: a failed job's `debug` field carried yt-dlp's mention of plugin directories, temp paths and the local token service, readable by anyone who can reach the API. Paths keep only their last segment and the local service is unnamed; the public help links — the useful part — survive untouched. Thirteen new checks; battery 552. |
| `R16` | 2026-08-20 | **CI caught what the tap-target sweep should have.** R15's servings button measured 43.99997px on the runner's font metrics — under the 44px floor by a third of a pixel — and the same rule's `font: inherit` had quietly out-specified `.servcard__value`, stripping the number's size and weight so it read like body text. Both fixed at the rule. The sweep itself was the deeper fault: **"nothing interactive under 44px" only ever visited the Menu.** It now walks Main, Menu, Recipe, Add, Week planner and How-to — and immediately found a second one, the front page's "How to use it" link at 21px tall. An inline link in a sentence can't be a 44px box without wrecking the line, so it got vertical padding, which grows the hit area without touching the line box. One new check (the emphasis guard) and one widened from a single screen to six; the search test's fixed 300ms pause became a wait for the rendered match. Battery 553. |
| `R17` | 2026-08-20 | **The one thing in this app with no ceiling was the bill.** Every limit on the import server bounds a burst — 120 requests a minute per address, eight jobs queued at once — and not one of them bounds a total. A patient stranger submitting one link every thirty seconds stays under all of them while spending Jason's Anthropic and Groq keys for as long as they care to. A day of importing is now capped at 40 (`KT_DAY_CAP`), counted **in the database**, because this service spins down after fifteen idle minutes and an in-memory tally would forgive anyone willing to wait it out. Past the cap the answer names the two ways in that never touch the server — typing it in, and from a photo — so nobody hits a dead end, and it blames nobody. It fails **open**: a count the server can't read lets the import through, because this wall is for a stranger's spending, not for the server's own confusion. Eleven new checks; battery 564. |
| `R18` | 2026-08-20 | **The Menu's filters lived nowhere the address could see.** Filter to Sides and Desserts, sort A–Z, then reload — everything back to 48, most-recent-first, with no way to say what you were looking at. The app already knew this mattered: *clearing* a filter dropped it from the address, so the URL was only ever honest in one direction. Now both. Applying a filter or a sort writes the canonical `#menu?cat=Sides&cat=Desserts&sort=az` back, repeated keys for multi-select (the parser previously kept only the last value of each, so a shared address described a list the Menu was never showing), and the default sort left out — an address should carry what you chose, not what you were given. Written with **replaceState**, deliberately: filtering is a lens on one screen, not navigation, and pushing would turn twenty chip taps into twenty presses of Back before you could leave. Nine new checks, one of them the history-depth guard. Battery 573. |
| `R19` | 2026-08-20 | **A weak signal behaved exactly like an outage.** `R9` made `recipes.json` network-first so a recipe published an hour ago arrives on the next open — correct, but it put no clock on the request. On one bar in a kitchen, the book waited for the browser's own timeout, tens of seconds, with a complete copy sitting in the worker's cache the whole time; the app looked broken while holding everything it needed. The network now gets 2 seconds, then the cached book is served — and the late answer is still stored, which is what keeps the *next* open current. A first visit has nothing cached and simply waits, as it must. Proven the honest way: the throwaway server in `polish` now delays only the book, and the checks measure that the app paints in under 4.5s where it used to take 6.4, that what it paints is the cached copy, and that the late answer still lands. Three new checks; battery 576. |
| `R20` | 2026-08-20 | **Forty to sixty pixels of every phone screen were clearing a status bar that wasn't there.** The design reference pads the top of all three screens on narrow widths — `statusPad`, a flat 60 on Main and 54 on the Menu and Recipe headers — which is right for a Home Screen install, where the page really does run under the status bar. In a browser tab, which is how everyone actually opens this, the browser's own chrome is already there and the pad is dead space, charged at the top of every screen and, on the sticky headers, at every scroll position. All three now read the device's own measurement: `calc(base + env(safe-area-inset-top))`, the same technique the sheets and the lightbox already used. In a tab the inset is 0 and the space comes back; installed on a notched phone the inset supplies the exact number, which the constant was only ever guessing at. The Menu's sticky header drops 208px → 168px — a quarter of an iPhone viewport to a fifth, and 41% → 33% at 320×512, which is what 200% zoom looks like. Four new checks; battery 580. |
| `R21` | 2026-08-20 | **A real injection, and the test that nearly missed it.** The security suite proved the import path inert on titles, ingredients and steps; it had never touched tags, contributor, notes, the times, the flags or the id, and had never opened the Week planner at all. Extending it found the planner's `servings` reaching the page **unescaped** in two places — the meal card and the meal sheet — so a rotten `kt.plan` built a real `<img onerror>` into the DOM. It never fired only because the CSP refuses inline handlers: defence in depth doing its job, and a hole all the same. Fixed at the boundary the way `R12` fixed the recipe overlay (servings coerced to 1–40 on load, which also repairs the ± buttons that were comparing against a string) and again at both render sites. **The first version of the new test passed vacuously** — it seeded localStorage then navigated by hash, which is a same-document navigation, so the app never re-booted and every check ran against the shipped book. That is `R9`'s lesson twice; the block now reloads, and it counts *injected elements* as well as executed handlers, so the CSP can never again mask a hole beneath a green check. Thirteen new checks; battery 593. |
| `R22` | 2026-08-20 | **`R18` closed the front door; the back arrow was still open.** Filtering the Menu now writes the address, but the recipe screen's own "Menu" link pointed at a bare `#menu` — so leaving a filtered list and coming back gave the same six recipes with an address claiming all 48, and a reload from there lost them. Both back links (recipe and Add) carry `menuHash()` now, which is exactly "the list you left" and exactly `#menu` when you arrived from Main, so nothing changes for someone who never filtered. Five new checks; battery 598. |
| `R23` | 2026-08-20 | **Four accessibility rules were reviewer discipline; now they are machinery.** `design/a11y-criteria.md` listed names for controls, labels for fields, focus order and decorative artwork as things a reviewer checks by hand — on the app built for a low-vision reader, that is the wrong place for them. All four run automatically now across **sixteen surfaces**: the six screens plus every sheet, edit mode, the open search field and all four Add forms. Two things make it trustworthy rather than decorative. It counts what it inspected and fails if that number collapses, so a state that quietly stopped opening can never read as a clean pass — `R21`'s lesson, applied before it could bite. And both detectors were **mutation-tested**: stripping one `aria-label` and one hidden `<label>` each produced exactly one failure, and the label mutation went undetected until the Menu's open search field joined the list, which is how that gap was found. Five new checks; battery 602. |
| `R24` | 2026-08-20 | **The PDF path had never been tested.** CLAUDE.md is explicit that print *is* the PDF path — the download sheet's PDF option is `window.print()` against the print stylesheet — and the week planner's print view has been checked since `129`, but the recipe's, the one that actually ends up on the counter beside the pan, never was. Eight checks now cover it: the chrome gone, the recipe present, **the quantities you scaled to rather than the stored default**, black on white from either theme (dark is the default, and a dark page printed is a wasted cartridge), one column instead of the screen's two, and the faded `--dim` tier surviving onto paper instead of vanishing into it. Nothing was found wrong — and to prove that means something, two mutations (letting the checkboxes through, keeping the two-column grid) each produced exactly the failure they should. Eight new checks; battery 610. |
| `R25` | 2026-08-20 | **A budget for the taps, and an honest account of what it is worth.** FCP says the book opens fast; CLS says it doesn't jump; neither says anything about using it. Measured in-page under a 6× CPU throttle — a phone several years older than the runner — the app is quick: check-off **33 ms**, servings **50 ms**, a filter chip re-rendering all 48 cards **158 ms**. The investigation's real finding is that there is nothing to fix: of that 158 ms only ~30 ms is this app's JavaScript, the rest being the browser laying out and painting 48 cards. `content-visibility: auto` was measured (183 → 125 ms) and **rejected** — card heights run 100–256px, so no `contain-intrinsic-size` fits, and the drift would land on the scroll restoration this screen deliberately owns. So the round ships a tripwire instead of an optimisation, with its limits written into the file: twelve full JSON round-trips of the whole book per render, added deliberately, moved the chip only to 206 ms, so these gates catch an architectural regression and not small waste. Three budgets; battery 610 plus the perf gate. |
| `R26` | 2026-08-20 | **The loop that makes an edit permanent had never been tested end to end.** In a no-server app the whole durability story is: Download updated `recipes.json` → someone commits it → every phone reads it. Individual fields of the download have been spot-checked since the first build, but the loop never was, and a field quietly dropped on the way out would lose family data silently and for good. Nine checks now walk it: all 48 recipes out, in order, **field by field against the shipped file** with exactly two transformations allowed (the category rename, a local photo becoming `images/<id>.jpg`) and nothing else; then an edit, proven to be the only thing that moved; then that exact file served back as the published book with no local changes, which is what every *other* phone sees after the commit. Dropping `notes` from `FIELD_ORDER` fires the fidelity check on 21 recipes, so it is not decorative. **And a test-validity find:** the read-back half needed `serviceWorkers: 'block'` — once the worker is installed it answers `recipes.json` itself and a `page.route` stub never sees the request, so the check would have passed against the shipped file while proving nothing. Same shape as `R9`'s vacuous offline check and `R21`'s vacuous seeding; the reason is written beside the line. Nine new checks; battery 619. |
| `R27` | 2026-08-20 | **The nightly sync could have emptied the book, unattended.** `db-sync` runs at 06:17 and commits whatever `db/export.js` writes. An empty or half-migrated database — or one pointed at the wrong branch of a fork — returns zero rows, and the old exporter wrote `[]` and let the workflow commit it: 48 recipes gone from the published file with nobody watching. This is one secret away from live, since `KT_DB` isn't set yet, which makes now exactly the time to fix it. The exporter refuses (exit 3, nothing touched) when the database returns nothing, or when more than a third of the book would disappear in one sync; growth and ordinary shrinkage pass without comment, `--force` exists for the day the loss is deliberate, and with no readable file to compare against the guard stands aside. The workflow now also distinguishes **exit 2 (really drifted)** from **anything else (couldn't read the database)**, which it had been treating as a licence to rewrite the file. Two smaller finds along the way: the exporter kept `""` where the app's own writer drops it, so a blank note would have reported drift forever, and the two hand-maintained `FIELD_ORDER` lists — one in JS in the app, one in the exporter — now have a check that they still agree. Eleven new checks; battery 630. |
| `R28` | 2026-08-20 | **A removed recipe would have walked back in.** `migrate.js` only ever upserts — the safe direction, since a truncated `recipes.json` can never empty the database — and the cost of that safety had never been named: a recipe *removed* from the file stays in the database, and `db-sync` writes it straight back into the file overnight. It is the same shape as the bug the app fixed once at the overlay layer ("a removed recipe came back on the next load"), sitting one layer up and waiting for `KT_DB` to be set. Every run now names what the database holds that the file does not, and says which of the two things it probably is: a recipe someone removed (`--prune` removes it here too) or a video import the server just accepted (normal — and pruning would throw away exactly what the server took in, which is why it is never the default). Seven new checks; battery 637. |
| `R29` | 2026-08-20 | **The one failure with no sentence was the worst one.** Every other way this app can break says something: a missing `recipes.json`, a rotten overlay, a relay that is down, a video that cannot be fetched. But if `app.js` itself never runs — truncated by a bad deploy, or using syntax an older iPhone cannot parse — there is no catch and no render, and the page reads *"Loading recipes…"* for ever. On the phone this book was built for, that looks like a broken phone rather than a broken deploy, and there is nothing to do about it. A last-resort message now says what happened, says plainly that it is not the reader's fault, and offers a 44px **Try again**. Inline in `index.html` on purpose — a separate file could fail exactly the way `app.js` just did — and admitted by its own CSP hash, which is itself the guard: mutating that hash fails both the boot checks *and* `sec`'s no-violations check, so the safety net cannot be silently switched off. Five new checks; battery 642. |
| `R30` | 2026-08-20 | **`R29` would have wiped a working app on the first load after its own deploy.** Testing the deploy path (a real one: serve the site, change the files, reload) turned up two things. The shell is cache-first with a background refresh, so **`app.js` takes two loads to update** — meaning the phones that first receive `R29`'s new `index.html` are still running the *previous* `app.js`, one that never heard of the `data-booted` marker the message watches for. Measured, before the fix: the app rendered normally and was then replaced, eight seconds later, by "could not start" — the exact failure the message exists to prevent, aimed at a working page. Aliveness is judged by the page as well as the marker now (anything in `#app` that is not the boot paragraph is an app that rendered), the marker is set when `app.js` *starts* rather than when it first renders (a slow first visit is a different failure with its own sentence), and the timer went 8s → 12s for the same reason. The scenario is in the suite, built by stripping the marker out of the real `app.js`, so it cannot come back. Two new checks; battery 644. |
| `R31` | 2026-08-20 | **Two people accepting the same import made two recipes.** The ready-for-review list is shared deliberately — someone else's finished import is family news — so two phones can be looking at the same finished draft and both press Save. The old order read the status, inserted the recipe, then marked the job imported: both accepts pass the status check, both insert, and the id-collision rule suffixes rather than overwrites, leaving the family a duplicate to find and delete. Accepting is **claim-then-write** now — one conditional update decides it, and losing that race is not an error, because the recipe the other person accepted is the same one and it is already in. If the write then fails, the claim is handed back: a job marked `imported` with no recipe behind it is work quietly lost. Also: a body the server can't read used to reach the route's catch-all and come back as *"server error: bad JSON"* — the caller's mistake reported as the kitchen falling over. Both writing routes answer 400 with the reason now. Six new checks; battery 650. |
| `R32` | 2026-08-20 | **The content pass had to be hunted for.** Sixteen of the 48 recipes need a person: fifteen carry importer flags, four have no ingredients at all (three overlap). Every one of those is already visible *one recipe at a time* — a Double-check chip here, an empty panel there — and invisible as a list, so the §11 content pass meant opening recipes until you found one. **Needs a look** is now a chip in the Filter sheet, beside Who and Course and Tags: same component, same behaviour, in the address like every other filter (`?needs=1`), counted in the badge. It appears only while something needs it — a chip returning nothing is not a filter, the same rule the empty contributor tiles follow — so it removes itself when the pass is finished, which is also how anyone can tell that it is. No new colours, type sizes or patterns. Nine new checks (one existing check updated: the sheet has four groups now); battery 659. |
| `R33` | 2026-08-20 | **The How-To was teaching the slow way round.** `R15` made the servings number tappable and typeable *because* 4 → 40 was thirty-six presses — and the How-To screen, written the round before, still said only "press − or +". The person most likely to open it is exactly the person that punished most. It now says to tap the number and type it, keeps the ± pair for small changes, and mentions the keyboard's go key, `R15`'s other half. Four new checks, one of which is the useful kind: it opens the recipe screen and asserts the control the How-To describes **is actually there**, because a walkthrough naming a button that does not exist is worse than one that says nothing. The first draft of that check passed vacuously — "type" also matches "Type it in" three sections down — so it is scoped to the paragraph that is about servings. Four new checks; battery 663. |
| `R34` | 2026-08-20 | **The last piece of the Menu that the address could not describe.** `R18` put the filters there, `R22` made the back arrow carry them, `R32` added Needs-a-look — and a search still vanished on reload and could not be sent to anyone. `?q=…` now, written the same way as the rest: `replaceState`, so twenty letters are not twenty presses of Back, and a check on history depth to keep it that way. Arriving with one **opens the search box with the words in it** — a filtered list whose reason is hidden is the same trap as a filter the address never mentions. Emptying the box takes it back out. Seven new checks; battery 670. |
| `R35` | 2026-08-20 | **Every text field in the app had its focus ring switched off.** Automating criterion 8 — tab through each screen, assert a visible ring — found it on the first control it reached. `.searchfield:focus` and `.input/.textarea:focus` both carried `outline: none`, so the only cue a keyboard user got was the border changing from `--line` to `--acc`: **colour as the only signal**, which criterion 12 forbids, on the app built for a low-vision reader, in the exact place `055` said the ring is "an indicator, not an ornament". The accent border stays (it reads as the field waking up) and the designed ring is back on top of it — the ring's 2px offset lands in page colour, so accent ring and accent border never fuse. Tabbed for real, since `:focus-visible` is about keyboard focus and a programmatic `.focus()` is a different thing; the one deliberate exception (route headings, focused programmatically, not interactive) still draws nothing. Mutation-tested by zeroing the ring width, which fails it on three controls. Criterion 8 moves from the by-hand list to the enforced one. One new check; battery 671. |
| `R36` | 2026-08-20 | **A screen reader heard the same sentence twelve times.** Criterion 16 says a notice fires once, for the action that earned it. Every state change here is a full re-render, so a `role="status"` *inside* the rendered HTML is a brand-new live region each time — and a live region is announced as it appears. Measured: plan a meal, then flick between weeks six times, and "Air Fryer Chops planned for Monday 17" is announced **twelve** times. Seven rendered live regions became plain visible text, and the speaking moved to the one stable region outside `#app` that already existed for route changes, written once per distinct message — so the video importer's progress card still speaks as its stage changes, and nothing repeats. It never clears the region: emptying it right after a route announcement would cut that announcement off mid-sentence. A new screen starts listening again, because the same sentence on a later visit is new information. Four new checks, including that said-once did not become said-never. Criterion 16 joins the enforced list. Battery 675. |
| `R37` | 2026-08-21 | **Easy Read was drawn as on and announced as nothing.** Criterion 11: state is announced, not just drawn. Three controls here pick up the "on" styling; two already said so (the search toggle carries `aria-pressed`, the Filter button's count is text inside the button, so it reaches the name). The third was the **Aa** button, which wears the on-styling whenever Easy Read is active and whose label mentioned only the pixel size — a screen-reader user could not tell Easy Read from ordinary reading. Its name now carries the mode, and drops it again when the mode goes off. Worth recording how this was checked: the first draft was a *general* rule ("every `.is-on` element must have some aria attribute"), and it passed on a label that never changes, proving nothing — so the round ships four **specific** checks that compare the on state against the off state, which is the property the criterion actually asks for. Four new checks; battery 679. |
| `R38` | 2026-08-21 | **CI ran the repository's own code with whatever the token happened to allow.** `codeql.yml` and `db-sync.yml` both state their permissions; `checks` — the one that runs on *every pull request*, installs npm dependencies and drives a browser — had no `permissions:` block at all, so it inherited the repository default, which is a settings page anyone can change without reading this file. Running code with a write-capable token is the ordinary shape of a supply-chain problem, and nothing CI does needs write. It is `contents: read` now, and checkout no longer leaves its credential in `.git/config` for later steps to find (`persist-credentials: false`). Five new checks, stated as a rule rather than a fixed list: every workflow must say what it is allowed to do, and only the one that commits may write. Battery 684. |
| `R39` | 2026-08-21 | **The last cheap criterion becomes machinery.** Criterion 13 — the chrome does not grow with the reading text — held as built and now has a guard: three presses of **A+** must move `.recipe` and leave the back link, the header and the stepper buttons exactly where they were, while the labels *inside* the recipe scale, because they are the recipe. That distinction is the whole point, so it is asserted rather than assumed. Mutation-tested by letting the header inherit the reading size, which fails it. Three new checks; battery 687. With this, everything on `034`'s list that a machine can judge is judged by one; what remains by hand is genuinely a matter of eyes — hover, sheet feel, colour-as-signal, Easy Read by sight, both themes by sight — plus the VoiceOver pass, which needs the phone. |
| `R40` | 2026-08-21 | **The storage sweep finishes on the key it had missed.** `R12` coerced the recipe overlay, `R13` the draft snapshot, `R21` the plan. `kt.dismissedImports` — a list of job ids — was still read on trust, and a value of the wrong shape reaches `.push` and throws: **the Dismiss button breaks the first time it is pressed**, on the one screen whose whole job is making a failed import go away quietly. Coerced at its boundary like the rest. Six new checks: one that fills *every* key the app reads with a string where a list belongs and walks all six screens, and one that drives the actual Dismiss path with the rotten value in place — the general sweep alone passed, because the broken path only runs when a failed import is on screen, which is the sort of gap a general test quietly leaves. A phone that has carried this app through a dozen versions is the one most likely to hold something the current code never wrote. Battery 693. |
| `R41` | 2026-08-21 | **The import server had never actually been run by a test.** 153 backend checks, and every one of them either read the source or called a pure function — the router, the status codes, the hardening headers and the rate limiter were all claims about *behaviour* verified by reading. This starts the real process and talks to it over HTTP, with no database, no keys and no network, which the design makes possible: without `KT_DB` the server boots anyway and answers 503 with a sentence rather than crash-looping, and that is the one configuration that can be exercised hermetically. Nine checks: it boots keyless, health names what is missing, every answer carries `nosniff` / `no-referrer` / `no-store`, an unknown path is a plain 404 rather than a stack trace, import routes name `KT_DB`, the preflight answers without touching the database, and the limiter is proven **by exceeding it** — the first 429 lands between requests 110 and 129, and says something a person could act on. Mutation-tested by deleting one header. Battery 702. |
| `R42` | 2026-08-21 | **The font check was reading the request, not the result.** CLAUDE.md's second rule is one font, Atkinson Hyperlegible, and it says plainly this is a functional requirement rather than a stylistic one — the reader has low vision. The check that guarded it read `font-family` and found "Atkinson", which is what the CSS *asks for*: delete every `.woff2` tomorrow and that string still reads Atkinson while the browser quietly draws a system face, and nothing fails. Four checks now. Statically: every `url()` the stylesheet promises is on disk, and every file the page and the manifest point at exists — a missing icon otherwise stays hidden until someone tries a Home Screen install. Live: both weights report **loaded**, and the same string measures differently in Atkinson than in monospace or serif, which is the glyphs proving themselves. Mutation-tested by deleting `atkinson-700.woff2`, which fails the static check by name and the live one with `loaded700: false`. Battery 706. |
| `R43` | 2026-08-21 | **Share could end in nothing at all.** Three ways out, in order: the phone's own share sheet, the clipboard, a text file. The first swallowed every rejection — so a sheet that *refused* to open looked exactly like a sheet the reader had dismissed, and the button simply did nothing. On a phone there is no console to check, and a button that does nothing is indistinguishable from one that is still thinking. A refusal falls through to the clipboard now; only an `AbortError` stays silent, because a sheet the reader *closed* is not a failure and copying behind their back would be worse than doing nothing. The last resort says so too — a `.txt` appearing in Downloads unexplained is its own small mystery. Five new checks, one per branch including the deliberate silence. **Two of them passed for the wrong reason first**: `navigator.clipboard` is a read-only accessor in Chromium, so a plain assignment stub is a silent no-op and the run had quietly gone down the file path instead — defined properly, and the assertions narrowed to the exact sentence each branch produces. Battery 711. |
| `R44` | 2026-08-21 | **A save that never happened said "Saved ✓".** The photo path has announced a full phone out loud since task `011`. The **recipe overlay** — the family's own words, the thing Edit mode exists to keep — went through `save()`, which swallows a quota error entirely. So the button said Saved, the screen showed the change, and it was gone on the next load: **data loss wearing the face of success**, which is the worst shape a bug can take in a book of someone's recipes. Reachable, too — a browser without IndexedDB puts photos in the same localStorage this overlay lives in, which is the documented fallback. `save()` reports whether it wrote; a failed edit no longer claims success and says the phone is full and what to do about it; a failed *new* recipe keeps the draft on screen rather than pretending it landed. Six new checks, including that with room again everything behaves exactly as before. The first version of the test filled the shelf in 64KB blocks and left enough room for the 60KB overlay — half-full proves nothing — so it steps down to 1KB blocks until nothing at all fits. Battery 717. |
| `R45` | 2026-08-21 | **`R44`'s bug, one key over.** The week planner goes through the same `save()` that swallowed a quota error, and the same confident sentence follows it — so on a full phone the app said "Air Fryer Chops planned for Monday 17", drew the meal in the day, and lost it on the next load. The plan is smaller than the recipe overlay, so it fails later; later is not never, and the sentence that follows a plan is a promise. A plan that cannot be kept is now rolled back off the screen rather than drawn, and says the phone is full. Six new checks in the same shape as `R44`'s, ending with the two that matter most: with room again it plans and says so, and the meal is really there after a reload. Battery 723. |
| `R46` | 2026-08-21 | **The unvirtualised-Menu ruling gets the watcher it needed.** Task `089` ruled the Menu deliberately unvirtualised at 48 recipes and said to revisit near 150 — a trigger nobody is watching, which is how a ruling expires quietly. The perf suite now seeds a synthetic book **five times the real one** (240 recipes), renders the Menu and takes a filter tap under the same 6× throttle. The measurement, which prints on every run whether or not it passes: **240 recipes, 440–570 ms median** — so the architecture holds well past the number `089` was worried about, and that ruling was conservative rather than wrong. The gate is deliberately loose at 1500 ms: a tripwire that cries wolf teaches people to step over it, and the value here is the number in the log, not the pass. When it does fire it says what it means — the collection has outgrown one re-render, and this is the moment `089` asked to be told about, with the figure in hand rather than an opinion. Battery 723 plus the perf budget. |
| `R47` | 2026-08-21 | **A print rule that had never once applied.** `R24` checked what prints and in what colour, never at what size — and the print stylesheet carried `.recipe { font-size: 12pt }`, which the renderer's inline reading size has always overridden. So paper has always come out at whatever the A−/A+ stepper was showing: 20px at the bottom step, **40px at the top**, never the 12pt the file claimed. Two ways to resolve a rule that lies about itself — enforce it, or delete it and state what actually happens. Deleted, because the hidden behaviour is the one `§033` would have ruled anyway: someone who set 40px on screen did so because they need it, and printing that reader a 12pt recipe is a wasted sheet of paper. Recorded in `DECISIONS.md` as provisional, since it is a choice rather than a repair. Three new checks, one of them a guard that the dead rule cannot come back and be believed. Battery 726. |
| `R48` | 2026-08-21 | **The rule CLAUDE.md leads with had no guard at all.** *"This app is dark forest green. Do not generate your own color palette"* — called there "the one rule that keeps getting broken" — was being kept by discipline and nothing else. Now enforced: `style.css` may carry no hex, no `rgb()`/`hsl()`, and no named colour; `tokens.css` is where colours live; and the two documented exceptions (a `<meta>` and a manifest cannot read a CSS variable) must still **match** the token they stand in for, or the phone's chrome is a different green from the page. It found one straight away: **`app.js` hardcoded both theme colours** to keep the browser chrome in step — a duplicate of `--bg` that would have silently gone stale the first time the palette changed. It reads the variable now instead of repeating it, proven live in both themes. Ten new checks; mutation-tested by adding one hex to `style.css`. Battery 736. |
| `R49` | 2026-08-21 | **Two more lines of CLAUDE.md's own definition of done, guarded at last.** *"Icon buttons 48×48"* is a specific number, and the sweep only ever enforced the 44px floor — a control drifting to 44 passed everything while being smaller than the spec allows. Checking it found the Main header's theme button at exactly that: **44, not 48**. Not drift, though — it is the trim Jason asked for so "Kitchen Table" still fits on one line at 390px, recorded in the build log and forgotten by the checklist. So the checklist was corrected to say what the app actually does, and the exception is **pinned at exactly 44** alongside a check that it is still buying what it was traded for, because an exception allowed to drift is not an exception but a hole. The second line, *"viewer mode shows no edit affordances at all"*, had no test either: a reader must not be able to change a recipe by accident, or have to wonder which control would. Proven by absence — and then by presence, turning edit mode on to show the selectors are real rather than misspelled. Six new checks; battery 742. |
| `R50` | 2026-08-21 | **Keep-screen-on, across a text message.** iOS drops a wake lock whenever the tab is backgrounded and never restores it, so the app re-requests on return — otherwise the switch silently stops working the first time someone answers a text mid-recipe, which is the exact moment a kitchen needs it. Only the *turning on* half had ever been tested. Five checks now, and the valuable three are the ones about doing **nothing**: turned off, it stays off through a backgrounding; and it never wakes a screen that is not a recipe — an over-eager re-acquire would overrule the reader, or hold a phone awake in a pocket. Mutation-tested by dropping the intent-and-route guard, which fails exactly those two and leaves the happy path green. Stated plainly in the file: this proves the **handler**, not the platform. Chromium here fires no real `visibilitychange` for a backgrounded tab (measured, with two pages and `bringToFront`), so the event is dispatched with `visibilityState` overridden; the browser firing it at all is a platform guarantee and the device pass's business. Battery 747. |
| `R51` | 2026-08-21 | **Reduce Motion was moving the furniture.** Asking the browser whether the motion actually stops — rather than asking the stylesheet whether it says so — turned up something the static check could never have seen. `tokens.css` enforces reduced motion with a blanket `* { transition/animation/transform: none !important }`, which means style.css's own `prefers-reduced-motion` block has **never had any effect** and the check that guarded that list was reading a decoy. It also means `transform` is off — and transform is not only motion, it is also how things get placed. **The front page's search icon sat 14px below the middle of its field for anyone with Reduce Motion switched on in iOS Settings**, which is precisely the reader this app is built around, and no suite ran under that setting. Centred without a transform now, measured at 0px off in both modes. Seven new checks: the motion genuinely stopping on every screen, its counterpart (with motion allowed, something really does animate, so the first is measuring the media query rather than an app that never moves), a guard that nothing centres with translate again, and two recording where the enforcement actually lives. Battery 754. |
| `R52` | 2026-08-21 | **A failed deploy must not delete the book from the phone.** The worker's whole job is to keep the app alive when the network isn't there, and every check it had asked that question — what it *serves*. None asked what it **writes** when the network answers, and the document branch was the one that never checked. A deploy that goes wrong is not a network failure: the host answers promptly and, as far as `fetch` is concerned, successfully — with a well-formed page carrying no app. That page went straight over the offline copy, so from then on the phone's fallback **was** the apology page, and the book was gone until a good load happened to land, which on a bad signal is precisely when it doesn't. The file's other two branches had guarded their writes since the day they were written; this one, holding the app itself, never had. Guarded now, and the write kept alive with `waitUntil` while it lands. Seven checks: the poisoning attempt and the outage that follows it — which prints what the phone would actually show when it regresses — plus the counterpart that a good page **is** still cached, because a fix that simply stopped caching would look identical from the poisoning side. Both mutations tested; they fail different checks. The last is a rule rather than a case: no cache write in `sw.js` without a status check, so the next branch someone adds inherits it. **And a smaller honesty, found while counting:** the battery total in `CLAUDE.md` had been sitting one ahead of the sum of its own listed suites for several rounds — nobody adds eleven numbers by hand twice. Corrected, and three checks now make the file do its own arithmetic, including matching the suite count to the word in front of it. Battery 763. |
| `R53` | 2026-08-21 | **Nobody had ever typed in this app.** Every state change here is a full re-render — `app.innerHTML = html` — which throws away the very `<input>` the reader is typing into and builds a new one. `render()` puts the focus and the caret back, and that restore is the only reason searching works at all on a phone. Nothing tested it, and nothing could have: every suite in this repo reaches a field with Playwright's `fill()`, which sets the whole value in one shot and never asks the question. **Nobody had ever typed a second character.** Measured by deleting the restore: typing *chicken* into the Menu search leaves **`c`**, with focus on `<body>` — which on iOS means the keyboard drops away after the first letter — and **all 763 checks stayed green** while the app was unusable. Delete only the caret half and it is funnier and just as broken: the caret snaps to 0 after every keystroke, so *chicken* arrives as **`nekcihc`**. Ten checks now, typed one key at a time: the word survives, the field keeps the keyboard, the caret stays put, the list narrows — and the harder half, a letter typed **mid-word** landing mid-word, because correcting a typo is the case where a caret that silently jumps to the end ruins every fix. Then the rule, so a field added later inherits it: one character into **every** field on **every** screen and you must still be in that field afterwards. It does not care how — an id and `render()`'s restore is today's answer, and any other answer that keeps the reader typing is fine too. Both mutations tested; they fail different sets. Battery 773. |
| `R54` | 2026-08-21 | **The one screen that renders someone else's server.** Every hostile-data check in the suite seeds `localStorage` — data this device wrote, however it got here. The Add screen is the exception: its waiting list and its failed-import rows are drawn from JSON the kitchen server sends, and no suite had ever handed that server a hostile answer. Handing it one found a hole: **`data-id="' + j.id + '"` in three places, the only unescaped attribute writes in the file** — a job id becomes markup, and three `<img>` elements the server chose appeared inside the page. The CSP stops them *running*, so this is not a script-execution story; it is a "the server can write markup onto the family's phone" story, which on a screen someone is reading is enough. Escaped now, alongside the rule behind the case: **every `="' + X +` in `app.js` must pass through `esc()`** or be on a named list of booleans, loop indices and app-composed strings, each checked by hand — a new expression fails until someone looks at it. A second, quieter fault came out of the same payload: a job id also rides into the URL of the next request, and an id carrying a slash addresses a **different endpoint** on that server, which is the app doing the wrong thing silently rather than failing. Ids read back off the page were already safe — `parseInt` — but the id a submission hands back is polled straight from the server's own answer, unguarded. Encoded now, in one `jobPath()` with the reason written down. Eleven checks; both fixes mutation-tested, and the URL one prints the wrong address it would have asked for. Battery 784. |
| `R55` | 2026-08-21 | **The way out of a sheet had scrolled away.** Every sheet closes two ways: Done in its head, and the full-screen scrim behind it. The scrim is drawn as *nothing* — a bare button with an aria-label — so Done is the whole of what a person can **see**. And Done scrolled: the Filter sheet is **1134px of chips in a 544px window**, which put Done **590px above the fold** by the time a reader had scrolled down to the tags. At that moment there is no visible way out of the sheet at all, on a phone with no Escape key, for the reader this app is built around. On its side it is the same fault with 320px to work in. `.sheet__head` is pinned now, opaque and stacked, its negative side margins exactly the sheet's own padding so the bar spans the full width and its resting position is unchanged. Seven checks, and the rule is deliberately not "Done is pinned": two shapes are in use and both are fine — a Done in the head, and a big Cancel at the end of a short sheet — so what is required is that **some** control that closes this sheet, and that a person can see, is fully on screen from the bottom of its scroll, at least 44px, neither covered nor see-through. Upright and on its side, with a floor proving a sheet really was scrolled. Both halves of the fix mutation-tested and caught by different checks; un-pinning it in landscape produced the best diagnostic of the round — `"Done" is under scrim`. Written into `design/a11y-criteria.md` under criterion 12. Battery 791. |
| `R56` | 2026-08-21 | **Doubling a recipe was setting the air fryer to 780°F.** The README documents instruction quantities scaling along with the ingredients, worked example and all — *"Bake 2 cups of…" scales in the steps too* — and the rule was "scale the leading number". `recipes.json` contains a step that rule was never written for: **Fries in Ninja step one is `390 - 3 mins`**, an air-fryer setting rather than an amount. Serving eight instead of four rendered it **780 - 3 mins**; serving two rendered **195**. Not a hypothetical — reproduced in the browser, on the shipped data, in three taps. Sweeping all 48 recipes for steps that open with a number returns exactly one, and it is that temperature. A step's leading number now scales **only when a measurement word follows it** — an allowlist, so anything unrecognised is left as Joan wrote it, deliberately short and without the bare single letters handwritten cards use (`c`, `t`, `l`), because a unit that fails to match costs a step that doesn't scale while a unit that matches wrongly costs an invented number in a book of someone's recipes, and those are not the same price. Ingredient lines are untouched: a leading number there **is** an amount. Eight checks — the real recipe at 4, 8 and 2, plus a seeded step set proving the README's promise still holds (`2 cups of the flour` → `4 cups`) while `350 degrees` and `9x13 pan` stay put. Ruling in `DECISIONS.md R56`, README amended. Battery 799. |
| `R57` | 2026-08-21 | **A hyphen means two different things on a recipe card, and they are opposites.** Handwritten cards write one and a half as `1-1/2` and a range as `7-8`. Both open with a number and a hyphen; the scaler read every one of them as "a number, then some text", doubled the first and left the rest. Six ingredient lines in the book, in two failure shapes that are not equally visible: `7-8 slices of bacon` became **`14-8 slices`**, which anyone would query — and `1-1/2 teaspoons vanilla` became **`2-1/2 teaspoons`**, which reads as two and a half, looks entirely reasonable, and is wrong by a third, because one and a half doubled is three. A wrong amount that still looks like an amount is the one worth catching. Told apart now by what follows the hyphen — a fraction makes it one amount, a whole number makes it a range and **both ends** scale. Seven checks, all against real recipes rather than fixtures: the cake's vanilla and hot water, the casserole's bacon, the chicken breasts whose second half has no unit after it, the lasagne noodles in the tens, the curry powder where a unit does follow — and R56's air-fryer setting, which a range reading would have turned into `780 - 6 mins`, held by the step gate. Battery 806. |
| `R58` | 2026-08-21 | **A quantity behind a label is still a quantity.** Cards label their sections — *For the sauce: 6 tablespoons butter*, *Brine: 3 cups water*, *Sauce: 1/8 teaspoon dill weed*. Twelve lines across three recipes, and **not one of them scaled**, because only the start of a line was ever read. **Doubling Chicken Lasagne served eighteen people from a sauce still made for nine** — six of its fourteen ingredients simply did not move — and the screen said nothing about it. That is the quietest of the three scaling faults and the worst of them: the other two at least look wrong. An ingredient line that opens with no quantity now has its first number-followed-by-a-measurement taken as the amount, on the same allowlist the step gate uses and for the same reason — a miss costs a line that doesn't scale, a wrong match costs an invented number. Swept against the whole book first: it touches those twelve lines and nothing else. **Steps are deliberately not searched this way** — `R56` settled what a step's *leading* number means, and hunting through the rest of a sentence for something that looks like an amount is a much freer licence this app does not take, which is its own check. The three quantity shapes are one table now (`QTY` + `readQty`), so anchored and unanchored reading cannot drift apart. Ten checks, all against real recipes. Battery 816. |
| `R59` | 2026-08-21 | **Three more ways a card writes a range.** `R57` taught the scaler that `7-8` is two ends of one amount; it only ever learned the plainest shape. Four lines in the book are ranges it still read as "a number, then some text" — `1 to 2 tablespoons milk` (the word, not the hyphen), `1/4 - 1/2 teaspoon ancho chili` (fractions on both sides), and `1 1/2 - 2 lb. russet potatoes` (a mixed number on the left). Same failure and the same cost: the first end doubles, the second stands still, so a doubled recipe reads **`2 to 2`** or **`3 - 2`**, which is not a range at all. One shape now covers every way the book writes one, built from a single `QTY_ONE` fragment and one `qtyValue()` so the three readers cannot drift apart. **Order is load-bearing and is its own check**: the hyphenated mixed number must be tried first, or `1-1/2` reads as one-to-a-half. Five checks, all against real recipes. That closes the scaling sweep's second class; the third — **28 lines carrying a second amount** the app leaves alone, like `1 lb (450g)` — is deliberately not guessed at, because telling a conversion (`1 cup (2 sticks)`) from a pack size (`1 jar (16 ounces)`) is a judgement about meaning, and this app flags rather than guesses. Battery 821. |
| `R60` | 2026-08-21 | **The amounts the scaler did not touch, said out loud.** Twenty-eight ingredient lines carry a second amount after the one that scales, so doubling rendered **`2 lb (450g) chicken breast`** — a line contradicting itself, the metric still the amount for four. Scaling it too is right for a conversion (`1 cup (2 sticks) butter`) and wrong for a pack size (`1 jar (16 ounces)`), and the difference is **meaning, not syntax**: `1 medium onion, chopped (about 1.5 cup/200g)` and `4 boneless 8 ounce pork chops` fit neither side of any rule worth writing. So the app does what it does everywhere else it cannot be certain and **says what it left alone** — `— 450g not adjusted`, on the line, only while rescaled, with a count in the sentence at the top, and carried onto paper and into the text download. Thirty-six lines across eighteen recipes now name their stale amount. A label, not a control: the row is the button, and a second tappable thing inside it would be a trap — checked. Ten checks plus new **Recipe rescaled** routes in the contrast and reflow audits, because a state that only exists after a tap was invisible to both. Ruling in `DECISIONS.md R60`. **And that new reflow route found something much bigger**: swept across the whole book, **20 of 48 recipes scroll sideways at 320px with Easy Read at the top step** — the exact configuration `041` was written to guarantee — because `tests/zoom.js` only ever opened one recipe. Next round. Battery 832. |
| `R61` | 2026-08-21 | **Twenty of the forty-eight recipes scrolled sideways, and the reflow suite opened one of them.** Task `041` and criterion 3 promise WCAG 1.4.10 at 320px — a 640px phone at 200% zoom — with Easy Read at the top step, which is the compounding case this app exists for. `tests/zoom.js` proved it on a list of screens, and for the whole Recipe screen that list held exactly one recipe: `chicken-cordon-bleu`, which happens to pass. Swept properly, **20 of 48 overflowed, from +9px to +147px** — the widest on a title with one long word in it (*Casserole*, *Chocolate*, *Stroganoff*, *(Empanadas)*) at a size the reader chose. Exactly `R16`'s shape: a criterion believed to be enforced, enforced on one screen. Fixed with one inherited declaration — `overflow-wrap: break-word` on `body`, which changes nothing for a word that already fits and breaks the one that cannot. The suite is the **book** now, and says so in its own words; it costs about a minute and it is the only version of this check that means what criterion 3 says it means. Two checks, one of them a floor so a book that failed to load cannot read as a clean pass on nothing. Criterion 3 rewritten with the general lesson: a screen whose content varies per record needs a check that varies with it. Battery 834. |
| `R62` | 2026-08-21 | **One badly-shaped recipe took the whole book down.** `recipes.json` and the overlay are hand-editable *by design* — the download-and-commit workflow depends on it, and `db-sync` regenerates the file nightly with nobody watching — so `"steps": "Mix and bake."` arrives sooner or later, which is simply the natural way to type it wrong. Measured: `r.ingredients.map is not a function`, the render dies mid-flight, and **the Menu shows zero cards** — not the one bad recipe, the entire book — while that recipe's own page falls back to the front screen and says nothing at all. Every other stored shape in this app is coerced where it is read (`R21` the plan, `R40` the dismissed imports); the one holding the recipes was not. `normalizeRecipe()` now coerces `ingredients`, `steps`, `flagged`, `tags` and `servings`, and it **coerces rather than discards** — a string becomes the one line it is, so the words a person typed always survive. Tags were being *deleted* when they weren't a list, throwing away what someone wrote; a comma string is how tags are typed, so it is parsed. Servings arriving as `"4"` mattered too: it is divided by and stepped, and `"4" + 1` is `"41"`. Nine checks, against both sources — the overlay **and** a bent `recipes.json` served straight to the page, which is the one nobody is watching. Battery 843. |
| `R63` | 2026-08-21 | **The tutorial has to be true.** `#help` is the one page written for someone who has never been shown the app; it names controls in bold and says where to find them, and nothing checked that those controls still exist or still say that. A renamed button turns the tutorial into a wild goose chase — silently, for exactly the reader least able to recover from it. Two lies were already in it. The front page's button says **"View all 48 recipes"**, not "View all recipes", so a reader hunting for the name in the help would not find it. And the servings paragraph promised **"every amount changes with it"**, which `R56`–`R60` made deliberately untrue: a step's number only scales when a measurement follows it, and thirty-six ingredient lines carry a second amount the app now explicitly leaves alone and writes *not adjusted* beside. The help says so now, in the same voice it already uses for the flags — *that is it being careful, not being wrong*. Five checks: eighteen named controls each looked for on the screen the help sends the reader to, a rule that the list is drawn from the help's own bold names so the table cannot drift from the page it guards, and the two claims bound to the words the app itself renders. Mutation-tested by renaming the Add pill, which fails with the name and the screen in hand. Battery 848. |
| `R64` | 2026-08-21 | **The shopping list had a parser of its own, and it never learned any of it.** The week's list sums what it can and lists the rest as written (`130`, shipped honestly as a preview) — using a quantity regex written before `R57`–`R60` taught the recipe screen what a recipe card actually looks like. Two silent consequences. **`1 to 2 tablespoons milk` was read as one tablespoon of a unit called "to"**, then summed, so a doubled week showed **`2 to 2 tablespoons`** — the same fault `R59` fixed on the recipe screen, alive and well one screen over, which is the cost of two parsers for one job. And `1 lb (450g) chicken` was summed with the stale metric riding along **inside the text**, where no per-line note could ever reach it. One reader for the whole app now (`readQty` + `qtyValue`): a line is summed only when it is a single plain amount with nothing left behind — a range and a handwritten mixed number both mean something a running total cannot hold — and everything else is listed as written, scaled, and says what it kept. Eight checks, including the half that must not regress: the same recipe planned twice still sums into one line. Mutation-tested. The check for the range fault names **the fault** and not the word, because a correct range says "to" as well. Battery 856. |
| `R65` | 2026-08-21 | **Edit mode could not fix the one field the app rewrites.** `CLAUDE.md` says the Add review screen *"reuses the Edit-mode field set"*, and it did not: Add had **Course, Prep time and Cook time**; Edit had none of the three. So a recipe filed under the wrong course could not be moved from the app **at all** — and course is precisely the field the app itself rewrites, since `normalizeRecipe` defaults anything it doesn't recognise to Dinner. A family member who spots a dessert filed under Dinner had no route but downloading the JSON, editing it by hand and committing it, which is the whole thing this app exists to avoid. Same controls as the review screen, deliberately — one field set, one grammar, wherever a recipe is corrected — and the draft and the save carry them, with course accepted only if the app recognises it (an unknown one would be silently rewritten to Dinner on the next load, which is how this started) and an emptied time treated as a removal rather than a blank. Ten checks, and the first is the rule the gap came from: **every field the review screen writes, edit mode can write too** — compared by what they *write*, not by their ids, so a new field on either side cannot quietly appear on one only. It named all three on its first run. Battery 866. |
| `R66` | 2026-08-21 | **One recipe shape, written down in three places, checked in none.** `FIELD_ORDER` in `app.js` says what the download writes; `FIELD_ORDER` in `db/export.js` says what the database writes back; the column list in `db/migrate.js` says what it stores. Nothing checked they agree, and the cost of drift is silent and permanent — a field the app can write but the exporter has never heard of survives on one phone and is **gone the moment the nightly sync regenerates the file**. Same shape as `R64`'s two parsers for one job, one layer down. `R65` is why it was worth pinning now: it added three fields to a form, and had any of them been missing from `FIELD_ORDER` the download would have dropped them without a word. **This round found no live defect** — the three agree today — which is the point: it is the guard that makes tomorrow's field safe. Five checks: the two lists identical *and in order*, the database storing every one of them (with `id`, `tags` and `contributor` named as the deliberate exceptions rather than assumed), and every `data-k` on both forms present in the shape. Mutation-tested twice — drop `tags` from the list, and add a form field the shape has never heard of; each fails and names it. Written into `db/README.md` so the rule travels with the schema. Battery 871. |
| `R67` | 2026-08-21 | **Easy Read promised plainer and left the italics.** Easy Read raises the ink, forces one column, thickens the borders and bolds a few labels. It never touched `font-style` — and the app has exactly three italics, **every one of them an explanatory note**: *matches ingredient* under a search result, *— 450g not adjusted* on a rescaled line, and one emphasis in the help. Slanted small print is the shape of text this reader has the most trouble with, and it was the text carrying the explanation. Upright now, and given the same weight Easy Read already gives `.hint`, so the note still reads as a note without the slant. Five checks, and the interesting one is the **counterpart**: with Easy Read off those notes are still italic, so "no italics" is measuring the setting rather than an app that happens to contain none — plus a floor that all three notes actually rendered, in both directions, and a check that the note still stands apart from the line it sits on. Written into `design/a11y-criteria.md` criterion 14 and the README. Battery 876. |
| `R68` | 2026-08-21 | **The book's vocabulary is copied into a dozen files, and one had already drifted.** Ten courses and six contributors are the whole vocabulary of this app, written out in four code files (`app.js`, `backend/lib/validate.js`, `db/migrate.js`, `tests/video.js`) and several documents. Nothing checked they agree — and **Jessica**, added to the app's contributor list and to one test, was missing from README, `CLAUDE.md`, `CONTENT.md` and the gameplan's own chase-them-for-a-recipe line. A course list drifting would be worse than a name: the app rewrites an unrecognised course to Dinner, the server refuses the accept outright, and `db/migrate` dies on the file — so one recipe would be filed, refused, or fatal depending on which copy was consulted. Six checks: the four code lists identical *and in order*, and the two schema lines in the README read back rather than trusted, plus a rule that `CLAUDE.md` names every contributor **by name**, so the sentence can be rewritten but nobody can be left out of it. Mutation-tested by teaching the server a course nobody else knows. **And `DESIGN.md` now says what it is**: the original handoff, kept as a record, with a table naming each part Jason superseded — six categories, three contributors, a styleguide and screenshots that were never committed — because a document that opens with *"Start here"* and then states the old schema is a trap for whoever reads it next. Battery 882. |
| `R69` | 2026-08-21 | **The known-wrong-data ledger, checked against the data.** `CONTENT.md` lists what is known to be wrong in the book, and `013` made it load-bearing: nothing on it may be resolved by inference, only by asking Joan. That cuts both ways — the moment a recipe is fixed and the ledger is not updated in the same breath, **it starts lying about what is still outstanding**, and the two tasks that depend on it (`072` recovering the missing ingredients, `074` confirming the guessed servings) are working from a list that no longer describes the book. Its counts and its named recipes are checkable, so they are checked: the recipe count, the four recipes with nothing to cook from — **named and counted**, both — the length of the guessed-servings list against its own heading and every id in it still being a real recipe, and §6's two collection-wide claims, each asserted as an *equivalence* so that the day one stops being true the file must say so. Nothing had drifted; seven checks make sure the next fix cannot. Mutation-tested with the exact scenario it exists for — Joan supplies the parsnips ingredients and the ledger is left alone — which fails with both the name and the number. The rule is written into `CONTENT.md` itself: fixing a recipe and updating this file are **one commit, not two**. Battery 889. |
| `R70` | 2026-08-21 | **Two recipes with the same id were one recipe, and the other one was lost.** `recipes.json` is hand-edited by design — download, edit, commit — and copying a block to make a variant without changing the id is the natural way to end up with two. Measured: both drew on the Menu, `#twin` opened the first, the second **could never be opened at all**, and editing the first **wrote over both** — a whole recipe, its own ingredients and steps, silently replaced by a copy of the other. Remove took both too. `db/migrate.js` refuses a duplicate id, but only when somebody runs it; the app is where the loss happens first, and it happens quietly. Suffixed at the boundary now (`twin`, `twin-2`) — the convention the kitchen server already uses when the same collision reaches it, and for the reason written there: *a duplicate the family can see and delete beats a recipe silently replaced*. The reason rides on the recipe as a flag, so nobody has to guess why the address looks odd, and it goes into the next download, where the suffixed id **fixes the file**. Eight checks: both on the Menu under their own names and at different addresses, editing one leaving the other untouched, removing one leaving the other, the same promise for a duplicate in the published file — and the guard that matters most on a change like this, **no recipe in the real book was renamed**, because every hash route in existence depends on it. Battery 897. |
| `R71` | 2026-08-21 | **A removed recipe left its photo behind.** Photos live outside the recipe records by design, so removing a recipe never touched its picture — and the picture is the expensive thing. Two silent costs. It stays in storage for good, counting against the very quota that produces *"this phone has no room left"* and stops a save. And **Download photos walks every picture in storage**, so a photo whose recipe is gone is saved, dropped into `images/`, committed, and sits in the repo forever with nothing pointing at it — the diagnostic on the failing run said it plainly: `["crepes.jpg", "ghost-recipe-that-does-not-exist.jpg", "scones.jpg"]`. Removing takes the photo now, and the confirm **changes its sentence** when there is one to lose, because nothing in this app deletes what a person was not told about. The download offers only what the book can reference, which also quietly covers the orphans a phone may already be holding from before. Six checks, including the two that keep it honest in the other direction: another recipe keeps her photo, and the download still offers the ones that are still referenced. Battery 903. |
| `R72` | 2026-08-21 | **The week is the reader's week, not UTC's — proven on two clocks a day apart.** A week planner has to agree with the calendar on the wall, and `toISOString()` is the shorter spelling that quietly hands back the **UTC** day: five hours behind, every evening after seven lands on tomorrow; one hour ahead, the first hour after midnight lands on yesterday. `app.js` was already right — it reads `getFullYear`/`getMonth`/`getDate` and contains no `toISOString` anywhere — but **nothing said so**, and nothing stopped the next refactor reaching for the shorter form. Now checked in Kiritimati (UTC+14) and Niue (UTC-11), which on the same instant are on different dates, so a UTC-based app cannot be right in both: exactly one day marked today, a meal planned into it stored under the **local** date, and drawn on the day marked. Mutation-tested by switching `isoDate` to UTC, which fails in Niue with the two dates side by side — and passes in Kiritimati, which is exactly why two clocks were needed. Plus the rule that `app.js` composes no date in UTC at all. **The one real defect was in the suites themselves**: three places seeded plan entries with `toISOString().slice(0, 10)`, so they only meant what they claimed on a machine set to UTC — CI is, a developer's laptop may not be. They compute the day the way the app does now. Battery 913. |
| `R73` | 2026-08-21 | **The course was the one guess the app never owned up to.** Every other guess an import makes is disclosed — servings that weren't found, a list split with no headings to go on, a missing title — and `082` is plain that a guess is flagged rather than silent. `guessCategory` fell through to **Dinner** for anything it could not read, and said nothing, so shortbread and tomato soup both arrived filed under Dinner and nobody was told to look. `R65` made the field editable *because* the app rewrites it; this is the other half — admitting that it did. It returns `""` for "I cannot tell" now, and every import path applies the default **and** the flag, in one shared sentence so it reads alike beside the servings guess next to it. Writing the counterpart check found a second gap: the pasted-text path never looked for a course at all, so even *"Category: Dessert"* written plainly in the text was thrown away — it reads a stated `Category:` / `Course:` line now, with cuisine deliberately left alone, being a tag and not a course. Five checks, and the one that keeps this from becoming noise is the negative: a course the source states is used and is **not** flagged. Battery 918. |
| `R74` | 2026-08-21 | **`recipes.json` is written one way, and that is a contract.** Three things write it — the app's download, `db/export.js` on the nightly sync, and a person with an editor — and all three produce `JSON.stringify(list, null, 2)` plus a newline in `FIELD_ORDER`. A hand-edit with different indentation, or with the keys shuffled, still parses to the same recipes, and then the next write reformats every line of a 71KB file. Not cosmetic, though not in the way it first looks: `db/export.js --check` compares **content**, parsing both sides, so a reformatted file does not by itself make the nightly sync think anything drifted. The cost lands later and lands hard — the moment a recipe genuinely changes, the write is canonical, so **one changed line arrives as a 71KB diff** with the real change buried in it, and the app's download behaves the same way. Nothing had drifted — the file is canonical today — so this is the guard, not the cure. Six checks: it parses and holds recipes, it is byte-identical to a canonical re-write, no byte-order mark, `FIELD_ORDER` was found to compare against, every recipe keeps its fields in that order, and it carries no field the writers would drop on the way out. Mutation-tested by reindenting to four spaces and shuffling one recipe's keys, which fails on both counts and names the recipe. Written into `db/README.md`. Battery 924. |
| `R75` | 2026-08-21 | **The nightly publish turns on a number, and nobody checked the two files still agree on it.** `db/export.js --check` exits **0** identical, **2** genuinely drifted, **3** refused-to-write, **1** something went wrong; `db-sync.yml` branches on exactly those, and only the 2 branch commits. Change the 2 to a 1 and the workflow reads it as *could not read the database* — the sync errors every night, **no video-imported recipe ever reaches the family again**, and a red cross nobody is watching is the only sign. Seven checks: the script run for real in the one state that needs no database (unconfigured must exit **1**, because an unconfigured sync that looks like *nothing to do* is exactly how it once went unnoticed for a fortnight), every code the workflow branches on being one the script can produce, the two that carry meaning named individually, an unrecognised code reaching the loud `else` rather than a commit, and the commit step being reachable **only** from the drifted branch. Mutation-tested by moving drift to exit 1. Writing it caught a detail worth keeping: the 0 and the 2 live inside a ternary — `process.exit(a === b ? 0 : 2)` — which a grep for `exit(2)` walks straight past, and the first version of this check did exactly that. Battery 931. |
| `R76` | 2026-08-21 | **The offline shell was checked in one direction only.** `R10` proves every file the worker promises to cache exists — `cache.addAll` is all-or-nothing, so one missing path leaves **no** offline support at all, silently. Nothing proved the other way round: that everything the app actually reaches for on a cold boot is on the list. Add a stylesheet, an icon, a second script, forget the list, and the outage test still passes — recipes still render — while the app is quietly wrong offline, in the typeface or the layout, for exactly the reader it was built for. Watched now: every same-origin request a cold boot makes must be in `SHELL`, with a floor so a boot that fetched nothing cannot read as a clean pass. The reverse rule is **deliberately not** asserted and says so — `manifest.json` is on the list and a browser may never ask for it. Nothing was missing; mutation-tested by dropping the bold face, which fails naming `/fonts/atkinson-700.woff2` — the one whose absence would be least visible and most felt. Battery 933. |
| `R77` | 2026-08-21 | **"From the original 4. 4 lines carry" — found by looking at it.** Twenty-six rounds of assertions had not caught this, because no assertion was ever going to: the sentence `R60` added put its count immediately after the servings number, so a rescaled stroganoff read **"Amounts adjusted from the original 4. 4 lines carry a second amount"** — two numerals with nothing but a full stop between them, which at 24px on the screen of someone with low vision is *4.4*. Caught by taking a screenshot and reading it, which is a lens the suites do not have. The original gets its noun and the count is spelled out — **"from the original 4 servings. Four of these lines carry…"** — so the two numbers can never touch. The check is the **shape, not the wording**: no numeral separated from another numeral by punctuation alone, which any future rewrite of the sentence still has to satisfy. Battery 934. |
| `R78` | 2026-08-21 | **The screen where you type a recipe in was 13.6px — at every text size.** Same lens as `R77`: a screenshot of edit mode showed the Serves box between − and + as a sliver, 41px of which 36 was `.input`'s own padding and border. Five pixels of window for a two-digit number, at every step, because `min-width: 0` let a field carrying two 48px buttons lose every flex fight. Fixing that led to the larger one behind it: the recipe screen puts the A−/A+ px on `.recipe` and lets everything inside be em, and **the Add / review screen never did** — so its title, ingredients, steps and servings all sat at 0.85em of body 16px. Set the text to 40px because you cannot read 24px, open the screen `ADDING.md` points the family at, and you got 13.6px boxes. The review form now carries the step exactly the way `.recipe` does. Checked as the criterion, not the symptom — a number input must be able to show the largest value it will accept, and the two forms must measure the same at every step — with a floor so the pair cannot pass by both being wrong. Writing it also caught the harness twice: a draft surviving in `sessionStorage` (so #add never showed the picker again) and a hash-only `goto` being same-document (so every size was the *previous* iteration's). Battery 945. |
| `R79` | 2026-08-21 | **The shopping list is recipe text, and it was frozen at 17px.** `R78` wrote the rule down — recipe text scales wherever it appears — and this is the other place it appears: the week plan's list is ingredient lines, the recipe's own words rescaled to the servings planned, and it is the screen you read **in the shop**, at arm's length, one-handed. Every line rendered at a hardcoded 17px at every step, and the plan screen carries no A−/A+ of its own, so it could not be fixed in place either. The list now takes the reader's step exactly the way `.recipe` does — which reaches paper too, the outcome `DECISIONS §033` had already settled and the print stylesheet already documented. Checked against the recipe screen: the same words at the same size on both screens, with a floor so they cannot agree by both being frozen. The round also found the **week planner had only ever been reflow-checked empty** — seven "Add dinner" buttons and nothing else, since `041`. The populated week is in `tests/zoom.js` now (it holds, 0px at 320px/Easy Read/40px), with a floor that proves the seed drew — one of the four recipe ids in the first draft was wrong, and the floor is what said so. Battery 949. |
| `R80` | 2026-08-21 | **An open menu that the keyboard could walk straight out of.** The sort menu was built as "a popup, not a sheet" — no scrim, dismissed by an outside tap — and it took none of the focus contract this app writes down for everything else: opening it left focus on `<body>`, Tab walked out of an open `role="menu"` into the page behind it, and closing dropped the caret at the top of the document because the re-render destroyed the element it was on. For a VoiceOver reader, activating "Sort" announced nothing and left the cursor where it was. **How a thing is drawn and where the keyboard is are two different questions**, and the popup styling was never the problem. It is on the same machinery as the sheets now — focus in on open, Tab trapped, Escape and choosing a sort both hand the caret back to the Sort button — while the outside tap deliberately stays a plain outside tap, because it has landed on something the reader meant to use. A menu opens on the option already chosen, scoped to `role="menuitemradio"` so the switches (Easy Read, Keep screen on) keep opening their sheets on the first control instead of moving depending on their own state. Both halves mutation-tested separately. **Correction:** `R79`'s row said zoom 14 / battery 949; the floor check landed after that count was taken and the true figures were 15 / 950. Battery 957. |
| `R81` | 2026-08-21 | **The rate limiter could be walked around by anyone who set a header.** `X-Forwarded-For` is a list the *client* can start and every proxy appends to — what arrives is `<whatever the caller sent>, <what the proxy actually saw>` — and `callerIp` took the **leftmost** entry, the half the caller writes. A loop rotating a fake header therefore got a fresh bucket on every request and **never met the wall in 130 tries**, with a paid Whisper call and a paid model call sitting behind it, per video. The limiter now keys on the last entry, the one the trusted proxy appended, and the assumption that buys it — exactly one hop, browser → Render → here — is written into `backend/README.md` along with what a CDN in front would break. Both directions are held: the forged header must not walk around the wall, **and** one visitor hitting it must not wall their neighbour — which is what the cheap fix (ignore the header, key on the socket) does, since behind one proxy that is a single address for the whole family. Mutation-tested both ways; the second mutation walls the neighbour on request zero. Battery 959. |
| `R82` | 2026-08-21 | **Paper was a screen the contrast audit had never looked at.** The print stylesheet carried a hand-written list — "these carry `--dim` / `--card-dim`, which are near-white on the dark theme and so vanish entirely on paper" — maintained by memory, and by the time anyone ran the audit under `media: print` it was missing **fourteen** things across both themes. Every tag chip at 1.46:1. The flag panel's "Worth double-checking" heading. The week's eyebrow. The shopping list's own title at **1.10:1 — invisible**. And all seven of `R60`'s "— 42g not adjusted" notes, which are the lines that tell a cook which amounts did *not* rescale: silently missing from a printed doubled recipe is the worst of the fourteen. The list is gone, replaced by one rule (`#app *` takes print ink) — anything needing a fill or a border on paper says so itself. Writing the harness mattered as much as the fix: the first version emulated print *before* setting the page state, and `.servbtn` is `display:none` on paper, so the rescaled route silently never rescaled and its seven failures were invisible. **A second print-truth fix rode along**: `As written, not summed:` is a `.hint`, and `@media print` hid every hint — so on paper the transcribed lines sat in the same column as the summed ones with nothing to say so, a list claiming a precision it does not have. Battery 962. |
| `R83` | 2026-08-21 | **The brightest control on the sheet said "don't".** One colour carries one meaning here: an accent fill is the thing you opened the screen to press. Three sheets were spending it on **Cancel** — the download sheet, the bulk-tag sheet, and worst of all the recipe picker, where you tap "Add dinner", get "Dinner · Monday 17" and a list of 48 recipes, and the loudest thing on the screen is the exit. That is exactly where a reader with low vision aims first, which is the whole reason the fill is loud. `Done` wearing it is right — that is finishing the task; `Cancel` wearing it is the same pixels meaning the opposite. The three keep their size, their place and their reachability (`R55` settled that a visible way out is not optional) and lose only the shout. The check reads the **word**, since the rule is about the word: no control whose label negates the task may wear the accent fill, and every such control must still be a full-size target — so the fix can never be to shrink or hide the way out. Found by taking a screenshot of the picker and looking at it, which is now three rounds running. Battery 965. |
| `R84` | 2026-08-21 | **The recipe picker was squashing its own recipes.** `.picklist` is a flex column with `max-height: 46vh; overflow-y: auto`, which reads as "some cards, scroll for the rest". It is not what a flex column does: **items shrink before a container scrolls**, and a card's automatic minimum is its min-content height — which here is the 64px icon slot, because the title and meta truncate and so contribute nothing to it. Every card was crushed to 44px with its own words drawn **27–49px outside the box**, over the top of the card below, at every font step, on the screen you use to plan a week. One line fixes it (`.picklist > * { flex: none }`) and the list scrolls as it always claimed to. Found the same way as `R77`, `R82` and `R83`: by looking at a screenshot. **The check also caught a suite-level fault of my own** — the new block navigates, and `S.listOpen` is in-memory, so `R82`'s print check downstream was measuring a shut fold and calling the empty result a pass. That block is self-sufficient now, which is the rule the whole file should hold to. Battery 968. |
| `R85` | 2026-08-21 | **The harmless mode was wearing the dangerous one's clothes.** The Menu has two modes behind two buttons sitting side by side: **Tag**, which adds a word, and **Remove**, which deletes a recipe. Tag mode was drawn in Remove mode's colours — its rows carried `.rrow`, whose own comment reads "Remove mode swaps every card for a danger-outlined row", and its Done button carried `.textbtn--removing`, a danger fill. One sentence of grey text was the whole difference between them. A danger colour that also means "you are adding tags" has stopped meaning careful, and someone who believes they are tagging is one tap from removing. Tag mode has the app's own selected grammar now (neutral until chosen, then the accent fill the filter chips use) and an accent Done, since ending the mode is finishing the task (`R83`). **Then the check found something worse, by asking a question nobody had asked**: neither mode had ever been in the contrast audit's route list, and adding them returned **192 failures — 48 rows × 2 modes × light theme**, all at **1.00:1**. `.rcard__meta` carries `--card-dim`, white-at-78% because it is drawn on the dark green *card*; `.rrow` is not a card, it sits on `--surf`, which in light mode is near-white. The contributor's name in remove mode and the tag list in tag mode — the one thing you open tag mode to read — were **exactly their own background colour**, and had shipped that way. Battery 972. |
| `R86` | 2026-08-21 | **Finishing the question `R85` only asked once.** Adding two missing modes to the contrast audit had just returned 192 failures, so "which states has the route list never visited?" deserved an answer rather than an anecdote. Seven more went in — the sort menu, a search that finds nothing, the week *with meals*, the recipe picker, the meal sheet, the bulk-tag sheet, and the photo lightbox, the app's one full-screen surface — and the fixture now attaches a photo, so the Recipe and Menu routes are audited with a real hero and thumbnail instead of the icon fallback. **All seven are AA clean**, which is the honest result: this round hardens rather than fixes. It did catch one thing, and it is the kind that matters — the floor asserting each state actually opened caught the meal sheet **never opening**, because the seeded plan entries had no `id` and the sheet keys off it. Four routes were auditing the screen behind an unopened sheet and reporting "AA clean". Every added route names the selector that proves it arrived. Battery unchanged at 972; the audit now covers 29 states × 2 themes × 2 Easy-Read settings, plus four on paper. |
| `R87` | 2026-08-21 | **The server's answers were shapes taken as promises.** `R62` settled the rule for stored data — coerce where you read, never trust — and the kitchen server is the same kind of input and more so: remote, deployed separately, able to be a version ahead of or behind the page asking. `normalizeDraft` already guarded the draft; the job **lists** did not, and they are rendered straight. `{jobs: "none"}` — a rename, a half-done deploy, an error body shaped like success — left a string in `S.videoReady`, and `.map` was called on it. **Not by the render that fetched it**: that one throws inside a `.then` with a `.catch` behind it, so the failure is swallowed and the bad value waits, detonating on the reader's next tap and taking down the Add screen, the way into the book for every new recipe. A `null` in an otherwise fine list did the same on `.id`. **The first version of the test passed all eight hostile shapes** — because it only checked that the screen was up, and the screen was already drawn before the fetch resolved. Tapping the theme button after the bad answer arrived is what made it honest, and it is the same trap this project keeps finding. **The first version of the fix was wrong too**, in a way worth keeping: it required a numeric id and silently dropped anything else, which broke `R54`'s hostile-id fixture and would have turned a future schema change into an empty waiting list with no explanation. A boundary passes through what it does not recognise, escaped; it does not swallow it. Battery 982. |
| `R88` | 2026-08-21 | **Two empty states that told the reader something untrue.** "Tap to check off as you go" was emitted *above* the branch, so on all four recipes with no ingredient list it invited a reader to check off items that are not there — directly above the panel explaining that there are none. And the Instructions section had no empty state at all: a recipe with no steps rendered a heading over an empty list, in silence. No recipe in the book is stepless today, but `recipes.json` is hand-editable **by design** and `R62` exists because that is not theoretical — a nightly sync, a bad merge, an import saved early. It now says so in the same words and offers the same route out (turn on Edit) that the ingredients section has offered since `071`. Both mutation-tested. The harness needed two fixes of the same kind the app keeps teaching: the overlay is read at boot so the stepless recipe needed a real load, not a hash change, and clearing the key afterwards was not enough — the next section was still looking at a book with one recipe in it. Battery 986. |
| `R89` | 2026-08-21 | **Offline, the app lost its italic.** `sw.js` precaches a hand-written SHELL array, and hand-written lists go stale — `R82` had just retired one for that reason. This one had never named `fonts/atkinson-400i.woff2`, which the app requests every time a rescaled recipe draws its kept-amount notes (**the lines telling a cook which amounts did *not* change**) or a search result says "matches ingredient". Offline the browser could not fetch it and synthesised an oblique from the regular face: a slanted approximation of a typeface chosen because the reader has low vision. The fix is one line; the durable part is the check, which does not read the list at all — it **walks the app, records every same-origin file actually requested, and requires the shell to hold all of them**, so tomorrow's asset is covered by nobody remembering. It sits opposite `R10`, which requires every path in the list to exist on disk: the two hold it from both sides. The Latin-Extended subsets stay out deliberately — no character in the book needs them, so they are never fetched. Bumping the cache to `v3` then exposed **two hardcoded `kt-shell-v2` strings in the suite**; they read the constant out of `sw.js` now, because a test that names a version is a test that breaks on the next one for no reason. Battery 990. |
| `R90` | 2026-08-21 | **The tutorial left out the one destructive thing in the app, and the best thing about it.** `R63` held `ADDING.md`'s sibling — the in-app How-to — to being *accurate*; this holds it to being *complete*. **Remove** sits on the Menu next to Tag and appeared nowhere on the page, which `R85` had just made worse by finding the two modes drawn in the same colours: the tutorial's silence was the only thing left distinguishing them, and it was silent. And **nothing told the family the book works with no signal** — there is a service worker precaching a shell so a kitchen the wifi never reaches still opens the recipe, real machinery keeping a real promise that no reader could discover. Both are now bullets in "Changing a recipe", in the page's own voice. The mode check is **derived from the Menu, not listed in the test**, so a third mode tomorrow asks for its own paragraph rather than slipping past. Battery 993. |
| `R91` | 2026-08-21 | **The day cap bounded the money, not the family's access to their own importer.** Forty video imports in 24 hours is a wall against a surprise invoice, and it works. It is not a wall against *one* caller taking all forty — which costs a stranger nothing, since the API has no login by design and its address ships in the page, and leaves the family unable to import until tomorrow. A caller may now take at most **15 of the 40** (`KT_CALLER_DAY_CAP`), always leaving 25 for everyone else. **`R81` is what made this worth building**: before it the caller key was the leftmost `X-Forwarded-For` entry, so anyone could rotate a header and be a new caller every request, and a per-caller count would have been theatre. Counted in memory on purpose and said so plainly — this is a *fairness* valve, not a spending wall, so a spin-down forgiving someone costs nothing while the database-backed forty still holds. Asked **after** the day cap, so that when the kitchen really is closed everybody hears the same sentence rather than one phone being told it is resting. Deliberately generous, because a household shares one address. Also checked this round and found already sound: the Menu's filter and sort **do** reach the address (`#menu?cat=Dinner&sort=az`), the Menu header **is** sticky and hit-testable at every scroll depth, and `recipes.json` is 72KB raw / **21KB gzipped**, not the ~300KB a stale note claimed. Battery 1003. |
| `R92` | 2026-08-21 | **A 200 is not the same as an answer — and this one told the family to close the page.** `R87` coerced the job *lists* at the kitchen-server boundary; the **submit reply** was trusted outright: `S.videoJob = { id: r.job_id }`. So a 200 carrying no usable id — a renamed field, a half-done deploy, a proxy's own cheerful JSON — produced the worst outcome this app is capable of. The progress card appeared and began stepping through *"Waiting its turn… Fetching the video… Listening to it"*. The pasted link was cleared, so there was nothing left to retry. The card said **"you can close this page, the finished recipe will be waiting"** — a promise that could never be kept. And it polled `/api/import/jobs/undefined` forever, every 404 swallowed as transient. CLAUDE.md's own line for it: a change reported as kept and silently dropped is the worst thing this app could do. Nothing is claimed now unless the server says what it did with the link, and the link survives so it can be sent again. **This reached the share sheet too**: a video shared from YouTube auto-submits through the same function, so the failure landed on someone who had done nothing but tap Share. Found by walking a shipped path nobody had exercised this session — the share target — and the other four failure modes it surfaced (unreachable, 503, 500-with-HTML, 502-empty) all already reported honestly. **It also forced a real judgement against `R54`**, whose check expected a hostile submission id to be *polled* as one encoded path: that guarantee moved to the listed-draft path, and the submit path now refuses, because *showing* an unrecognised thing and *claiming success* on it are different promises. Battery 1025. |
| `R93` | 2026-08-21 | **The scaler swept over the whole book — and the mutations caught my checks, not the app.** `R56`–`R60` found four real scaling bugs, every one by hand, in the code that decides how much of everything to use: an air fryer set to 780°F, `1-1/2 tsp` doubling to `2-1/2`, a lasagne whose entire sauce never scaled. The existing checks cover one recipe; this sweeps all 48 at 1 serving, at 40, and at their own count. **The app came through clean** — no NaN, no vanished amounts, nothing rewritten. The honest finding is about the checks: the first version claimed a "round-trip" proof and **two mutations walked straight through it**. `scaleLine` short-circuits at mult 1, so returning to a recipe's own count is tautological; and comparing the DOM against what the DOM showed a moment ago passes any transformation applied consistently. Both are fixed — the amount-survives check now notices a quantity rounding away to nothing and leaving `" tsp salt"` behind, and the book check compares against `recipes.json` rather than against itself. The comment states exactly what the sweep is worth and what it cannot see: a value scaled *wrongly* still sails through, so `R58`'s and `R59`'s bugs would not have been caught, and those still need a person. Also 70× cheaper than it first appeared — 140 seconds of harness round-trips became 2 seconds by driving the servings control in-page. Battery 1029. |
| `R94` | 2026-08-21 | **The tutorial promised a wider search than the app has.** `README.md` is exact — search reads titles, ingredients and tags, and "steps are deliberately not searched this way" — and `matchField` agrees with it. The in-app help said search "looks inside the recipes too, so 'bacon' finds **anything** with bacon in it", which is a promise the app does not keep. Not hypothetical: **`purée` is in Crannachan's method, and searching the book for that exact word, accents and all, returns nothing at all** — a cook chasing the thing they half-remember from the steps gets an empty screen and no reason for it. The sentence now says what is read and, in one clause, what is not. Same class as `R63` and `R90`, and checked the same way — **behaviourally, not by wording**: the test lifts a word that appears only in some recipe's method at runtime, confirms search finds nothing, and only then holds the help to it, so a future rewrite has to stay true. Found while fuzzing search with regex metacharacters (`(`, `[`, `\`, `(?:` — all handled as literals, nothing thrown) and noticing that an accented word in the book returned zero. Worth recording: **no accented word exists in any searched field**, so `087`'s diacritic folding is currently unobservable in the real book — the help's own "creme finds crème" example describes a rule that holds but cannot be demonstrated on this data. Battery 1033. |
| `R95` | 2026-08-21 | **Every list the Menu can make shared one scroll position, so a filtered list opened at its own end.** The position was filed under the screen's *name* — `menu:` — but the Menu is not one screen: it is every list the filters, the search and the sort can produce. Scroll the whole 48-card book, go back to Main, tap **Desserts**, and the six-card list inherited an offset far past its own bottom; the browser clamped it, and the reader landed on the LAST recipe of a list they had just asked for, with the heading, the search box and the first four recipes above the viewport — measured at 624px on a 624px-tall page, first card 383px off-screen. It is worst exactly where it matters most: the bigger the text step, the taller the unfiltered book, the deeper the offset that comes with you. Two halves, and **both proved load-bearing by mutation** — the position is now filed under `menuHash()`, which is already the canonical description of the list on screen, *and* a Menu-to-Menu navigation that changes the list now counts as navigation at all, which it never had (same name, same id), so the scroll/focus/announce block simply never ran for it. Removing either half puts the reader back at 624. Read live rather than from the parsed route on purpose: chip taps rewrite the list by `replaceState` without a navigation, so the saved position has to follow the list actually being read — which is what keeps the promise this must not cost, checked alongside it: a filtered list you leave for a recipe still gives you back your place in it. Found while verifying an unrelated claim (the Course sort **does** follow the documented ten-category order — my probe had used `sort=cat`, which `parseHash` correctly ignores, so I was reading the default order and calling it a bug). Battery 1036. |
| `R96` | 2026-08-21 | **The shopping list keyed on the unit as written, so one ingredient came out twice.** "cups" and "cup" were two different units to it: plan Boiled Eggs beside Chicken Lasagne and the list reads **"1½ cups water" and "1 cup water", one line apart** — the arithmetic the list exists to do, handed back to the person in the shop. Twelve such splits exist in the 48 recipes as they stand; four are a bare plural and three an abbreviation of the same word (`tbsp` / `tablespoons` olive oil across five recipes). Fixed with a **named unit table**, and both rules on it matter: it collapses only SPELLINGS OF ONE UNIT — never a conversion, so tablespoons are never folded into cups and no amount is ever restated in a unit the recipe did not use — and it works off a list rather than a stemmer, because the word in the unit slot is just the first word of the line and is often not a unit at all. That is what keeps "3 teaspoons minced garlic" and "4 cloves minced garlic" apart, and "1 large egg" away from "1 medium egg"; both are checked. A merged line says the unit in full, since neither original spelling is the whole truth of it any more, and a line that never met a second spelling keeps the recipe's own words, abbreviation and all. **The fix's own first draft was wrong and the suite caught it**: inflecting to agree with the total ran both ways, turning "½ cup celery" into "½ cups celery" on thirteen lines — a card says half a cup, never half a cups. The threshold is at-or-below-one now, and the guard that names it fails on the mutation. The closing hint stops excusing the old behaviour ("Same wording and unit sum together") and describes the new one. Battery 1044. |
| `R97` | 2026-08-21 | **A recipe that never said how many it serves was told it had been rescaled.** `normalizeRecipe` *deletes* a servings value it cannot read — the one place in this app that discards rather than coerces — and the recipe screen filled the hole with four. Three lies stacked on it, and the sentence held them all: **"Amounts adjusted from the original undefined servings."** The literal word `undefined`, in a line a cook reads at 24px; a claim that the amounts were adjusted when **nothing had moved** (`mult` has no base, so it is 1); and a card reading "4 people" for a recipe that never said four. Worst of it: **the stepper still ran** — tap + and the number goes to 5 while every quantity stays exactly where it was. A cook could set eight and cook the amounts for two dozen, told in writing they had been adjusted. Now: the card says **Not given**, the ± buttons are off, and one sentence says the book does not record a count so the amounts are exactly as written and cannot be rescaled. Reachable by the workflow this project is built on — `recipes.json` is hand-editable by design, `db-sync` rewrites it nightly, and `CONTENT.md` §1 is 34 inferred servings counts: *"makes about 2 dozen"* is the honest answer for a batch of cookies and is exactly what someone will type. Eleven checks, six on the fault and five on the half that must not regress — a recipe that does say still shows it, still steps, and **still really rescales**, which is what separates this from simply switching the feature off. **The same hole had a second home**, so the fix would have been honest on one screen and not the other: the week plan filled it with *one* — a meal card reading "Dinner · serves 1", a number nobody chose, and a meal sheet offering a stepper for it that moved that number while the shopping list went on using the recipe's amounts exactly as written. **And a third**, which is where the hole started writing itself into the file: `saveDraft` read `parseInt(field) || r.servings` inside a clamp, and with nothing on either side of the `||` that arithmetic is **NaN** — which `JSON.stringify` writes as `"servings": null`, into the overlay and then straight into the `recipes.json` somebody commits. Open a countless recipe, press Save, and the book gains a key it never had. **And the picker**, the screen where you choose the recipe in the first place, which was still writing the raw value unescaped — `" · serves " + r.servings` — and read *"Desserts · serves undefined"*. One predicate (`hasCount`) now answers the question for all five places, and a field the book does not have stays absent. Found by auditing every read of `.servings` rather than stopping at the screen that reported it: the second, third and fifth faces were all downstream of the same hole and none of them would have shown up in the first test. Twenty-five checks across `polish` and `plan` — the fault on every screen, and the halves that must not regress: a recipe that does say still shows it, still steps, **still really rescales**, and a typed count is still saved. Every fault check fails on its mutation. The tutorial takes `R94`'s standard with it — the bullet promising that − and + change the amounts now admits the recipes where they cannot. Battery 1070. |
| `R98` | 2026-08-21 | **The handler that promises no broken image kept it for the picture and broke it for the button.** Its own comment states the case and the rule: a recipe can reference `images/<id>.jpg` that was downloaded but never committed — *Download updated recipes.json* and *Download photos* are two separate buttons — and "the broken-image glyph must never be the fallback". It removed the `<img class="r-hero">`. But that image is the entire content of a `<button class="herobtn">`, so what was left was **358×44 of nothing**, sitting between the header and the recipe title, offered to a screen reader as *"Show the photo full screen"* — and it still worked: pressing it opened the lightbox, a full-screen modal dialog whose only content was the broken image, a class the handler did not cover at all (`naturalWidth: 0`, measured). The control goes with the picture now; the lightbox, still reachable on a slow connection where the button is painted before the 404 returns, says what happened and keeps its close button rather than being yanked away. And the branch list gained a **default**: five named classes were five out of the six kinds of `<img>` the app renders, which is the shape of bug the `sw.js` SHELL list already has checks on both sides of — anything unlisted now disappears rather than showing the glyph. The stand-in sits on `--card`/`--card-ink` rather than straight on the scrim, where contrast would depend on whatever happened to be behind it. Ten checks; the guards — a photo that loads still shows its hero and still opens full screen — pass before and after. Battery 1080. |
| `R99` | 2026-08-21 | **The share target asked for `text` and then threw it away.** `manifest.json` declares `params: {title, text, url}` — and `tests/video.js` has asserted that since task 018 — which is what makes the app an option in the share sheet for a *selection* rather than only a link. But `consumeSharedLink` fished a URL out of the shared string and **returned null when there wasn't one**, having already stripped the query from the address bar: select a recipe in Notes, share it to Kitchen Table, and it opens on the front screen with nothing on it and no way back to what you sent. Measured — `hash: ""`, `search: ""`, not a word of the recipe anywhere on the page. The words *are* the recipe, and the app already has the place for them: **the paste box**, the one import path that needs no network at all. Pre-filled, never parsed on arrival — the URL path pre-fills too, and only a video ever submits itself. **My own guard caught a hole in my own fix**: `?text=%20%20%20` is truthy, so three spaces opened an empty importer nobody asked for; the early return is trimmed now. Honest about reach: iOS does not implement Web Share Target, so the family's phones cannot take this path through the OS share sheet today — it is Android and desktop Chrome, and any `?text=` address. The feature is shipped either way, and a shipped feature that discards what it asked for is a bug wherever it runs. Battery 1086. |
| `R100` | 2026-08-21 | **`recipes.json` has two writers, and they disagreed about `null`.** The phone writes the file through `orderFields` in `app.js` when someone presses *Download updated recipes.json*; the nightly `db-sync` writes it through `rowToRecipe` in `db/export.js`. Each carries its own hand-typed `FIELD_ORDER`, in two files, with nothing binding them — the same shape of gap `R89` closed for the offline shell. The fields matched; **the emptiness rules did not.** `app.js` dropped `undefined` and `""`; `db/export.js` dropped `null` as well. So a recipe carrying `"notes": null` — which a hand edit leaves, and `recipes.json` is hand-editable *by design* — came out of the phone **with** the key and out of the sync **without** it. Commit the phone's file, run `node db/export.js --check`, and it reports DIFFERS over a difference nothing can see; the next sync rewrites the file to strip them, and the one after that is a spurious commit in a book of someone's recipes. Demonstrated end to end — a real download from the app beside `rowToRecipe` on the same content — before a line was changed. One condition fixes it: `null` goes too. The guard is the other half and is written the `R89` way, against what the system actually produces rather than a second list: the two `FIELD_ORDER`s must be **identical, in order**; every key any recipe in the book carries must be on them; every key `backend/lib/validate.js` can emit must be too, since an accepted import reaches the file through the sync; and **the two writers must produce the same recipe from the same row**. Nine checks. Battery 1095. |
| `R101` | 2026-08-21 | **The contrast audit could not reach a state that needed particular data.** Every route in `tests/contrast.js` was handed the *same* seed — one theme, one Easy-Read flag, one photo, one week's plan — so a screen that only exists for a particular recipe was structurally unreachable, and simply went unmeasured. That is why `R97`'s **Not given** servings card and `R98`'s missing-photo stand-in were argued from their tokens rather than measured when they were built: there was nowhere to put them. `R85` found **192 failures** the moment two unswept modes were added and `R86` caught four routes auditing the screen behind a sheet that never opened, so *which states can this audit reach* is the question, not *are these two fine*. A route now carries an optional fourth element — its own `localStorage` seed, applied after the shared one so it can override it — and the first to use it is a recipe whose count the book does not record. It is in the `need` map too, so it has to prove it opened. **Honest about what this is**: a coverage round, not a defect fix. Both new states were measured first at 320px, top text step, with and without Easy Read — no overflow, no clipping, no sideways scroll — and the sweep finds them AA clean. The value is that the audit can reach data-dependent states at all, including whatever the next round adds. Battery unchanged at 1095; the sweep is counted separately. |
| `R102` | 2026-08-21 | **The help undersold what works with no signal, in the direction that costs the most.** `R90` added the offline bullet and it is right about the book — a precached shell means the recipes open in a cellar. Its last sentence was not right about adding one: *"Only the four ways of adding a recipe need the internet."* One of those four is **Type it in**, which touches the network at no point — a blank form, filled in, saved to `localStorage` — and so does the paste box `R99` now sends shared text to, the path `CLAUDE.md` calls the only one that can never break. Proved before rewriting a word: with every off-origin request aborted, a recipe typed in lands in the book. That is the wrong direction for a tutorial to be wrong in. Someone standing in a kitchen the wifi never reaches, with Joan dictating, reads that sentence and **does not try** — and getting her recipes down is the one thing this app is for. Same lineage as `R94` and the servings clause in `R97`, and checked the same way: behaviourally first, then the help is held to what was just demonstrated, so a future rewrite has to stay true. Battery 1100. |
| `R103` | 2026-08-21 | **You could not add or remove a line in Edit mode. Four buttons, one misspelled attribute, and the whole point of the screen gone.** The single click handler reads `data-key`. Edit mode's *+ Add ingredient*, *+ Add step* and every *Remove* button were written with `data-k` — the spelling the **input** handler uses for field bindings. So `key` came back `null`, `S.draft[null]` is `undefined`, and every press threw `undefined.push` / `undefined.splice` into a console nobody reads. Measured: ten ingredient fields before, ten after pressing Add; ten after pressing Remove; six steps before and after. **The buttons look live, they press, and nothing happens.** Typing still worked, which is what made it look fine — you could retype a line that was already there and nothing else. That is Edit mode's entire job: *Changing a recipe* is one of six help topics, `ADDING.md` walks the family through it, and the download-and-commit workflow exists so someone can **fix** Joan's recipes — not delete a wrong line, not add a missing one. It hid because the Add screen's review form uses `data-key` on the same controls and works, so every import test passed, while the Edit-mode tests only ever edited the title. The convention is now true in both places: a **field binding** carries `data-k`, a **click action** carries `data-key`. Then the audit that should have existed: read from the handler which actions dereference `key`, walk the app collecting all 110 controls it renders, and require every one of those to carry a key — derived from the source, not from a list anyone maintains. It found no second face — and neither did the same question asked **the other two ways**, which the guard now asks too: the input handler binds by `data-k`, so a field written with `data-key` would be just as dead (all 28 are right); and a control can be dead simply because *nothing branches on its action*, so every one of the 47 actions the app renders is required to be among the 98 a handler answers (none orphaned — after fixing the character class, which excluded `+` and reported `serv+` and `fs+` as false deaths). A guard that looks one way is the "five classes out of six" mistake `R98` fixed. Battery 1119. |
| `R104` | 2026-08-21 | **A deleted line took another line's tick with it.** The check marks are keyed by position and cleared only when you LEAVE the recipe, so a line deleted in Edit mode slides every tick below it up one. Tick butter and sugar, delete butter, come back: **"1 cup packed brown sugar" reads as done, and nobody ticked it** — measured exactly that. The cook skips it. Skipping an ingredient is the harm; an un-ticked line only ever gets checked twice. So a list that changed loses its marks, **each list independently** — fixing a step is no reason to forget which ingredients are already in the bowl — and the app **says so** rather than appearing to lose someone's place, once the save is real (`R44`). Remapping the ticks by matching text was the cleverer option and the wrong one: text changes too, and a wrong remap is precisely the harm being avoided. **This arrives with `R103` because `R103` created it** — while the delete button threw, the list could not change under you, which is the honest reason it was not a bug yesterday. Guards: a title-only edit keeps the ticks and says nothing. Battery 1127. |
| `R105` | 2026-08-21 | **Pressing Go on the Menu's search did nothing — the one screen you reach in order to search.** The Return key on an iPhone reads *Go*; the on-screen keyboard is standing over the bottom half of the results, and pressing it is the obvious move. On Main it opened the top hit. On the Menu it did nothing at all: no navigation, and the keyboard did not even drop. The handler asked `t.id === "menu-q"` — that string is the field's **`data-act`**; its id is `menu-search`. No element in this app has ever carried the id `menu-q`, so that half of the condition **had never once been true**. It reads plausibly because `pick-q` genuinely is both an id and a data-act, which is how the two namespaces got confused — the same confusion `R103` found going the other way, one round earlier, which is what sent me looking. Measured before and after: 14 hits on "chicken", first card `#bacon-ranch-chicken-casserole`, hash unchanged. Guarded on there **actually being a search term**, because the Menu's list is `a.rcard` whether anyone has searched or not: unguarded, Go in an empty box opened whichever recipe happened to sort first — measured, not assumed, by removing the guard. The keyboard now goes away either way, because a search matching nothing prints *"No recipes match"* in exactly the place the keyboard was covering. Then the audit that would have caught it, derived from the source rather than a list anyone maintains: **every id the app looks up must be an id it can render** — `getElementById` and `.id ===` on one side, literal and templated `id="…"` on the other. Writing it found a false positive of its own first (`typeof j.id === "number"` is a type test, not a lookup), which is the reason the guard carries a check that it can still fail. All four mutations — the id, the empty-query guard, the blur, and a synthetic dead lookup — fail exactly the checks that name them. Battery 1138. |
| `R106` | 2026-08-21 | **Every press threw the caret onto `<body>`.** A render throws the screen away and builds it again, so the element under the caret stops existing. Focus was carried across **by id** — and only the FIELDS carry ids, so only the fields ever came back. A button re-rendered by its own press dropped the caret onto `<body>`. That is not cosmetic, and both faces were measured before a line was written. The **servings stepper became a one-shot by keyboard**: press + once, the caret is gone, press + again and *nothing happens* — 4 people could never become 6, which is the stepper's whole job. And **every ingredient ticked off threw a screen reader back to the top of the page**, on the screen someone is standing at a hob using, a line at a time. This is the user this app was built for: the typeface, the 24px floor and the 44px targets are all there because of low vision, and the caret was being dropped on every single press. The fix costs nothing new: a control is found again the way **`openSheet` already finds the one it must return to** — by its action plus whatever says WHICH one (`data-key`, `data-i`, `data-id`, `data-d`). Same vocabulary, asked on every render rather than only when a sheet closes. **Restored only on the same screen**, and that guard is load-bearing rather than decorative: the theme button is drawn on every screen, so pressing Back with the caret on it announced the destination's theme button *and then* the heading — `["BUTTON:theme","H1:"]` without the guard, `["H1:"]` with it. Counted, not sampled, because a final-state check cannot see it. The three contracts that already existed still win, because all three run after the render: a route lands on its heading, an opening sheet takes the caret, a closing one hands it back. Guard: every id-less control the app draws must be pickable out **alone** by that selector — walked across six routes and Edit mode, 60+ controls, and it can still fail (proved with a planted duplicate). Scrims are the one exclusion, and only scrims: a backdrop drawn as a button carries the sheet's close action but is never a place the caret returns to. **And the mark is compared as values, never assembled into a selector** — which is the second half of this round, and `tests/sec.js` is why. The first cut built `[data-act="…"][data-key="…"]` and escaped it; the escaping guard (`R54`) refused to let a new expression through unlooked-at, and looking found the hole: `recipes.json` is hand-editable by design and `normalizeRecipe` runs `parseTags` only when tags are **not** already a list, so a tag holding a newline reaches the DOM as written — `[data-key="sun<LF>dried"]` is a CSS syntax error, and pressing that chip threw out of the **middle** of `render()`, leaving the screen standing while the sheet focus and the draft snapshot at the end of it never ran. Comparing attribute values cannot fail on any input, so the escaping question stops existing rather than being answered: the guard now needs **no exemption at all**, which is the sign the second design is better and not merely quieter. Losing the caret is a shrug; a convenience that can take down a render is not. Battery 1153. |
| `R107` | 2026-08-21 | **A refusal and an outage got the same sentence, and it promised a retry that was never coming.** Save on a video draft writes to this phone and then tells the kitchen server, so the database gets the reviewed recipe and the job leaves the waiting list. That call fails two ways and they are not the same thing: the server could not be **reached**, or the server **looked at the recipe and said no**. `validateRecipe` refuses a contributor over 60 characters, a notes field over 2000, a list over 100 items, a servings outside 1–40 — every one reachable from the review screen. Both got *"couldn't be told **yet** … stays in the waiting list **for now**"*. For a refusal that is a promise of a retry that is never coming: the job sits in the list forever, the server said exactly what was wrong, and the app threw the words away — **the error was not even bound in the catch**. Measured side by side: a 400 and a 503 produced byte-identical notices. `kitchenFetch` had always known the difference — it marks an answered error so it will not retry one — then built a fresh `Error` and **dropped the flag on the floor**, so no caller could act on it. It now carries `answered` and `status` as **two facts, not one**, and the distinction earns its keep: `answered` still governs retrying, unchanged; `status` governs what a caller may SAY, and only a **4xx** (less 408 and 429, which mean *ask again* rather than *no*) is the server refusing this payload. The first cut used `answered` alone and called a **503 — a Render service still waking** — a refusal, which is the transient case wearing an HTTP status; the mutation that restores that crude test fails the outage check by name. The refusal now repeats the server's own words, drops the "yet", and still says the recipe is safe here. Battery 1161. |
| `R108` | 2026-08-21 | **Both empty-state panels asserted things that were not true — and `R103` is what made anybody able to see them.** A missing list gets a panel that says so loudly; that is `071` and it is right. What it *said* was not. *"This recipe's list didn't survive transcription"* is a guess about a **cause**, and a wrong one for every recipe that was never transcribed — **all 48 shipped ones**. It was unreachable prose while Edit mode's Remove button threw; the moment `R103` fixed that, anyone can empty a list in a few taps and be told an import they never ran went wrong. It also named **Joan** in a book with six contributors, so a recipe of Jennifer's asked for Joan's original. And *"This recipe has ingredients but no method"* is contradicted by the panel **directly above it** the moment both lists are empty — two sentences on one screen, one of them false whichever way you read it, and that state is two taps away now. An empty state says what is **missing** and what to do about it; how it came to be missing is the flag machinery's job (`R82`), and that speaks from what was recorded rather than from a guess. The panels now name the recipe's **own** contributor, read from the record. Same rule as `R102` and as the sentence `R104` wrote into `saveDraft`: *a message that has stopped being true is the same fault as one that was never true.* Guard: no panel may name a contributor the recipe does not — walked across three seeded recipes and all six names, which is precisely how "Joan" survived into a six-contributor book. Both mutations — the transcription claim with its hardcoded Joan, and the "has ingredients but" clause — fail exactly the checks that name them. Battery 1171. |
| `R109` | 2026-08-21 | **The help made a claim about the book, and nothing had ever checked it against the book.** *"A few recipes never said how many they make"* — and **none of the 48 do**; every recipe carries a count. The behaviour it describes is real and reachable (clear the number in Edit mode, or hand-edit `recipes.json`), but the claim about the **collection** was not, so a reader could go looking for one of those few forever. It reads as a condition now, not a census, and is true whether or not such a recipe exists. `R94`, `R102` and this are the same fault three times — the help describing an app that has moved — each found by reading a sentence and going to see, with nothing checking any of them. So now something does, **two-way on purpose**: the day a recipe without a count enters the book, a help that says nothing about it fails just as loudly (proved by deleting a real `servings` and watching the check flip). Second correction: the go-key sentence never said **where**, and it only ever worked on the front page — on All recipes the same key did nothing at all until `R105` — so a reader who tried it there was told the app does something it did not. It works in both boxes now and the sentence says so. **And one claim I nearly broke by "fixing" it.** Measured against page height alone, *"Add recipe at the bottom of the All recipes screen"* looks plainly wrong: the pill sits two cards down a 7,400px list, ~6,800px above the end. It is **`position: sticky`**, pinned to the bottom of the viewport at every scroll position — the help was right and the reading was wrong. Measuring before writing is what caught it, and the sticky behaviour is now pinned by a check so neither the pill nor the sentence can drift from the other. Battery 1180. |
| `R110` | 2026-08-21 | **Five states a reader can land on that the contrast audit had never reached — and the two reasons it could not reach them.** `R85` found 192 failures the moment two unswept modes were added, so *"which states can this audit even reach"* stays the question. Added: a **plan that outlived its recipe** (task 127 — the slot degrades to the name it was planned under and says "No longer in the book", a `.rcard__meta` on a surface that exists nowhere else); a **recipe with nothing written down**, drawing both of `R108`'s panels at once — a heading and a paragraph inside `.panel--flag` that the flagged route does not carry; a **photo that failed while the lightbox was open**; the **waiting card**, which is not an edge case but where a reader sits for MINUTES while a video is written up, carrying the cold-start wording `CLAUDE.md` requires to read as *"waking up the kitchen…"* rather than as an error; and the **refusal** shown for a link the server cannot take. `R101`'s per-route seed answers *which recipe*. It cannot answer *what did the wire do* — `R98` wrote the lightbox branch for a case its own comment describes, the hero painted before the 404 comes back — nor *what was half-finished when the page reloaded*, which is where the waiting card lives (gameplan 084, in **sessionStorage**). A route may now do both: stub the network, and seed session as well as local storage. The lightbox is reached by leaving the photo **hanging and then refusing it**: the request never completes, so the hero button is painted with nothing in it yet — exactly `R98`'s case, the button existing before the failure comes back. **The first cut got this wrong and CI caught it.** It served the hero a real image marked `no-store` and refused everything after, on the theory that the lightbox must go back to the wire. `no-store` governs the HTTP cache; it does not stop a browser reusing an image it has already **decoded** for an identical `src` in the same document. Locally it re-fetched and the state opened; in CI it did not, and four routes audited the screen behind a lightbox that was never there — **the floor caught it a second time, in CI, on the fix for the first time it caught me.** Never letting the photo load leaves nothing to reuse: a state, not a race, and verified at two click timings 1.5s apart. The floor now **waits** for its selector rather than sampling once, because a state that arrives off a network failure lands when the failure does and not on a fixed timer. All five clean in both themes × Easy Read; the sweep stays at **0**. **The `R86` floor earned its keep on this round's own work**: the first cut of the lightbox route seeded no photo at all, so nothing opened and four routes reported "AA clean" while auditing the screen behind a lightbox that was never there — the exact failure the floor was built to catch, caught. Battery unchanged at 1180 (the audit is counted separately). |
| `R111` | 2026-08-21 | **`ADDING.md` is the page a person is handed, and nothing had ever checked it against the app.** Its own opening says a step that does not work as written *is a bug in the page* — an invitation nobody had taken up. `R109` had just found the in-app help drifted three times (`R94`, `R102`, `R109`); a markdown file read by whoever is standing in a kitchen drifts faster than a screen does, and the reader it is written for is the least able to work around a wrong instruction. **Everything in it checked out** — the sticky green pill, the four ways in, *"Ready to check over"* as the exact heading, the paste box under the link field, the share target in the manifest, the endpoint the iPhone Shortcut posts to, all three button labels word for word, the Remove flow, and the undo appearing only once there are local changes to undo (which the doc describes correctly). So the round is the check rather than a fix, and the rule is derivable rather than a list anyone maintains: **a label these docs put in bold quotes must exist, verbatim, in the app** — bold alone is not the signal, since they bold plenty that is not a control, but bold *plus quotes* is how they mark a thing to press. Plus the one list the walkthrough spells out by hand: the categories, which went from six to ten once already, checked **both ways** — the doc drifting and the app drifting each fail it, proved by mutating each in turn. Battery 1185. |
| `R112` | 2026-08-22 | **Nothing had ever driven the book the family actually opens.** Every suite runs against a local copy served from the repo root — the right way to test the code, and blind to everything only a deploy has: Pages serves from a **subpath**, with its own headers and MIME types and whatever service worker was published last. `tests/live.js` drives the real URL in a real browser: it renders, it holds **every** recipe the published `recipes.json` holds, one opens with its ingredients and its method, the servings stepper moves, a line ticks off, Atkinson comes from the deploy rather than a fallback, no file the page asked for is missing, nothing throws and the console is clean. **14 checks, all green, 25 files answering 200.** Off CI on purpose: a third party's uptime must not gate a pull request. The browser cannot use this session's egress proxy, which is a problem `tests/ocr-live.js` had already solved — every request is satisfied from node via curl, so the page is driven exactly as a phone would drive it and only the transport is bridged. **Proving it locally first was not enough, and that is the lesson.** The local run passed 14/14; against the real host nothing rendered. A CONNECT proxy answers *"HTTP/1.1 200 Connection Established"* **before** the origin says anything, and each redirect adds another block — so splitting on the first blank line handed the page an `app.js` whose first line was `HTTP/2 200`. A syntax error, a blank screen, and no error anywhere that named the cause. Header blocks are consumed until what remains stops looking like a response. A local copy has exactly one block, which is precisely why only the real thing could show it. Also removed: `tests/_probe94b.js` and `_probe94c.js`, two throwaway probes `R94` committed by accident — no assertions, referenced nowhere, dead since the day they landed. |
| `R113` | 2026-08-22 | **The zoom suite failed every Saturday and Sunday, on any change or none — and it went red mid-session with nothing touching it.** `tests/zoom.js` seeds a week at `today`, `today`, `+1`, `+2` and requires all four meals to draw. The planner draws **Monday to Sunday of the current week**, so `today + 2` walks off the end of it on a Saturday and `today + 1` does on a Sunday: four meals Monday through Friday, three on Saturday, two on Sunday. It passed at 23:0x on the Friday and failed at 00:33 on the Saturday, on a branch whose only changes were a contrast route and a documentation guard. **Nothing was wrong with the app, and nothing was wrong with the change** — the calendar moved. Seeds are anchored to the displayed week's Monday now, so every day of the year lands the same four meals. **Second face, fixed with it:** `tests/contrast.js` seeds its planner states the same way, and there it degrades instead of failing — on a Sunday one of the two meals simply does not draw and the sweep audits a thinner screen without saying so, which is worse than red. Both now use **local date parts** rather than `toISOString()`, for the reason the app's own `isoDate` does: the UTC day and the local day are not the same day for half the world, and a plan is kept in the cook's own days. Found because `R112` sent me back through every suite one at a time looking for a CI failure the contrast fix had not caused. |
| `S01` | 2026-08-22 | **A write gate, because the worst case changed.** Until now a stranger who found the Render address could spend money on imports, and the rate limiter, the caller day cap and the URL allowlist bounded that. An endpoint that changes recipes moves the worst case from a bill to **Joan's recipes** — silently, with nobody able to say who did it. One shared family passphrase (`KT_WRITE_KEY`), entered once per phone; no accounts, no identity, and **`contributor` still never consulted** — the passphrase says *someone in this family is holding the phone*, not *this is Jennifer*. **Fails closed**: unset means nobody writes, because the unconfigured state is the dangerous one. Compared over SHA-256 digests so the comparison is fixed-length and the real key's length is not measurable. Wrong answers walled at ten an hour per caller — a family passphrase is attacked by patience, not by flooding — and **correct ones are never counted**, so ordinary saving never walks toward the wall. A near-miss refusal is byte-identical to no attempt at all. Lives in `lib/writegate.js` rather than inside a request handler, so it is **proved rather than trusted**: 12 checks. |
| `S02` | 2026-08-22 | **`PUT /api/recipes/:id` — the one thing the import path deliberately will not do.** `acceptJob` suffixes on an id collision because two people accepting one draft must not silently replace each other. An edit is the opposite instruction: someone opened **this** recipe, changed it and pressed Save, and writing it anywhere but over that row would be the bug. So it overwrites — and takes the id from the **path**, so a body cannot redirect the write onto a different recipe. Upsert, not update, because a recipe that has never been through the database is the normal state of all 48. `position` is deliberately never updated: **fixing a typo must not jump a recipe to the top of "recently added".** Tags are replaced rather than merged, or removing one would never stick. |
| `S03` | 2026-08-22 | **A change reaches the family in minutes, not overnight.** `db-sync` already regenerates `recipes.json` from the database and commits; a write now asks it to run. Three rules, all about not making a write worse: a failed poke **never** fails the write (the recipe is already in the database and the nightly run would publish it anyway); it is **debounced**, so four typos fixed in a row cause one publish; and no token means no poke and no complaint in the write path. **The tests caught a real bug in it**: `lastAt` started at 0, so the *first* poke after a start was measured against the epoch and debounced away — and this server sleeps on Render's free tier, which makes "the first poke after a start" most of them. The one dropped would have been the one somebody was waiting on. |
| `S04` | 2026-08-22 | **Save can now mean everyone.** With the passphrase in, Edit mode's Save writes to `localStorage` and says so, **then** sends the recipe to the kitchen. The order is the whole design: the phone's copy is saved and reported saved before a byte goes anywhere, so a server asleep on the free tier costs the reader nothing they typed. With no passphrase the behaviour is exactly what it always was — nothing sent, nothing claimed. `R107`'s rule governs the sentence afterwards, on the path it was written for: a **4xx** is a refusal that will never clear (*"It stays here until that is sorted"*), a **503** is Render waking (*"try Save again in a minute"*), and a kitchen that took the recipe but cannot republish says *nightly* rather than *a few minutes* — because that one would be a promise nobody kept. All five paths driven end to end. |
| `S05` | 2026-08-22 | **The one recipe field the app turns into a URL took any two thousand characters at all.** `image` is the only one that becomes an address, and `validateRecipe` checked its length and nothing else. Nothing catastrophic was reachable through it — the page's CSP is `img-src 'self' data: blob:`, so a remote address never loads, and `R98` clears a broken picture away quietly — but the result is **a dead link committed into `recipes.json` and published to everyone**, and since `S04` that string arrives from a phone rather than only from an import review. Pinned to the one shape the app itself writes (`orderFields`: `"images/" + id + ".jpg"`), which "Download photos" already writes the files to match. Refused now: a remote address, a protocol-relative one, a `javascript:` URL, `../../`, a path climbing out halfway, a file type the download never writes, and a kilobyte of noise. **The shape, not the exact id** — `recipes.json` is hand-editable by design and two recipes sharing one photo is a reasonable thing for a person to write. And the name allows exactly what an id allows, because two rules about the same slug that disagree is a bug waiting for whoever writes the first id starting with a hyphen — checked by generating a photo path for every id shape the validator accepts and requiring it to accept those too. Plus the cross-file guard `R100` earned: the app's writer and the server's rule live in different files and have drifted before. Battery 1247. |
| `S06` | 2026-08-22 | **The arc I had just shipped left a false sentence on the help page.** *"Your changes live on your phone only. Nobody else sees them until they're published."* was true for the whole life of this app, and `S04` made it false for any phone holding the family passphrase — where Save reaches everybody. The same fault `R94`, `R102`, `R108` and `R109` each found, a sentence that has stopped being true, **except this time the round that broke it was mine, one round earlier.** Worth saying plainly: shipping a feature includes the sentences it falsifies. The page reads **this phone** now rather than describing phones in general, so it cannot be half-right: the passphrase is either in the box or it is not, and it says which — and when it is, it says *within a few minutes* rather than *instantly*, because the server may only manage the nightly run. Checked from **both sides**, since a claim that is right half the time is exactly what got us here; and three things it must NOT have swept along: the download-and-commit path is still offered in both states because it is the one that cannot break, the passphrase is never printed on the page, and **removal is still described as this phone only** because `DECISIONS.md S` deliberately left it local. The sharing half is a state nothing had audited, so it joined the contrast sweep too (`R110`'s question, asked again). Battery 1257. |
| `S07` | 2026-08-22 | **The same false claim had a second home, and there it made the document contradict itself.** `S06` fixed the in-app help. `ADDING.md` — the page a person is actually handed — said it in two more places, and nobody had looked: its opening (*"it saves **on your phone only** … nobody else will"*) and its Fixing-mistakes bullet (*"Same rule: on your phone until published"*). The `S` arc had **added a section to that same file** describing the passphrase that sends changes to everyone, so for the length of one round the walkthrough told the family both things. This is the discipline the loop keeps earning — `R97` found four extra faces, `R98` an incomplete branch list, `R100` a second writer in another file — applied to a fault I made: **fix the screen that reported it, then go and find where else it lives.** The opening now turns on the one box that decides it, says the app tells you which happened every single time (*believe the sentence, not the setting*), and names the two things that never travel — **removal and photos** — because those are still local by design and sweeping them along would be the opposite mistake. Guarded two-way off whether the app can share **at all**: the flat claim returning fails it, and so does `shareEdit` disappearing while the walkthrough still promises the passphrase. Both proved by mutation. Battery 1262. |
| `S08` | 2026-08-22 | **The panic button was over-promising, and it is the control somebody presses when they are worried.** *"Undo all my changes on this phone"* drops the local overlay and falls back to the published book. That was the whole story until `S04` — and a change sent to everyone **is** the published book now, so pressing this **restores** it rather than removing it. The old wording (*"undoes everything changed, added, or removed on this phone"*) would have someone press it believing a shared mistake was gone, while it sat on the website and on everyone else's phone. *"Anything already downloaded and committed is unaffected"* gestured at this and does not cover it: a person who shared with the passphrase never downloaded or committed anything. It now says **which kind of change it can reach** and what to do about the other kind — open the recipe, put it right, Save again — and only says it on a phone that actually shares, because there is no *everyone* to warn about otherwise. Read from the **dialog a person is actually shown** rather than from the source. Third face of the same arc-made fault: `S06` the help, `S07` the walkthrough, `S08` the one that could have cost somebody a recipe they thought they had put back. Battery 1270. |
| `S09` | 2026-08-22 | **Typing a new recipe in did not share it, and nothing said so.** `S04` wired sharing into Edit mode only, which left the family a rule nobody could have guessed: **change** a recipe and everyone sees it, **type a new one in** and only you do. The video path had told the server since the day it was built, so two of the three ways in already shared — and the quiet one is the one somebody uses to add Joan's card off the counter. Every other new recipe goes the same way now when the phone holds the passphrase: same endpoint, same local-first order, same honest reporting. **Not both, for a video draft** — `acceptVideoJob` writes that recipe already and asking twice would be one write arguing with another. Asserted rather than assumed: the accept test now also requires **zero** PUTs. Found by asking the `S06`/`S07` question one more time — *where else does this arc's rule apply and nobody was told* — and getting an answer that was not copy. Battery 1280. |
| `S10` | 2026-08-22 | **Removing said "from the collection", and the `S` arc turned that vagueness into a trap.** One action described two ways: `R71` wrote the photo half as *"from this phone"* because that is what it does, while the other half still said *"from the collection"* — a bigger-sounding place, and already vague. On a phone that shares, *the collection* reads as **everyone's book** — and removal is deliberately local (`DECISIONS.md S`). The harm is the worst shape a wrong sentence can take here: someone removes a recipe believing the family lost it too, so it stays live for them **and** their own copy is gone, leaving them unable to open it and put it right. Both halves say *from this phone* now, and a sharing phone is told the recipe stays in everyone else's book — *removing is the one thing this phone keeps to itself*. **The first mutation of this was a false pass**: a `perl` substitution that silently failed to match, and a suite that passes against unmutated code looks exactly like a suite that passes against a fixed one. Redone with an asserted anchor, it fails two checks by name. Sixth face of the same arc. Battery 1288. |
| `S11` | 2026-08-22 | **A tag change made to twenty recipes at once reached nobody.** `S04` wired sharing into the two places a recipe is written one at a time — Save in Edit mode, and `S09`'s Save on the Add screen — and missed the two places this app writes to **many**: Tag mode's *Add tags*, and renaming or merging a tag. Both wrote the overlay and stopped, on a phone where every single edit reached the family, and both said *done* — "Tagged 20 recipes.", "Renamed to X — 14 recipes updated." — with nothing to say that here, *updated* had stopped meaning everyone. Worst possible place for the gap: tag hygiene is the one part of this app built to keep the **whole book** consistent (`067`–`069`), so a rename landing on one phone only is exactly how the near-duplicate tags it exists to prevent get made — and the next ordinary edit from the other phone puts the old spelling back. `shareEdits` sends every recipe a bulk change touched, **stops at the first failure** (one outage is one answer, not a wasted 30-second wait per recipe), and reports how far it actually got. Every write but the last carries **`?more=1`**, which holds the publish poke: without it the *first* row of a 48-recipe burst fires the republish and the publisher's own 90-second cooldown guarantees no later row can fire a second one — a race between Neon and a GitHub runner starting up, proved real in the suite before the cure is asserted. A phone closing mid-burst never pokes and the nightly catches it: late, never wrong. Seventh face of the same arc; the round also found `CLAUDE.md` still claiming removal's sentence was *left alone*, which `S10` had changed. Battery 1329. |
| `S12` | 2026-08-22 | **The thirty seconds after "Saved" said nothing.** `S04` set `S.sharing` before each write and `S.shared` after it, and **nothing ever read either one** — write-only state meant to be an indicator that never became one. Measured: the phone saves, the button reads *Saved ✓*, and a request then runs for up to half a minute against a Render free tier waking up, with the screen blank — `shareEdit` passes `quiet` to `kitchenFetch` deliberately, so even the *waking up the kitchen…* card is suppressed. Close the page there and you lose a change the app had just called safe. The rule it settles: **the eye gets progress, the ear gets one sentence** — the visible line counts through a burst (*"Sending to the family's book — 3 of 12…"*) while `liveMessage()` returns a constant sentence, so `announceOnce` drops the repeat and 48 recipes are not read out 48 times. Four hand-written `if (S.notice)` blocks became one `noticeHtml(cls, style)`; there had been nowhere for a second line to go, which is part of why this was never built. `S.shared` deleted rather than wired — it only repeated the notice. video 186 → 201; both mutations fail by name. **`sec` caught the new helper writing a raw `style=` within minutes of it existing** — every caller passes a literal, but the rule is that an attribute built by concatenation is escaped, so nobody has to re-derive per call site whether that value can be reached by anything a person typed. Battery 1344. |
| `R114` | 2026-08-22 | **`R54`'s exemption list asserted something about call sites, and nothing checked it.** The escaping rule exempts `id`, `act` and `cls` as *"literals passed by the caller"* — true today, checked by hand when each was added, and **unverified ever since**. One `tagsFieldHtml(r.id, …)` written next year leaves `sec` green while a recipe id flows unescaped into an `id=` and a `for=` attribute, and `normalizeRecipe` deliberately does not constrain ids because `recipes.json` is hand-editable by design and `db-sync` regenerates it nightly with nobody watching. The exemption is enforced now rather than remembered, the way `R48` did for colours and `R49` for the 44px carve-out: every call site of a function that interpolates an exempted parameter must pass a real string literal, and a locally declared `var cls = …` must read no property. Two floors so a rename cannot empty it. Mutation-tested both halves — passing `r.id` and assigning `S.videoJob.status` each fail by name, quoting the offending expression. Found by auditing for the `S12` defect class (write-only state — none left) and then asking what else the suites assert without checking. sec 53 → 56. Battery 1347. |
| `S13` | 2026-08-22 | **A change the kitchen could not take can be sent again.** `DECISIONS.md S` had this as deliberately-not-built: a failed single edit could honestly say *try Save again*, one tap away, but a failed bulk tag could not — redoing it means re-picking every recipe, and a rename cannot be redone at all once the old name is gone — so `S11` named the state and stopped rather than giving an instruction nobody could follow, and recorded that the real fix was a queue with a button. `kt.unsent` is that queue: **ids only, never recipes**, so every one is read fresh from the overlay when it is sent — a change made after the failure goes too, and an id whose recipe has since been removed drops out. Coerced where it is read (`R21`/`R40`/`R62`). Offered on the Menu only on a sharing phone, only while something waits, never mid-send. **Every bulk failure sentence now points at it**, which `S11` had no right to do; the single-edit sentence still says *try Save again in a minute*, because that reader is looking straight at the Save button. Said on the help page and in `ADDING.md`; the `DECISIONS` entry is struck through rather than deleted, because the reasoning is the point. Three mutations — never queueing, keeping a queued id with no recipe, never clearing on success — each fail by name. video 201 → 218. Battery 1364. |
| `R115` | 2026-08-22 | **A validator that judged the raw value and stored the trimmed one.** `validateRecipe` checked `r.title` and stored `r.title.trim()` — two different strings. Three spaces is truthy, is a string, and is under 300 characters, so every check passed and the trim made it empty on the way into the database: **a nameless recipe in everyone's book**, out of the one function whose whole job is to refuse what the app would never send. The contributor was worse — `putRecipe` inserts it into `kitchen.contributors`, so an empty one mints a blank row that becomes a blank tile under *Whose recipe?*, and a contributor outlives the recipe that created it. An optional field of spaces became `""` in a column meaning *nothing here*. **The same gap was in `db/migrate.js`**, on the path that reads the hand-editable `recipes.json` every night with nobody watching; both boundaries now hold the same line and are checked against each other. `image` stays deliberately strict (`S05` gave it a required shape, so whitespace is malformed, not absent) — pinned by a test so the asymmetry is a decision, not an accident of statement order. Found by auditing the write endpoint the `S` arc opened, which was the least-examined surface in the repo. `migrate`'s `die` exits rather than throws, so that half is measured in a child process — the way the nightly job actually behaves. Mutation-tested on both boundaries; each fails by name. backend 233 → 243. Battery 1374. |
| `R116` | 2026-08-22 | **A recipe with no name took two whole screens with it — silently.** `R62` coerced `ingredients`, `steps`, `flagged`, `tags` and `servings` at the storage boundary and left `title`, and three places sort by one: the Menu's A–Z, the Menu's Course, and the week planner's picker, all `a.title.localeCompare(b.title)`. `undefined.localeCompare` throws. Measured with **one** nameless recipe: A–Z drew **0 cards** with `#app` at 205 characters, `#plan` fell to 3 elements — two entire screens, not one row — and **`pageerror` stayed empty**, so it read as an empty book rather than a bug. A missing name also reached the reader as the literal word *undefined* in the browser tab. **The cure is render-time only, deliberately**: `normalizeRecipe`'s output is what *Download updated recipes.json* writes, so inventing a name there would commit it to everyone's book as though somebody had typed it. `titleOf(r)` gives *"Untitled recipe"* — the app's own existing word, already used for a nameless video job — to display, sort and match, while `startDraft` keeps the stored value so Edit offers an **empty** box and Save cannot bake the placeholder in; that line has its own check. Found by turning `R115`'s question on the frontend's own boundary. Two mutations fail by name — and reverting just the A–Z sort takes Course, the planner and the Menu with it, which is the blast radius restated. kt 255 → 263. Battery 1382. |
| `R117` | 2026-08-22 | **The copy that leaves the phone still said "undefined".** `R97` taught every screen to ask whether the book says how many a recipe makes and to print *Not given*; `R116` did the same for a missing name. Neither reached `recipeText` — which is **not a screen**, it is what goes to another person through Share and *Download as text*. On a recipe with no contributor and no count it read `From: undefined` / `Serves 4 (adjusted from undefined)` **while the screen beside it correctly said Not given**. The worst place in the app for it: everywhere else the reader sees the gap and judges it; here they hand it to somebody else in their own name. The count guard has to be `hasCount(r)`, not `S.serves === r.servings` — a countless recipe still has a working stepper, so `S.serves` is a real number while `r.servings` is absent, which is exactly how *adjusted from undefined* was produced. Print was already honest (`dl-pdf`/`plan-print` render the DOM), so only the two text paths were wrong. Found by asking `R116`'s question of the field beside `title`. The first control case was wrong and the suite said so — it borrowed a shipped recipe, which the authoritative overlay had replaced; the control is seeded now. Two mutations fail by name. kt 263 → 270. Battery 1389. |
| `R118` | 2026-08-22 | **A dot with nothing on one side of it.** The visible tail of `R116`/`R117`: four lines built as `a + " · " + b` with both halves taken on trust, so a recipe with nobody named read **"Dinner ·"** on its card, **"· Dinner"** on its own page, **"Dinner · · matches ingredient"** when the search had something to add (a doubled separator the screen sweep had not reached — the suite found it), and left an empty `<p>` on the front page. Small on a screen and not small in a kitchen: this app exists because someone reads it with low vision, a leading middle dot is punctuation noise where a name should be, and a screen reader says it out loud. `metaLine(parts)` drops the empties and joins the rest — and deliberately does **not** escape, because every caller assembles already-escaped parts and one carries a `<span>`. The hero's empty line was measured at **zero height** before the record claimed it cost space; the comment says what was measured. Mutation fails by name. kt 270 → 277. Battery 1396. |
| `R119` | 2026-08-22 | **Editing a recipe quietly rewrote a serving count nobody touched.** `R97` settled that a recipe with *no* count must not gain one by being edited; `saveDraft` still ran `Math.min(40, typed)` over whatever the field held, **including a value the reader never went near**. Measured: a recipe stored as **200** — a church-hall pot, which people genuinely write — opened in Edit, title changed and nothing else, Save pressed → stored servings **40**, silently, and on a sharing phone straight into the family's book. Three places disagree about the range and that is what let it hide: `normalizeRecipe` accepts 1–999, `saveDraft` clamped to 40, `validateRecipe` and the `kitchen.recipes` column allow 1–40. **Making them agree is a schema change and Jason's call — recorded, not taken**; not rewriting what a person wrote is not, so a count the reader did not touch survives untouched and one they *did* type past the limit is clamped **and said**. Found by probing implausible-but-present data after the bare-recipe sweep (`R116`–`R118`) — overflow, 60 ingredients, 40 tags and a 999 stepper were all clean; this was not. Two mutations fail by name. kt 277 → 285. Battery 1404. |
| `R120` | 2026-08-22 | **The form went on showing what the save had refused.** `saveDraft` has always tidied on the way past — a cleared title falls back to the old one, blank lines are dropped, and since `R119` a count past the limit comes back to 40 — and **none of it was ever shown back**. Measured: type 300 into Serves, press Save; the notice correctly says *"300 was saved as 40"* and the field goes on reading **300**. Told the right thing and shown the wrong thing in the same moment, and the box in front of them is the one a person believes. Clearing a title was worse — field empty, book quietly holding the old name, nothing said at all. Fixed **structurally rather than one patch per field**: the form is re-seeded from the recipe actually stored, so screen and book cannot disagree; it also picks up the tidying nobody had thought about, like blank ingredient lines. Found by asking `R119`'s question of the other fields `saveDraft` writes. Mutation fails by name. kt 285 → 292. Battery 1411. |
| `R121` | 2026-08-22 | **Typing a recipe in guessed like an import, and never said so.** The Add screen's save read `Math.min(40, Math.max(1, parseInt(...) || 4))`: a count left blank became **4**, one typed past the limit became **40** — both the right value, both reached in silence, with an empty `flagged` list and nothing on screen. **The values were right; the silence was not** — and this app already knows better, because every import path that cannot read a count defaults to 4 and *flags* it (*"Servings — no count was found; 4 was assumed."*), which the recipe page shows as a Double-check chip (`082`). An import cannot ask; a person typing can leave the box empty, so this was the one path guessing without disclosing. Same sentence now, so one situation has one wording. The clamp is **said** rather than flagged (`R119`'s distinction: a flag is for checking later, a number you typed a moment ago needs telling now) — which needed `S.carry`, a sentence that outlives the one navigation this screen makes. Closes the open item carried since an earlier session. The first test harness reused one context and the fill silently missed, reading 4 where the probe measured 40 — a check added that the field really held the value under test. Two mutations fail by name. add 79 → 89. Battery 1421. |
| `R122` | 2026-08-22 | **A flag that had been dealt with went on saying it had not — for good.** `082` gave every flag the name of its field and a Double-check chip beside it; **nothing ever took one down**. `saveDraft` never mentioned `flagged` and there is no dismiss anywhere, so an import that could not read a count kept saying *"no count was found; 4 was assumed"* after the reader had typed the real one, permanently, beside a number they had set themselves. A *worth double-checking* list that never empties is a list nobody reads — costing the reader the one mechanism built to tell them what still needs attention, and it is the fault this project keeps finding (a sentence that stopped being true) on the feature whose whole job is to be true. The rule is the naming convention itself: **a flag naming a field is answered when that field changes**; one naming no field is left alone, because nothing can tell whether it was dealt with and inventing a dismissal for those is a different feature. Found by asking what `R121`'s new flag does after the reader acts on it. The harness needed fixing twice — a re-seed that `goto` ignored (same hash, no reload) and a vacuous list check — and now proves its own seed took. Mutations in **both** directions (never clear, clear everything) fail by name. kt 292 → 301. Battery 1430. |
| `R123` | 2026-08-22 | **The review screen let you fix a flagged field and kept the flag anyway.** `R122`'s sibling, on the screen built for exactly this job: the import review shows the flags *and* every field they name, then carried all of them through verbatim (`addFlags = (d.flagged || []).slice()`). An import that found no title flagged *"Title — none was found on the page; add one."*, the reader typed one in the box **directly beneath that sentence**, and the recipe was born still carrying it. No baseline needed — *"none was found"* is answerable from what is being saved — which is what kept this cheap rather than threading a pristine draft through eight assignment sites. **The count was the exception and the first attempt got it wrong**: it assumed `R121`'s save-time logic would regenerate an inherited assumption, but that logic only distinguishes blank from filled, and a draft holding an assumed 4 is indistinguishable from a reader typing 4. The suite caught it; the count takes the course's rule instead (*4 was assumed* → a saved count that is no longer 4 was changed). The course itself stands when the category is still Dinner: agreed or never looked, and nothing can tell those apart. Two mutations fail by name, the second pinning exactly the flaw the suite found. add 89 → 99. Battery 1440. |
| `R124` | 2026-08-22 | **Two copies of one rule, parsing a shape a language model writes.** `R122` and `R123` each answered a flag by the field it names, each with its own copy of `/^([A-Za-z]+)\\s+—/` — the drift `R114` exists to stop, and worse than most because `backend/lib/extract.js` asks the model for *"Field — what needs checking"* and then stores whatever comes back **verbatim**: every other field there is coerced, flags alone are taken as typed. A model writing *"Servings: no count was found"* yields a flag neither round can ever answer — `R122`'s permanent stale warning returning through an unwatched door. Fixed on the **reading** side on purpose: flags have four writers (extraction model, link parser, photo parser, hand-edited `recipes.json`), so a field-name list on the server would be a fifth thing to keep in step; one tolerant reader covers all and cannot drift from itself. **The mutation testing earned its keep**: dropping the separator altogether passed every check, because the prose case began *"The"*. The gap was real — *"Steps taken after the oven were never shown"* would have read as an answered `Steps` flag and vanished — and is pinned now, so the separator is load-bearing rather than decorative. A vacuous check (`=== null || true`) was caught and replaced in the same pass. kt 301 → 308. Battery 1447. |
| `R125` | 2026-08-22 | **The stylesheet could accumulate fossils and nothing noticed.** `R48` has protected `tokens.css` since it was written — no colour outside it — but **nothing protected the other direction**: a rule for a class the app stopped emitting stays forever, and the next reader of `style.css` cannot tell a live rule from a fossil. Three had accumulated: `.form-grid` (the app draws `.topgrid`/`.bodygrid`), `.main__marks` (never rendered), `.sheetbtn--acc` and its entry in the shared focus-ring list — 16 lines. Removed, and the gap closed the way `R48` closed its own: **enforced rather than remembered**, with two floors so a scan that stopped finding classes cannot pass silently. The exemption list is present and empty on purpose, carrying `R114`'s rule — a concatenated class could trip this honestly, and the first one gets a name and a reason rather than a loosened check. `tokens.css` stays outside the scan: verbatim from the handoff, the one file this project does not tidy. Nothing visual moved — `contrast`, `zoom`, `quick` and `feat` all unchanged. Found by auditing for dead code after confirming **none of the 171 functions is unreachable** (two apparent hits were `.map(normalizeRecipe)` and `setInterval(pollVideoJob)`, checked rather than assumed). polish 272 → 275. Battery 1450. |

---

## 11. Waiting on you

*The loop writes here when it hits a 👤 or an unanswered 🤝. Everything below
blocks something. Clearing an item here unblocks the loop.*

| Task | What is needed | Blocks |
| --- | --- | --- |
*Rewritten 2026-08-01 after the completion push. Everything below needs a
person; nothing below can be done by an agent, and none of it blocks another
agent task — the agent queue is empty.*

| Task | What is needed | Blocks |
| --- | --- | --- |
| `004`–`007` | **A physical iPhone.** Does Atkinson render; Save-to-Notes through the real share sheet; a 40-minute wake-lock bake; print to real paper. | `015`, `035`, `042`, and re-checking `049`/`054` on-device |
| `015`–`024` | **Sessions with Joan and one other family member**, on their own phones. All ten of Phase 2 is watching real use. | `023` re-checks `027`; `024` extends `033`; `020` shapes `074`; `022` feeds `025` |
| `025` | The family's answer: is "download and commit" acceptable friction? **The check on the provisional no-server ruling.** | reopening `026` |
| `035`–`040`, `042` | **VoiceOver on iOS, Reduce Motion + Increase Contrast.** Findings amend `design/a11y-criteria.md` and may reopen `055`/`056`. | the deferred section of `034` |
| `072`–`074`, `076`–`078` | **The content truth pass — sit-downs with Joan.** `CONTENT.md` is the worklist: 4 ingredient lists, the `parsnips` truncation, 34 servings, first tags, first photos, chasing the other four contributors. Rule 5: nothing inferred. | the `029` done-bar for 1.0; re-running `080` after |
| `081` | **Twenty real photos of Joan's recipe cards** through the proven pipeline; write down the honest error rate. | final wording of the `082` flags |
| ~~*light the kitchen*~~ ✅ | **Done 2026-08-03.** All four env vars are in Render (`KT_DB`, `ANTHROPIC_API_KEY`, `GROQ_API_KEY`, `YT_API_KEY`) and the server reports `missing: []`. Instagram imports work end to end; YouTube works through the Data-API salvage, both proven with live jobs. Remaining courtesy items: flip the service's Branch to `main`, and rotate the Neon password. | — |
| *`KT_DB` Actions secret* | Same value, repo Settings → Secrets → Actions, so the nightly `db-sync` keeps `recipes.json` telling the database's story — including recipes accepted from video imports. | accepted imports reaching the published file |
| *rotate the credential* | The Postgres password transited a chat; rotate it in the Neon console once the env vars are placed (the two spots above are the only two places it lives). | hygiene, not function |
| *(struck)* `096` | ~~Neon Data API decision~~ — superseded 2026-08-02: the Render import server is the small worker; see §10. | — |

---

## 12. If only three things happen

If this plan is never finished, these three are the ones that mattered:

1. **Phase 8 — the content truth pass.** The app is finished and the data is not.
   34 inferred servings and four empty ingredient lists are the difference
   between a working app and a trustworthy one.
2. **Tasks `004`–`007` — real-device testing.** The one device that matters has
   never run this. The font, the share sheet and the wake lock all behave
   differently there, and all three are load-bearing.
3. **Phase 2 — the session with Joan.** Every accessibility decision so far was
   made from a specification rather than from watching her use it. She is the
   standard, not WCAG. AA is the floor.
